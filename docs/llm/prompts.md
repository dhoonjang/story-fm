# 프롬프트 — 모델에게 무엇을 어떻게 말하나

**프롬프트는 코드다.** 상수가 유일한 원본이라 지금 도는 문장이 곧 저장소의 문장이고,
변경은 diff로 남는다. 런타임 오버라이드는 두지 않는다.

**프롬프트에 관한 결정은 전부 여기 산다** — 원칙(§1), 모델이 받는 입력의 해부(§2),
출력 문법(§3), 프롬프트마다 무엇이 어디에 사는가(§4), 스킬 표면(§5), 판정과 권한
경계(§6·§7), 그리고 **일부러 싣지 않는 것**(§8). 누가 읽는지는 [agents.md](./agents.md),
어느 모델로 가는지는 [models.md](./models.md).

## 1. 원칙 — 좋은 프롬프트는 이렇게 생겼다

롤플레이 커뮤니티의 프리셋과 제공자의 가이드는 같은 방향을 가리킨다 — 짧은 역할, 입력의
지도, 적은 규칙, 예시, 그리고 데이터와 지시의 분리. 그것을 이 게임의 구조에 맞춘 것이
아래 열한 줄이고, 모든 프롬프트와 입력 빌더가 이것을 지킨다.

1. **역할은 한 문장이다.** "당신은 게임 마스터다. 유저는 감독이고, 당신은 감독을 뺀 세계
   전부다." 그 뒤는 전부 규칙이다.
2. **지도를 먼저 준다.** 모델이 받을 블록의 이름과 몫을 시스템 프롬프트 앞머리에서 한 번
   알린다(`# 입력`). 이름 붙은 블록이 무엇인지 모르면 모델은 그것을 지시로도 대사로도
   읽는다.
3. **규칙은 적게, 한 줄씩, 하는 쪽으로 적는다.** 금지는 "대신 무엇을 하는가"로 적는다
   ("시각 줄을 쓰지 마라" → "모든 줄은 @로 시작한다 — 시각 줄은 코어가 붙인다").
   이유는 규칙의 적용을 바꿀 때만 한 구절 붙이고, 비유·수사·체크리스트는 넣지 않는다 —
   모델은 설명을 읽고 설명대로 쓰며, 체크리스트를 주면 항목 수만큼 문단을 만든다.
4. **예시가 규칙보다 세다.** 출력 문법은 말로 적은 뒤 예시 한 장면(`<example>`)으로
   보인다. **형식만 담은 하나**다 — 내용이 실리거나 둘 이상이면 장면이 그 골격을 복사해
   모든 세이브가 같은 장면을 연다. 인물의 목소리는 카드의 예시 대사가 맡는다.
5. **프롬프트의 문체가 출력의 문체다.** 출력 문법에 없는 표기 — `**볼드**`·이모지·느낌표
   강조 — 를 프롬프트에 쓰지 않는다. "반드시"·"절대" 같은 강조어도 쓰지 않는다: 시스템
   프롬프트에 민감한 모델일수록 강조어가 과잉 반응(도구 과호출·허락 구하기)을 만든다.
   도구 설명도 같은 층이라 같은 규칙이다.
6. **데이터 블록에는 데이터만.** 인물 카드·레퍼런스·상태 스냅샷은 사실을 싣고, 규칙은
   시스템 프롬프트가 한 번 갖는다. 블록이 가질 수 있는 지시는 둘뿐이다 — 그 블록의
   표기를 읽는 **범례 한 줄**("기자가 아는 사실 (이 밖은 묻지 못한다)")과, **그 턴에만
   뜻이 있는 오퍼레이터 지시**("장면은 7월 14일에서 연다"). 카드에 실린 지시문은 이력에
   굳어 턴마다 쌓인다.
7. **한 규칙은 한 자리에만 산다** (§4). 원본이 둘이면 같은 도구를 한쪽은 "확률대로
   판정하라"로, 다른 쪽은 "확률은 참고일 뿐"으로 말하게 되고 둘 다 같은 턴에 실린다.
8. **수정은 지우는 방향이 기본이다.** 문제가 보이면 지시를 덧붙이지 말고 그 행동을 시키고
   있는 기존 문구를 먼저 지운다. 새 지시로 증상을 덮으면 원인은 남고 프롬프트만 길어진다.
9. **코어가 막는 것과 없는 도구는 프롬프트에 없다** (§8). 코어가 멈춰 세우는 일을
   프롬프트가 또 시키면 페이싱만 굳고, 없는 도구를 막는 문장은 모델이 그 대신 감독에게
   허락을 구하게 만든다.
10. **사실이 먼저, 감독의 말이 마지막이다.** 이번 턴 블록은 상태 스냅샷 → 인물 카드 →
    화면 조작 → 감독 발화 순으로 서고, 감독의 말이 모델이 읽는 마지막 줄이다 — 긴
    입력에서 질문이 끝에 올 때 답이 가장 정확하다.
11. **프롬프트 변경은 별도 커밋으로 분리한다.**

## 2. 입력의 해부 — 모델이 받는 블록과 순서

입력은 변경 빈도 순 층이다 (→ [agents.md](./agents.md) §5). 여기서는 **모델이 보는 그대로**
— 블록의 이름과 순서, 각 블록이 싣는 것과 싣지 않는 것 — 를 적는다. 블록의 이름은
`GM_SYSTEM`의 「입력」이 모델에게 알리는 이름과 같다.

### 평시 (GM)

| 층       | 블록                                                            | 싣는 것                                                         | 원본                                                |
| -------- | --------------------------------------------------------------- | --------------------------------------------------------------- | --------------------------------------------------- |
| 고정     | 도구 정의 · `GM_SYSTEM`                                         | 규칙 전부                                                       | `skill-descriptions.ts` · `gm-prompt.ts`            |
| 레퍼런스 | `[구단과 감독]`                                                 | 구단 · 감독 이름(화자 태그) · 배경. **세이브당 고정인 것만**    | `buildGmReference`                                  |
| 요약     | `[접힌 대화의 요약]`                                            | 압축된 세이브에만                                               | `buildGmDigest`                                     |
| 이력     | `[이번 장면의 인물]` · `[조작: …]` · `@감독:` · 모델 턴         | 창 안의 지난 턴. 카드는 실렸던 자리에 다시 선다                 | `buildGmHistory`                                    |
| 이번 턴  | `[상태 스냅샷]` → `[이번 장면의 인물]` → `[조작: …]` → `@감독:` | 스냅샷이 앞, 감독의 말이 끝(§1-10). 스냅샷은 이력에 남지 않는다 | `buildGmStateNote` · `describeCharacters` · `gm.ts` |
| 이번 턴  | 도구 결과                                                       | 조회·실행의 답                                                  | `gm-tools.ts`                                       |

- **상태 스냅샷은 어댑터가 감독 발화 앞에 붙인다** — Gemini는 유저 메시지 앞머리로,
  Anthropic은 오퍼레이터 채널(system)로. 어느 쪽이든 감독의 말이 마지막이고, 저장되는
  이력에는 감독의 말만 남는다.
- **레퍼런스에는 선수 이름·능력치·감독 수치·인물 카드가 없다** — 이유는 전부 캐시다
  ([agents.md](./agents.md) §5). 선수 이름은 스냅샷의 「선수단」 줄이, 수치는 조회가 낸다.
- **첫 장면**은 같은 조립에 `[오퍼레이터 지시 — 새 게임 첫 장면]` 한 블록이 스냅샷 앞에
  붙고, 수석코치 카드가 지목으로 선다.

### 경기 (해석 · 중계)

| 에이전트 | 고정                  | 레퍼런스                                                   | 이력              | 이번 턴                                                                                                                                                      |
| -------- | --------------------- | ---------------------------------------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 해석     | `MATCH_INTENT_SYSTEM` | —                                                          | —                 | `[경기 장부]` `[지금 걸어 둔 것]` `[공략 가능한 지점]` → `[감독]` 발화 (판세는 빠진다 — 분류에 쓰이지 않는다)                                                |
| 중계     | `MATCH_CASTER_SYSTEM` | `[구단과 감독]` + 수석코치 카드 + `[경기 전 감독이 한 말]` | 이 경기의 중계 턴 | `[경기 장부]` `[지금 걸어 둔 것]` `[현재 판세]` `[공략 가능한 지점]` → `@감독:` → `[이번 구간에 일어난 일]` `[구간 종료]` → `[감독의 지시에 코어가 답한 것]` |

- 경기에서는 **감독의 말 뒤에 사건이 선다** — 코어가 그 말을 받아 굴린 구간이라 순서가
  곧 인과다. 대화만 건 턴에도 `(진행 없음)` 대본이 와서 킥오프 턴과 갈린다
  ([agents.md](./agents.md) §3).
- 결산 셋·압축은 시스템 프롬프트 + 브리프 하나다. 이력이 없다.

## 3. 출력 문법

```
모델턴   := 시점헤더 (요소)+
시점헤더 := "[2026-07-13 AM 9:30]"  (평시, 모델이 쓴다)  |  "[67']"  (경기, 코어가 붙인다)
발화     := "@" 화자이름 ":" (연출 | 대사)+     — 이름, 직책 아님
내레이션 := "@:" (연출)+
연출     := "*" 텍스트 "*"
스킬호출 := 실제 tool call (렌더러가 칩 또는 카드로 표시)
유저턴   := "@" 감독이름 ":" 텍스트  |  조작
조작     := "[조작: 시간 진행 — 하루]"          — 화면 손잡이, 감독 발화 아님
```

- **첫 줄은 언제나 시점이고 한 턴에 하나**다. 평시에서는 이 줄이 곧 구단의 시계를
  움직이므로 GM이 쓴다(agents.md §2). **경기에서는 캐스터가 쓰지 않는다** — 시각의
  주인이 장부라 코어가 장부의 분을 붙이고, 모델이 적은 시각 줄은 화면에 닿기 전에
  걷힌다(agents.md §3 ④). 그래서 중계 프롬프트는 "시각 줄을 쓰지 마라"가 아니라
  "모든 줄은 @로 시작한다 — 시각 줄은 코어가 붙인다"다(§1-3).
- **헤더의 시각은 시간대(`AM`·`오후`)가 붙으면 12시간제, 없으면 24시간제로 읽는다** —
  `오전 12:05`는 자정 00:05, 시간대 없는 `12:05`는 정오 12:05, `14:30`은 14:30이다.
  24시간 헤더를 12시간제로 접으면 시각이 오전으로 뒤집혀 코어의 되감기 방지에 걸리고
  그 턴의 시계가 멎는다.
- **화자 태그는 이름**이다 — 카드가 있는 사람도 카드에 적힌 이름으로 말하고, 직책은
  화면이 붙인다 ([../data/people.md](../data/people.md)). 중계만 `@중계:`로 역할 태그를 쓴다.
  카드의 머리글이 태그를 함께 적는다(`[인물] 이름 (@태그:)`) — 동명이인으로 태그와
  이름이 갈릴 때 모델이 태그를 알 자리가 거기뿐이다.
- **선수 id는 서사에 쓰지 않는다.** 도구 입력에는 감독이 부른 이름을 그대로 적는다 —
  코어가 편다(agents.md §7). 서사에 새어 나온 id는 저장 전에 이름으로 바뀐다
  (`humanizePlayerIds`).
- **화자는 게임 내부의 수치를 입에 담지 않는다** — 능력치·성사 확률·적응도·소화율.
  "슈팅 84" 대신 "리그 정상급 왼발", "성사 확률 15%" 대신 "쉽지 않습니다". 금액·날짜·
  순위처럼 구단 사람이 실제로 말하는 숫자는 그대로 쓴다.
- **장면은 도구를 다 부른 뒤 한 번에 쓴다.** 사고 과정·검토했다 버린 선택지·`<thinking>`
  같은 내부 태그는 출력에 넣지 않는다.
- **연출을 여는 별표는 하나다** — `**볼드**`는 이 문법에 없다. 그래도 화면은 **연속된
  별표를 하나의 구분자로 읽고**(`splitStaging`) 별표를 어느 경우에도 남기지 않는다.
  별표를 하나씩 세면 `**`에서 경계가 한 칸 밀려 **대사가 통째로 연출로 뒤집히므로**,
  문법 밖의 출력 하나를 프롬프트에만 맡기지 않는다(AGENTS.md §6.7).
- **화면 손잡이는 `@` 채널 밖으로 싣는다**(`buildOperatorMessage`). `@:`는 GM이
  내레이션을 쓰는 채널이라, 조작을 거기 담으면 감독의 화면 조작이 모델 자신의 출력
  문법으로 이력에 서고 인물이 그 손잡이를 아는 것으로 읽힌다.
- **문법은 예시 한 장면이 보인다** (§1-4) — `GM_SYSTEM`과 `MATCH_CASTER_SYSTEM`의
  `<example>`. 이름은 어느 이름 풀에도 없는 가상의 것이라 카드의 이름과 겹치지 않는다.

### 문법도 코어가 지킨다 (평시만)

도구를 부르는 턴에서 모델은 반복마다 시점 헤더와 "…확인하겠습니다" 한 줄을 새로 찍는다 —
프롬프트로 눌러도 남는 습성이라 코어가 걷어낸다.

- `sanitizeSceneText` — 헤더는 맨 앞 하나만 남기고, `@`로 시작하지 않는 줄은 화면에도
  저장에도 내보내지 않는다.
- `filterSceneStream` — 스트리밍에도 같은 위생. 줄의 **첫 글자**만 보면 판정되므로
  지연되는 것은 줄 앞머리뿐이다.
- ⚠️ **`@` 줄이 하나도 없는 응답은 손대지 않는다** — 규약을 통째로 어긴 응답까지 지우면
  빈 턴이 되어 무슨 일이 있었는지조차 사라진다. 빈 턴보다 어긴 장면이 낫다.
- ⚠️ **중계에는 이 위생을 걸지 않는다** — 구간마다 헤더를 새로 찍는 것이 정상이다.

## 4. 프롬프트별 구성 — 무엇이 어디에 사는가

**한 규칙은 한 자리에만 산다.** 자리는 그 규칙이 **무엇에 걸려 있는가**로 정해진다.

| 자리                                    | 무엇이 사는가                                                                                                    |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| 시스템 프롬프트 (`GM_SYSTEM`)           | 도구와 무관하게 매 턴 서는 것 — 입력의 지도 · 권한 경계와 인물 · 장면의 속도 · 지시를 받았을 때 · 출력 문법 · 말 |
| 도구 설명 (`SKILL_CATALOG`)             | 그 도구를 **언제 부르고, 인자를 어떻게 채우고, 결과를 장면에 어떻게 옮기는가**                                   |
| 중계 프롬프트 (`MATCH_CASTER_SYSTEM`)   | 입력의 지도 · 사건을 옮기는 법 · 한 턴 · 출력 문법 · 말                                                          |
| 해석 프롬프트 (`MATCH_INTENT_SYSTEM`)   | 경기 중 필요한 판정·해석 근거 전부 — 도구 표면이 0이라(§5) 도구 설명이 실리지 않는다                             |
| 결산 셋 · 압축                          | 무엇을 보는가 · 규칙. 폭과 인원은 코어 상수에서 읽는다 (agents.md §4)                                            |
| 데이터 블록 (카드 · 레퍼런스 · 스냅샷)  | 사실, 범례 한 줄, 그 턴만의 오퍼레이터 지시 (§1-6)                                                               |
| 첫 장면 지시 (`ONBOARDING_INSTRUCTION`) | 누가 여는가 한 줄                                                                                                |

- **한 도구의 사용법을 시스템 프롬프트가 다시 적지 않는다.** 원본이 둘이면 갈린다.
- **여러 도구에 걸친 규칙은 도구 이름을 열거하지 않고** 시스템 프롬프트에 한 줄로 남긴다
  ("도구가 성공으로 답한 일만 완료형으로 쓴다"). `GM_SYSTEM`·`MATCH_INTENT_SYSTEM`에
  도구 이름이 한 줄도 서지 않는 것은 테스트가 지킨다.
- **평시 도구 설명과 경기 프롬프트가 같은 규칙을 갖는 것은 중복이 아니다.** 경기 중에는
  도구 설명이 실리지 않으므로, 없으면 같은 판정이 평시와 경기에서 다른 근거로 내려진다.
- 옮길 때는 더하지 말고 지우는 방향으로. 받는 쪽에 이미 있으면 지우기만 한다.

### `GM_SYSTEM` — 섹션의 순서

역할 한 문장 → **입력** → **감독과 세계** → **한 턴** → **지시를 받았을 때** → **출력
문법**(+ 예시) → **말**. 깨지면 가장 아픈 것이 앞에 선다 — 감독은 유저의 것, 한 턴은 한
장면. 형식은 예시 바로 앞에, 톤은 끝에.

| 섹션             | 갖는 것                                                                                                                                     |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| 입력             | 블록의 지도(§2) · 스냅샷과 카드 밖의 사실은 조회로 확인한다 — **이 규칙의 유일한 자리**                                                     |
| 감독과 세계      | 감독은 유저의 것 · 감독의 단정은 시도다 · 인물은 카드대로, 자기 자리에서 알 만한 것만 · 장면을 여는 사람 · 게임 밖의 일은 세계에 없다       |
| 한 턴            | 한 장면 · 결론짓지 않는다 · 선택지 대신 의견 · 골격을 달리한다 · 도구 뒤 한 번에 · 분량 · 시간은 흐른 만큼 · 하루 넘게 흘렀으면 하나를 골라 |
| 지시를 받았을 때 | 시킨 일은 도구로 · 모호하면 되묻기 · 이름 없는 지목은 직전 대상 · 선수 인자는 이름 · 도구가 답한 대로 · ❗ 협상 · 화면에서 바꾼 것          |
| 출력 문법        | 헤더 · @이름: · @: · `*연출*` · `<example>` 한 장면                                                                                         |
| 말               | 한국어 · 톤 · 수치를 입에 담지 않는다                                                                                                       |

### 인물 카드 (`describePersona` · `describeCharacters`)

카드는 **데이터**다 — 줄마다 사실 하나.

```
[이번 장면의 인물]

[인물] 이름 (@태그:) — 직책 · 원형
성격: …
동기: …                       (full 깊이에만)
말투: …                       (rumour에는 없다)
  예) …                       (full에만 — 지문만으로는 톤이 붙지 않는다)
관계: 이름 — 결이 맞는다/부딪힌다 (먼저 보는 것: 나 … · 상대 …)   (full에만)
감독과의 거리: 평판으로만 안다 — 말투도 속내도 모른다              (rumour에만 — 사실로 적는다)
있었던 일:
  날짜 — …
```

무엇이 실리고 빠지는지는 **깊이**가 정하고 그 판단은 코어의 것이다
(`characterEntry` → [../data/people.md](../data/people.md) §6). 카드에 없는 것은
§8에 있다 — 조회 포인터, 실명 가드, "화자 태그는 이름이다" 같은 규칙 문장.

### 상태 스냅샷 — 사실과 범례

`[상태 스냅샷]`은 매 턴 새로 조립되는 사실의 목록이다. 읽는 법은 둘로 갈린다.

- **`GM_SYSTEM`이 갖는 것** — 「주의」의 ❗(판정 대기 협상), 「감독이 화면에서 바꾼 것」(이미
  반영된 사실), 「선수단」(명단이지 화자 명단이 아니다).
- **블록이 갖는 범례** — "기자가 아는 사실 (이 밖은 묻지 못한다)", "그가 아는 사실 (이
  밖은 말하지 못한다)". 그 표기를 읽는 법이라 그 자리에 선다.
- **그 턴만의 오퍼레이터 지시** — 손잡이로 시간을 넘긴 턴의 "시간이 흘렀다: A → B —
  장면은 B에서 연다". 헤더에 적힌 날짜가 화면에 그대로 서므로 날짜 하나는 일러 준다.

### `MATCH_CASTER_SYSTEM` — 섹션의 순서

역할 → **입력** → **사건** → **한 턴** → **출력 문법**(+ 예시) → **말**. 사건이 한 턴보다
앞인 이유: 중계의 유일한 진실이 사건 목록이라, 무엇을 옮기는가가 어떻게 끝내는가보다
먼저다. 정지점이 감독의 차례라는 것은 「한 턴」의 한 줄이다 — 세 번 적지 않는다.

### `MATCH_INTENT_SYSTEM`

분류기다. 역할 한 줄 뒤에 산출의 갈래마다 한 절 — 무엇을 고르나 · advance · 대화 · 판을
바꾸는 것 · unresolved. 평시 도구 설명이 갖는 판정 기준(맥락 적합성·설득 근거·수용성)과
"부름은 면담이 아니다"를 여기도 갖는다 — 경기에는 도구 설명이 실리지 않는다.

### 결산 셋 · 압축

"당신은 …다" 한 줄, **무엇을 보는가**, **규칙**. 폭·인원·글자 상한은 코어 상수를 그대로
문장에 넣는다 — 손으로 적으면 코어만 조여지고 판정자는 옛 밴드를 믿는다. 산출 도구는
`toolChoice`가 강제하므로 "그 도구로만 답하라"는 적지 않는다(§8).

### 첫 장면

`ONBOARDING_INSTRUCTION`은 누가 여는지 한 줄이다 — "오늘은 감독의 부임 첫날이다. 상태와
인물 카드를 읽고 수석코치의 말로 첫 장면을 열어라." 소재·구성 체크리스트를 붙이지
않는다: 모델은 항목 수만큼 문단으로 갚아 모든 세이브의 첫 장면이 같은 골격이 된다.

## 5. 스킬 표면 — 한 판단은 한 도구

도구는 **37개**이고 **전부 평시 GM의 것**이다. 경기 중에는 도구 표면이 **0**이다 —
지시 해석이 JSON 하나를 내고 코어가 같은 스킬 함수를 부른다
([agents.md](./agents.md) §3). 같은 순간에 함께 정해지는 것들이
갈려 있으면 GM이 하나를 빠뜨린다 — 라인업은 1·2군 이동까지 한 요청(`set_lineup`의
`squadLevels`), 한 선수의 자리·역할·개인 지시는 `set_player_tactic` 하나, 오퍼는 방향을
인자로(`send_offer` kind=buy/sell/loan/loan_out), 오퍼 판정은 누가 답할 차례인지 협상이
알아서 처리한다(`respond_offer`). 거꾸로 **함께 정해지지 않는 것은 가르는 것이 맞다** —
층만 옮기는 1·2군 이동은 `set_squad_level`이 따로 받는다. `set_lineup`으로 보내면 선발
열한 자리를 다시 적게 해, 감독이 말하지 않은 배치까지 이 한마디에 딸려 바뀐다.

| 그룹      | 수  | 도구                                                                                                                                                                                             |
| --------- | --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 진행      | 2   | `start_match` · `accept_manager_offer`                                                                                                                                                           |
| 전술·훈련 | 9   | `set_lineup` · `set_squad_level` · `set_captain` · `set_tactics` · `set_player_tactic` · `exploit_point` · `set_match_plan` · `set_training` · `set_development_focus`                           |
| 대화·서사 | 5   | `team_talk` · `talk_to_player` · `respond_to_media` · `respond_to_approach` · `apply_narrative_event`                                                                                            |
| 경기      | 1   | `substitute`                                                                                                                                                                                     |
| 이적      | 11  | `deal_odds` · `list_negotiations` · `send_offer` · `respond_offer` · `accept_deal` · `open_renewal` · `open_release` · `set_transfer_list` · `withdraw_offer` · `release_player` · `recall_loan` |
| 재정      | 2   | `apply_finance_event` · `adjust_transfer_budget`                                                                                                                                                 |
| 조회      | 7   | `search_players` · `get_squad` · `get_team` · `get_league` · `get_career` · `get_finance` · `scout_player`                                                                                       |

### 계약은 넷으로 갈린다

| 유형   | 계약                                                                                            |
| ------ | ----------------------------------------------------------------------------------------------- |
| 진행형 | 시계를 움직인다 — **경기 시계는 지시 해석의 진행 의도로 코어가 밀고**, 날짜는 장면 헤더다       |
| 설정형 | 검증 후 그대로 기록 — 라인업·전술·훈련·이적 리스트                                              |
| 판정형 | LLM이 `{outcome, intensity}`·`stance`를 판정, **변화량은 코어 공식**                            |
| 거래형 | 상대 판정은 LLM, 확률 앵커·한도는 코어 ([../simulation/transfer.md](../simulation/transfer.md)) |

- **읽기 전용 여덟**(조회 6 + `deal_odds` + `list_negotiations`)은 호출 기록을 남기지
  않는다 — 조회 로그가 스킬 칩을 덮는다. `scout_player`는 상태를 바꾸므로 조회 그룹이지만
  읽기 전용이 아니다.
- **도구 설명도 프롬프트다.** `SKILL_CATALOG`이 원본이고 표시 이름(label)·그룹까지 함께
  갖는다. 화면의 칩 이름은 웹이 따로 가진 표를 쓰고 어긋남은 테스트가 지킨다. 문체 규칙
  (§1-5)도 같이 걸린다 — 볼드·이모지·강조어 없이, 언제 부르고 무엇을 돌려주는지만.
- **설명에는 길이 예산이 있다** — 한 도구의 상한과 카탈로그 총량을
  `packages/agents/test/skill-descriptions.test.ts`가 잰다. 고정층에 매 턴 실리는 문장이라
  규칙 하나를 지울 때마다 설명 두 줄이 붙는 것이 아무 데서도 드러나지 않았다.

### 입력 계약 — 스키마는 한 벌이다

도구가 무엇을 받는지는 **Zod 스키마 하나**가 정하고, 모델이 보는 JSON 스키마는 거기서
파생된다(`tool-schema.ts`의 `toToolSchema`). 손으로 두 벌을 맞추면 언젠가 갈리고, 갈리는
방향마다 값이 다르다 — JSON이 느슨하면 모델이 통과할 줄 알고 보낸 입력을 코어가 반려하고,
Zod에만 있는 상한은 모델이 모르는 채로 그 도구를 계속 실패시킨다.

**같은 종류의 인자는 같은 검증을 지난다.**

- 장부·피드에 영구히 남는 자유 문구(`note`·`label`)는 전부 상한을 갖는다. 상한이 없으면
  감독 발화가 통째로 실려 원장 라벨 한 줄이 단락이 된다.
- 대상 목록(`playerIds`·`targetIds`)은 빈 배열을 받지 않는다 — 아무에게도 닿지 않는
  호출이 하루 한도를 소모한다.
- 날짜 인자는 형식과 **오늘**을 함께 본다. 지난 날짜로 잡은 훈련은 영영 `scheduled`로
  남고, 그 날짜로 소집을 앞당기면 대가가 지난 날수만큼 부풀려 매겨진다.
- 선수 자리는 id가 아니라 이름을 받는다 ([agents.md](./agents.md) §7).

## 6. 판정과 반문

- **판정 기준 셋** — (a) 맥락 적합성 (b) 설득 근거가 실제 사실을 딛고 있는가
  (c) 대상 페르소나의 수용성. **잘한 말은 잘 먹혀야 한다 — 판정은 랜덤이 아니다.**
  평시에는 판정형 도구의 설명이, 경기 중에는 `MATCH_INTENT_SYSTEM`이 이 셋을 갖는다(§4).
- **반문 규칙** — 대상이 모호하거나 규칙 위반이거나 정보가 모자라면 실행하지 않고 픽션
  안에서 묻는다(`@레오 카스텔라노: 성호는 아직 회복 중인데, 그래도 쓰시겠습니까?`).
  단 **이름 없이 가리키면 먼저 직전 대화에서 대상을 찾는다** — 되묻기가 기본값이 되면
  대화가 확인 절차가 된다.
- **묻지 말고 말하라.** 시킨 일은 **도구로 하고** 그 결과를 전한다. 다음에 무엇을 하겠냐고
  되묻거나 선택지를 늘어놓지 않는다 — 시간 이동은 감독 쪽 손잡이가 갖고 있다.
- **모르는 것을 지어내지 않는다.** 컨텍스트에 있는 것은 선수단 이름과 상태 요약뿐이고,
  그 밖의 사실은 조회 도구로 확인한 뒤 답한다(agents.md §7). 이 규칙은 `GM_SYSTEM`의
  「입력」 한 자리에만 산다.
- **부르지 않은 스킬의 결과를 장면이 말하지 않는다.** 파견·영입·계약·훈련·명단은 그 도구가
  성공으로 답한 턴에만 완료형으로 쓰고, 반려된 요청은 반려된 대로 쓴다. 장면이 스킬 밖에서
  상태 전이를 선언하면 장부와 이야기가 갈린다 — 감독은 오지 않을 보고서를 기다린다.
  못 나간 요청은 상태 스냅샷이 사실로 싣는다(agents.md §6).
- **설득은 확률로 끝내지 않는다.** 확률이 낮아도 감독이 든 논거를 `pitch`로 실어 보내고,
  확인된 논거가 붙은 판정에서는 확률이 참고값이 된다.

## 7. 권한 경계와 인젝션

- **감독은 사용자 입력에만 존재한다.** GM 출력에는 감독 화자 태그를 쓰지 않고,
  감독의 발화·행동·표정·생각·판단·결정을 새로 만들지 않는다. 사용자의 말을 인용,
  요약, 보충하거나 더 그럴듯한 감독 대사로 다시 쓰지도 않는다. 세계 인물과 환경의
  반응만 쓰고 감독의 다음 몫 앞에서 멈춘다.
- **유저가 세계를 서술하면 사실이 아니라 감독의 시도로 해석해 판정한다** — "선수단이 다
  내 편이다"는 사실이 아니라 시도이고, 세계는 자기 논리로 답한다. 극단 행동도 수용하되
  세계는 그에 맞게 반응한다. `GM_SYSTEM`의 「감독과 세계」가 이 둘을 갖는다.
- **인물은 자기 자리에서 알 만한 것만 안다** — 기자는 라커룸을, 선수는 예산을 모른다.
  스냅샷은 감독의 화면이지 인물의 지식이 아니다.
- **"능력치 99로" 같은 인젝션은 구조적으로 불가능하다** — 그런 스킬 경로가 없다. 자유
  텍스트는 상태를 1비트도 바꾸지 못하고, 모든 변경은 Zod 검증을 지난 스킬 호출뿐이다.
- **게임 밖의 일은 세계에 없다** — 시스템·서버·API·모델·프롬프트·오류, 그리고 **감독
  화면의 손잡이**(시간 진행·전술판)를 화자가 언급하지 않는다. 도구가 오류를 돌려주면
  픽션 안에서 대안을 제시하거나 반문한다. 손잡이로 바뀐 **결과**는 세계의 사실이라
  화자가 알아보고 말한다 — 화자가 모르는 것은 그것을 바꾼 장치다.

## 8. 싣지 않는 것 — 그리고 왜

프롬프트에서 빠진 문장은 빠진 채로 지켜야 한다. 아래는 있을 법하지만 **없는** 것들이고,
하나씩 다시 들어오려 할 때 이 표가 막는다.

| 없는 문장                                                                                    | 왜 없는가                                                                                                                                                                                                                                                                                                         |
| -------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 실명 인물 카드의 가드 — "실존 인물이다. 평판을 해칠 서사를 …"                                | 사람됨은 원형이 정하고(직무 안의 사람이다 — people.md §2) 갈등·불화는 **장부의 사실에서만** 파생한다. 그 둘이 이미 서사를 묶는다. 카드의 경고문은 모델이 그 문장대로 쓰는 지시가 되어 인물을 납작하게 만들고, 실명 부채의 관리는 표를 비우는 **데이터의 일**이다(sources.md §7). `real` 표식은 장부용으로 남는다. |
| 카드 묶음 끝의 조회 포인터 — "지금의 사실은 이 카드에 없다. 조회로 확인하라"                 | 같은 규칙이 `GM_SYSTEM`의 「입력」에 있다. 카드는 이력에 굳으므로 포인터도 턴마다 쌓여, 한 규칙이 이력에 열 번 선다.                                                                                                                                                                                              |
| 레퍼런스의 "당신은 이 화자를 대신 연기하지 않는다"                                           | 「감독과 세계」의 첫 줄이 원본이다. 레퍼런스는 감독의 이름과 태그만 안다.                                                                                                                                                                                                                                         |
| 레퍼런스·스냅샷의 도구 이름 — "능력치는 get_squad가 준다", "accept_manager_offer로 수락한다" | 도구가 무엇을 돌려주고 언제 부르는지는 그 도구의 설명이 원본이다(§4). 데이터 블록에 적으면 같은 사용법이 두 자리에 산다.                                                                                                                                                                                          |
| "무직이라 팀의 도구는 부를 수 없다"                                                          | 코어가 부르는 자리에서 막고 픽션 안의 문장으로 답한다(`gm-tools.ts`). 스냅샷은 "맡은 팀이 없다"까지만 말한다.                                                                                                                                                                                                     |
| "시간 이동 중 도착한 오퍼는 보고만 하라"                                                     | 코어가 그 턴의 판정을 막는다(`deferNegotiationIds`). 막힌 도구는 반려로 답하고 장면은 반려된 대로 쓴다.                                                                                                                                                                                                           |
| 시간 도구를 막는 문장 — "advance_time을 부르지 마라"                                         | 없는 도구다. 시계는 장면 헤더가 밀고 손잡이는 코어가 민다. 금지를 읽은 모델은 그 대신 감독에게 "시간을 넘길까요?"라고 허락을 구한다.                                                                                                                                                                              |
| 결산·해석의 "그 도구로만 답하라. 그 밖의 텍스트는 쓰지 않는다"                               | 산출 도구는 `toolChoice`가 강제한다(models.md §3-2). 도구가 안 불린 응답은 `requireToolCall`이 실패로 센다.                                                                                                                                                                                                       |
| 중계의 "시각 줄을 쓰지 마라"                                                                 | 모델이 적은 시각 줄은 코어가 걷어내고 장부의 분을 세운다. "모든 줄은 @로 시작한다 — 시각 줄은 코어가 붙인다"로 적는다(§1-3).                                                                                                                                                                                      |
| 실명 인물을 위한 별도 프롬프트·섹션                                                          | 실명이든 가명이든 카드의 모양은 하나다. 이름으로 갈리는 규칙을 두면 모델이 이름을 보고 사람을 다르게 그린다.                                                                                                                                                                                                      |
| `**볼드**` · 이모지 · "반드시" · "절대" · "(다른 무엇보다 먼저)"                             | 문체가 샌다(§1-5). 중요한 규칙은 앞자리와 한 줄의 명료함으로 세운다.                                                                                                                                                                                                                                              |

## 9. ⚠️ 불변식

- **프롬프트의 원본은 코드 상수 하나다.** 런타임 오버라이드를 두면 지금 도는 문장이
  저장소의 문장과 갈리고, 무엇을 고쳐서 무엇이 달라졌는지 diff로 되짚을 수 없다.
- **한 규칙은 한 자리에만 산다** (§4). 원본이 둘이면 같은 도구를 한쪽은 "확률대로
  판정하라"로, 다른 쪽은 "확률은 참고일 뿐"으로 말하게 되고 둘 다 같은 턴에 실린다.
- **데이터 블록에는 데이터만 싣는다** (§1-6). 카드·레퍼런스·스냅샷의 지시문은 이력에
  굳어 턴마다 쌓이고, 실명 인물에게만 붙는 문장은 이름을 보고 사람을 다르게 그리게 한다.
- **없는 도구를 막는 문장을 싣지 않는다** (§8). 금지를 읽은 모델은 그 대신 감독에게
  허락을 구해, 화면 손잡이가 있는 자리에 대화로 조르는 길이 열린다.
- **코어가 이미 막는 것을 프롬프트가 또 지시하지 않는다** (§8). 지나치면 안 되는 것
  앞에서는 코어가 멈춰 세우므로, 같은 일을 프롬프트가 또 시키면 페이싱만 굳는다.
- **출력 문법의 예시는 형식만 담은 하나다** (§1-4). 내용이 실리거나 둘 이상이면 모든
  세이브의 장면이 그 골격을 복사한다.
- **프롬프트에 출력 문법 밖의 표기를 쓰지 않는다** (§1-5). 볼드를 금지하면서 볼드로
  적은 프롬프트는 볼드를 가르친다.
- **입력 스키마는 한 벌이다** (§5). 모델이 보는 JSON 스키마는 Zod에서 파생한다 —
  두 벌을 손으로 맞추면 느슨한 쪽은 코어가 반려하고 엄한 쪽은 모델이 모르는 채 계속
  실패한다.
- **자유 텍스트는 상태를 1비트도 바꾸지 못한다** (§7). 모든 변경은 Zod 검증을 지난
  스킬 호출뿐이므로 "능력치 99로" 같은 인젝션에는 닿을 경로가 없다.
- **감독은 사용자 입력에만 존재한다** (§7). GM 출력이 감독의 발화·판단·결정을 새로
  만들면 감독은 자기가 하지 않은 선택의 결과를 읽는다.
- **게임 밖의 일은 세계에 없다** (§7). 시스템·모델·프롬프트·오류, 그리고 감독 화면의
  손잡이를 화자가 언급하지 않는다 — 손잡이로 **바뀐 결과**만 세계의 사실이다.
- **부르지 않은 스킬의 결과를 장면이 말하지 않는다** (§6). 장면이 스킬 밖에서 상태
  전이를 선언하면 장부와 이야기가 갈리고, 감독은 오지 않을 보고서를 기다린다.
- **`@` 줄이 하나도 없는 응답은 손대지 않는다** (§3). 규약을 어긴 응답까지 지우면 빈
  턴이 되어 무슨 일이 있었는지조차 사라진다.
- **프롬프트 변경은 별도 커밋으로 분리한다** (§1).

## 10. 미해결

- 프롬프트 회귀 측정 — 문구를 고쳤을 때 무엇이 좋아졌는지 재는 하네스가 없다.
  `packages/agents/test`가 지키는 것은 출력 문법·카탈로그 일치·길이 예산·도구 이름
  부재까지다. 예시 장면(§1-4)이 문법 위반을 얼마나 줄이는지도 잰 적이 없다 —
  `sanitizeSceneText`가 걷어내는 줄의 수가 그 눈금이 될 수 있다.

## 코드 위치

| 무엇                                              | 어디                                                                         |
| ------------------------------------------------- | ---------------------------------------------------------------------------- |
| 평시 시스템 프롬프트 (`GM_SYSTEM`)                | `packages/agents/src/gm-prompt.ts`                                           |
| 지시 해석 프롬프트·스키마 (`MATCH_INTENT_SYSTEM`) | `packages/agents/src/match-intent.ts` · `match-intent-schema.ts`             |
| 중계 시스템 프롬프트 (`MATCH_CASTER_SYSTEM`)      | `packages/agents/src/match-caster.ts`                                        |
| 결산 프롬프트 셋                                  | `packages/agents/src/training-rater.ts` · `match-rater.ts` · `mood-rater.ts` |
| 이력 요약 프롬프트 (`HISTORY_COMPACTOR_SYSTEM`)   | `packages/agents/src/history-compactor.ts`                                   |
| 첫 장면 지시 (`ONBOARDING_INSTRUCTION`)           | `packages/agents/src/gm.ts`                                                  |
| 도구 설명·그룹·표시 이름 (`SKILL_CATALOG`)        | `packages/agents/src/skill-descriptions.ts`                                  |
| 도구 바인딩·스키마                                | `packages/agents/src/gm-tools.ts`                                            |
| 인물 카드 · 레퍼런스 · 스냅샷 · 장면 위생 · 헤더  | `packages/agents/src/gm-input.ts`                                            |
| 스냅샷 안의 범례 블록 (회견 · 찾아온 사람)        | `packages/engine/src/club/press.ts` · `approach.ts`                          |
| 스킬 실행부                                       | `packages/engine/src/skills/index.ts`                                        |
