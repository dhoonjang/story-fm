# 경기 시뮬레이션

**코어가 기대 득점(xg)으로 사건을 굴려 확정하고, LLM은 그것을 중계·연출·대화로
옮긴다.** 감독의 지시는 전력 패킷을 통해서만 결과에 닿으며, 이득은 소화율만큼만
반영되고 대가는 온전히 치른다.

요구사항:

1. **영향성** — 능력치·전술·상태·감독 능력이 결과에 통계적으로 반영된다.
2. **일관성** — 경기 장부(스코어·시계·인원·스탯)에 모순이 없다.
3. **설명 가능성** — 주요 사건은 패킷 항목(원인 태그)으로 설명된다.
4. **양방향** — 유저의 결정이 좋은 쪽으로도 나쁜 쪽으로도 결과를 움직인다.
5. **결정성** — (시드, 경기, 구간)이 같으면 같은 사건. 개입은 패킷을 바꿔
   **그다음 구간부터** 반영된다.

## 1. 전력 분석 패킷 (코어, 결정적)

킥오프·교체·전술 변경·구간 진행마다 재계산되는 경기의 판세. 힌트가 아니라
**결과를 만드는 수치**다.

```ts
StrengthPacket {
  lineup / bench,        // 개인 단위 전력 + 깎인 이유 분해 (§1.1)
  zones,                 // { attack, midfield, defense } × 양팀
  matchups,              // 존별 우열 + 근거 한 줄
  tactical,              // 지시 적용률(uptake) + 이득·대가 노트
  keyPoints,             // 전술 미스매치 (sim/key-points.ts, 13축)
  guide: {
    expectedGoals,       // 구간 시뮬레이터의 분당 발생률 원본 (÷90)
    upsetChance, intensity,  // intensity 0.8~1.3 — 피로·파울·부상률을 함께 움직인다
  },
}
```

### 1.1 전력은 개인에서 시작한다

```
effective(선수) = roleFit(15축 × 자리 가중치)
                × stateModifier(폼·사기·피로 + 경기 중 피로)
                × profFactor(포지션 적응도)           0.88~1.00
                × famFactor(전술 적응도 × 자리 민감도) 0.79~1.00

zones = XI 가중 평균(ZONE_CONTRIBUTION) × (1 + 전술 델타·상성, ±12%) × tacticalFit(감독)
```

- 포지션 적응도의 감점 폭(30%p)이 전술 적응도(15%p)보다 크다 — 전술은 몇 주면
  익히지만 자리는 커리어가 만든다.
- 전술 적응의 무게는 자리마다 다르다(`TACTICAL_SENSITIVITY`): 중원 1.4 · 수비 1.2 ·
  AM 1.0 · 윙/CF 0.7 · ST 0.6 · GK 0.8.
- 지시 적용률(`uptake` 0.45~1.0)만은 팀 평균을 쓴다 — 전술 지시는 열한 명이 함께
  소화한다. `0.45 + 0.35×(감독 전술/99) + 0.20×(팀 전술 적응도)`.

### 1.2 전술 6축 — 이득과 대가를 함께 낸다

| 축 | 이득 | 대가 |
| --- | --- | --- |
| 멘탈리티 ↑ | 공격 | 수비 |
| 압박 ↑ | 중원 (체력이 받쳐줄 때 더) | 뒷공간 + 강도↑(피로·카드·부상) |
| 수비 라인 ↑ | 중원(압축) | 수비 — 상대 최전방이 빠를수록 비싸다 |
| 템포 ↑ | 공격 | 실책 (침착성 낮으면 더) + 강도↑ |
| 폭 ↑ | 공격 (측면 자원이 좋을 때 더) | 중원 밀집 약화 |
| 롱볼 ↑ / 짧게 ↑ | 공격(제공권 필요) / 중원(연결 필요) | 중원 점유 / — |

**적용률은 이득에만 곱하고 대가는 온전히 남는다** — 소화 못 하는 팀에 과격한
지시를 내리면 순손실이다.

### 1.3 전술 상성 (`sim/tactical-counters.ts`, 14종)

두 전술이 서로를 만나는 지점. 설계 규칙 셋:

1. **조건이 맞을 때만 발동** — 코어가 사실만 대조한다(판정 LLM 없음). 대가는 이미
   치렀으므로 근거 없는 지시는 손해가 된다.
2. **발동하면 문장으로 드러난다** — `why`가 키포인트로 올라가 중계가 인용한다.
3. **효과는 작다(3~8%)** — 세게 잡으면 "정답 전술" 게임이 된다.

뒷공간 · 압박 함정 · 압박 우회 · 빌드업 붕괴 · 중원 과부하 · 측면 자유 · 크로스
폭격 · 무딘 점유 · 블록 흔들기 · 역습 · 늘어진 전술(멘탈리티>라인) · 속도의 대가 ·
백라인 숫자 · 측면 일대일. 같은 스쿼드로 전술만 바꿔 xg가 40%까지 갈린다
(전력 열세여도 역습이 맞으면 앞선다).

### 1.4 기대 득점 — 질과 판을 곱한다

```
xg = 1.35 × (우리 XI 질 / 상대 XI 질)^5.5      ← 선수가 정하는 기본
        × (우리 공격 존 / 상대 수비 존)^1.3      ← 전술·상성·지시가 기울이는 판
        × 홈계수(홈 1.08 · 원정 0.95 · 중립 없음)      범위 [0.6, 3.0]
```

축이 둘인 이유가 있다. 존에는 전술 보정이 이미 곱해져 있어서 **존 하나로 재면
공격적으로 세팅한 약팀이 강팀을 앞선다** — 실측에서 코번트리(2부)의 공격 존이
88.9로 아스날의 86.3보다 높았고, 같은 경기를 간이 시뮬은 1.90:0.96으로 보는데
구간 시뮬은 1.48:1.48로 봤다. 질의 지수가 크고 판의 지수가 작아서 **전술은 판을
흔들되 선수를 대신하지 못한다.**

- **질**(`QUALITY_EXPONENT` 5.5)은 XI 유효 전력의 평균 비 — 간이 시뮬이 쓰는 축과
  같다. 두 시뮬이 같은 것을 재야 감독 팀만 다른 규칙으로 놀지 않는다.
- **판**(`MATCHUP_EXPONENT` 1.3)은 존 매치업 — 전술 6축·상성·개인 지시·공략·구멍이
  실린 값. 존의 폭이 묶여 있어 지수를 크게 둬도 뒤집히지 않는다: 양쪽이 반대로
  극단을 잡으면 최대 1.6배까지 갈리고, 그건 10점 차 스쿼드의 질 우위(1.9배)보다
  작다.
- ⚠️ **존은 XI 전체의 가중 평균**이다(`ZONE_CONTRIBUTION`). 그룹 평균으로 재면
  원톱을 세운 팀의 공격 존이 잘하는 공격수 한 명의 값이 되어 포메이션이 전력을
  왜곡한다.
- ⚠️ **전술이 만드는 폭은 ±18%**(`TACTIC_SWING`) — 전술 6축·상성·개인 지시·공략을
  다 합친 값이다. 자르지 않고 **부드럽게 포화**시킨다(tanh): 자르면 상한에 닿은 두
  지시가 같은 값이 되어 "더 공격적으로"가 아무 일도 안 한다.

### 1.5 점유 — 중원 우위가 닿는 길

중원 존은 오래 **읽히기만 하는 값**이었다: 매치업 문장에는 서는데 기대 득점은
공격/수비 존만 봐서, 중원을 지배해도 결과에 아무 일이 없었다. 지금은 중원이
**점유**로 번역되고, 점유가 두 곳에 실린다.

```
점유(우리) = 우리 중원 / (우리 중원 + 상대 중원)        범위 [0.35, 0.65]
```

| 어디에 | 어떻게 |
| --- | --- |
| 기대 득점 | `× (점유/0.5)^0.5` — 공을 쥔 팀이 더 자주 상대 문 앞에 선다 |
| 체력 | 공 **없는** 팀이 더 뛴다 — `chaseFactor`가 소모에 곱해진다(양 끝 ±12%) |
| 패스 배분 | 구간 길이 × 점유 × 템포로 팀 총량을 정한다 (§4) |

- 점유 지수를 작게(0.5) 둔 이유는 미드필더의 질이 이미 두 축에 들어 있어서다 —
  XI 평균(질)과 존 가중 평균(판). 여기서 더하는 것은 **점유라는 별개의 사실**이
  만드는 몫이다.
- **체력 경로가 이 설계의 핵심이다.** 중원에서 밀린 팀은 90분 내내 공을 쫓느라
  더 지치고, 그 피로가 구멍 문턱(78)에 먼저 닿아 후반에 라인이 열린다 —
  중원 우위가 즉시 골이 되는 게 아니라 **경기 후반에 값을 한다.**
- ⚠️ 점유는 **패킷이 갖는다**(`guide.possession`). 기대 득점·체력·패스가 같은
  값을 봐야 갈리지 않는다. 간이 시뮬도 같은 함수(`possessionShare`)를 쓴다.

### 1.6 공략 (`sim/exploits.ts`)

키포인트를 표적으로 만든다 — 감독이 "요케레스 뒤로 계속 보내"라고 하면 중계 모델이
후보 목록에서 id를 골라 `exploit_point`를 부르고, 코어가 실재를 대조한 뒤 존 조정을
얹는다. 없는 지점은 반려. 이득에만 소화율, 대가는 온전히, **동시에 두 곳까지**.
AI 팀도 자기 분석 등급만큼 공략한다.

## 2. 진행 루프 (`advance_match`)

```
코어: simulateSegment(패킷, 장부) — 다음 정지점까지 사건 확정
  · 발생률(분당) = 기대 득점/90 · 슛 = 골×7 · 카드·부상 = 강도 비례
  · 사건 시각은 지수분포, 종류·주체는 가중 추첨
→ 장부 검증(§5) → 기록 → 경기 중 피로 누적 → 패킷 재계산
→ 매치 티어 LLM: 확정된 사건 목록을 중계·연출·대화로 옮긴다
→ 정지점 — 감독 개입 가능
```

- **정지점**: 골 · 퇴장 · 부상 · 하프타임 · **연장 개시** · **연장 하프타임** ·
  종료 · (조용하면) 25분 상한.
- **국면은 넷이다** — 전반 45 · 후반 90 · **연장 전반 105 · 연장 후반 120**
  (`PHASE_END`, 추가시간은 연장 하프에선 1~2분). 90분이 끝날 때 승부가 남았으면
  `full_time` 자리에 `extra_time_start`가 들어가 장부가 계속 열려 있고, 감독은 그
  정지점에서 **다른 정지점과 똑같이** 교체·전술을 지시한다. 연장으로 가는지는
  구간 시뮬이 아니라 코어의 `needsExtraTime`이 정해 `toExtraTime`으로 넘긴다 —
  구간 시뮬은 대회도 대진도 모른다.
- **연장의 발생률은 0.84배/분** — 30분에 90분의 `EXTRA_TIME_GOAL_SHARE`(0.28)만큼만
  낸다(간이 시뮬 `EXTRA_TIME_RATE`와 같은 눈금). 카드·부상은 그대로다: 지친
  다리는 덜 뛰지만 덜 거칠지는 않다. 피로는 30분치가 그대로 더 쌓인다.
- 개입(전술·교체)은 패킷 재계산으로 그 구간부터 반영. 재시뮬은 없다.
- **LLM에게 결과를 바꿀 도구가 없다** — 중계에만 있는 골도, 기록 누락으로 멈추는
  시계도 구조적으로 불가능하다.
- AI 팀도 교체한다(`planAiSubstitution`) — 58분 이후 지친 선수를 같은 계열 최고
  벤치 자원으로.
- mock과 실모드가 같은 시뮬레이터를 쓴다 — 차이는 서술 주체뿐.

경기 중 대화 규칙: 킥오프 전·하프타임(라커룸)은 선수 전체와, 진행 중 정지점은
수석코치·벤치 선수와만. 그라운드 위 선수에겐 외침(짧은 개인 지시)뿐. 대화 자체는
상태 무영향 — 판정형 스킬로 이어질 때만 상태가 변한다.

## 3. 체력 — 자리와 전술이 함께 정한다 (`sim/stamina.ts`)

실제 포지션별 주행거리를 기준선으로: 중앙 미드 1.25 · 풀백/윙백 1.20 · 윙어/DM
1.15 · AM 1.10 · ST 1.00 · CB 0.85 · GK 0.35. 여기에 전술 배율(0.72~1.55 —
압박이 가장 비싸다)이 얹히고, 자리마다 같은 지시의 무게가 다르다(폭→측면,
압박→앞선·중원). 지구력이 ±25%를 더 가른다.

90분 기준(기본 전술): CM −65 · 풀백 −62 · CB −44 · GK −18. 게겐프레스면 CM −97.

- **그날의 몫 ±12%** (`drainVariance`)가 곱해진다. `시드:경기:선수` 해시라
  결정적이고 **경기 내내 고정**이다(구간마다 굴리면 평균으로 상쇄된다).
  ⚠️ FNV-1a만으론 하위 비트 확산이 약해 편차가 숨은 능력치처럼 굳는다 — murmur3
  마무리 믹스 필수(테스트로 고정).
- ⚠️ `FULL_MATCH_DRAIN` 60 아래로 낮추면 로테이션이라는 판단 자체가 사라진다.
- **정산도 이 값 그대로** (`pendingMatch.matchFatigue`) — 화면에서 본 소모와
  장부에 남는 소모는 같은 숫자다. 상대 팀도 같은 장부에서 정산된다.

### 3.1 회복

소모와 같은 축(`stamina`)이 회복도 정한다(`dailyRecovery`): 본훈련 8 · 훈련 없는
날/휴식/경기일 13 · 회복 세션 16. 기본 마이크로사이클이 엿새에 +74(주 1경기는
로테이션을 강요하지 않는다), 사흘 간격은 +42(강요한다).

- ⚠️ 회복이 이보다 낮으면 주 1경기만으로 시즌 내내 하강해 12월에 스쿼드가 눕는다.
- ⚠️ 회복의 지구력 편차(±10%)는 소모(±25%)의 절반 아래여야 한다 — 한 축이 양쪽에
  곱으로 걸리면 격차가 복리로 벌어진다.
- 하루의 회복은 하루에 한 번 — 그날의 성격 하나를 정해 한 번만 얹는다.

### 3.2 구멍 — 교체를 미루면 그 라인이 열린다

피로 78을 넘으면 그 라인 전체가 −7%(최대 −25%)를 치르고, 키포인트에 문장으로
뜬다("교체하지 않으면 그 자리가 계속 열린다"). 부상 확률도 함께 오른다.
⚠️ 구멍 문장에 소진 수치는 적지 않는다 — 상대 체력은 안개를 지나 막대로만 간다.

## 4. 사건과 기록

```ts
MatchEvent { minute, type, team, actors[], causes: CauseTag[], detail }
```

- `causes`는 코어가 패킷 항목에서 뽑는다(매치업 → 키포인트 → 전술 노트). 이 태그가
  감독의 전술 XP 근거가 되므로 검증 없는 자유 문자열을 두지 않는다.
- `actors` 순서가 뜻을 갖는다 — goal은 [득점자, (도움)], substitution은 [아웃, 인].
- 도움은 골을 무효로 만들지 못한다 — 못 믿을 도움 슬롯은 조용히 떨구고 골은
  살린다(`sanitizeEvent`).

**사건이 될 만한 것만 사건이다.** 골·슛·선방·카드·교체·부상은 `MatchEvent`,
패스·전진 패스·xG 합은 `MatchLedgerState.stats`에 숫자로 쌓인다. 흐름의 양은
굴리지 않고 나눈다: 구간 길이 × 점유 × 템포로 팀 총량 → 자리·`passing`·`vision`
으로 배분. 전진 패스 비율은 전술 × 선수 성향(`daring` = 시야 0.5 · 킥 0.3 ·
침착성 0.2)이라 같은 팀 안에서도 23~38%로 갈린다.
⚠️ 템포 계수의 기준(3)은 1.0 — 아무 지시도 안 한 팀이 손해 보면 안 된다.

슛에는 xG가 붙는다(`MatchEvent.xg` — 슛 평균 0.1, 골 0.3 안팎). 팀 xg 예측과
견주면 "기회를 만들었나 vs 넣었나"가 갈린다.
⚠️ 한 값은 한 곳에서만 — 뷰의 슛·선방·패스는 `stats`가 원본, 골·도움·카드만
사건에서 센다.

**도움은 결과까지 살아남는다** — `MatchResult.assists`는 `scorers`와 같은
순서·길이·형식(`"home:playerId"`), 단독 득점은 빈 칸. 유저 경기와 타 팀 퀵심이
같은 규약을 쓴다.

## 5. 검증 — 경기 장부의 무결성

| 규칙 | 내용 |
| --- | --- |
| 시간 순증 | 이벤트 분은 단조 증가, 0~90(+추가시간) — 연장이면 120까지 |
| 국면 | 전반→후반→(연장 전반→연장 후반)→종료. 종료는 90′ 또는 120′ 이후에만 |
| 인원 | 퇴장·교체아웃 재등장 금지 · 교체 5인/3회(**연장이면 6인/4회**) · 미등록 선수 금지 |
| 스코어 | goal 이벤트 수 = 스코어 |
| 빈도 상한 | 배치당 20개, 팀당 6골 — 시뮬레이터가 상한을 먼저 알고 자른다 |
| 파생 스탯 | 요약 스탯은 이벤트에서 코어가 재계산 |

사건을 만드는 쪽이 코어이므로 이 검증은 LLM 방어가 아니라 **시뮬레이터의 계약
검사**다 — 반려는 시뮬레이터의 버그를 뜻한다.

## 6. 경기 후 반영 (코어, 결정적)

스탯 적재 → 평점 → 폼 Δ → 사기 Δ → 피로 정산 → 부상 확정 → 재정 → 리그 테이블.
사건은 창발, 반영은 공식.

### 평점 (`engine/match/ratings.ts`)

기준선 6.0에서 장부 사실만으로 조정(난수 없음): 승 +0.4/패 −0.3 · 골 GK +2.0 ·
DF +1.4 · MF +1.1 · FW +0.9 · 도움 +0.6 · 무실점 GK +0.8/DF +0.5 · 실점(첫 골
면제) GK −0.3/DF −0.2 · 경고 −0.3 · 퇴장 −1.5. 범위 3.0~10.0.
시즌 평점은 저장하지 않고 `ratingSum ÷ apps`로 파생(`seasonRating`).

**경기 후 LLM 채점** — 코어가 앵커를 박고(`buildRatingBrief` — finalizeMatch와
같은 함수), LLM이 사건 목록·출전 시간을 읽어 앵커 ±`RATING_BAND`(1.2) 안에서
재채점 + 근거 한 줄. 실패하면 앵커가 남는다(경기 결과를 막지 않는다). mock 모드는
건너뛴다.

### 밸런스 손잡이 한눈에

| 상수 | 값 | 무엇을 정하나 |
| --- | --- | --- |
| `QUALITY_EXPONENT` | 5.5 | 선수의 질이 득점으로 번역되는 세기 (구간 시뮬) |
| `MATCHUP_EXPONENT` | 1.3 | 전술·상성·지시가 그 위를 기울이는 세기 |
| `TACTIC_SWING` | 0.18 | 한 존이 전술로 움직일 수 있는 폭 (tanh 포화) |
| `BASE_EXPECTED_GOALS` | 1.35 | 대등한 두 팀의 기대 득점 |
| `MIN/MAX_EXPECTED_GOALS` | 0.6 / 3.0 | 하한은 **이변의 여지**, 상한은 폭주 방지 |
| `POSSESSION_EXPONENT` | 0.5 | 점유가 기대 득점에 실리는 세기 |
| `CHASE_DRAIN` | 0.8 | 공 없는 팀이 더 뛰는 폭 (양 끝 ±12%) |
| `QUICK_SIM_EXPONENT` | 4.5 | 같은 축의 간이 시뮬 판 — 리그 순위 분포의 손잡이 |
| `HOME/AWAY_BASE_GOALS` | 1.33 / 1.19 | 간이 시뮬의 기준 기대 득점 |
| `EXTRA_TIME_GOAL_SHARE` | 0.28 | 연장 30분의 기대 득점 (90분 대비) — 두 시뮬 공통 |

## 7. 타 팀 간이 시뮬 (`engine/match/quick-sim.ts`)

시즌 2,100여 경기 중 유저 경기만 구간 시뮬을 지나고 나머지는 전력 스칼라 + 홈
어드밴티지 → 확률 샘플로 한 번에 처리한다. **장부는 얇지 않다**:

- 결과·득점자·도움 + **골의 분·카드·퇴장·교체**까지 남긴다.
- 카드 상수(`CARDS_PER_MATCH`·`STRAIGHT_RED_CHANCE`·`BOOKED_AGAIN_WEIGHT`)는 구간
  시뮬과 공유, BOOKING·SUSPENSION은 같은 문(`engine/match/discipline.ts`)을
  지난다 — 리그가 우리 팀만의 규칙으로 돌면 안 된다.
- 퇴장은 남은 시간만큼 결과에 닿는다(자기 −30% · 상대 +35%). 교체 최대 4명,
  피로는 뛴 시간만큼.
- 도움은 골의 70%에만 붙는다(`ASSIST_RATE`).
- **회복은 리그 전체가 받는다**(`recoverOtherClubs`) — AI는 체력 70 이하면 더
  신선한 자원으로, 35 이하면 기량과 무관하게 뺀다.
- **같은 날 경기는 킥오프 순서를 지킨다** — tick은 우리 킥오프 이전 경기까지만
  굴리고 나머지는 `finalizeMatch`가 잇는다. 감독이 미래 결과를 미리 알면 안 된다.

⚠️ 두 모델의 전력차 민감도가 다르면 유저 팀만 유리해진다 — quick-sim은 스칼라
3제곱, 구간 시뮬은 존 비율 1.3제곱. 어느 쪽을 만지든 리그 평균 득점(아래)을
다시 재야 한다.

### 밸런스 기준선 (시즌 완주 실측)

| | 목표 (실제 EPL) | 측정 (시드 42·7·99) |
| --- | --- | --- |
| 리그 평균 득점/경기 | 2.8~2.9 | 2.97 · 3.06 · 2.89 |
| 우승팀 승점 | 85~93 | 84 · 83 · 80 |
| 감독 팀 최종 순위 | — | 6위(59점) · 1위(83점) · 5위(68점) |
| 경기당 경고 / 퇴장 | ~3.5 / ~0.2 | 3.30 / 0.233 |
| 팀당 교체 | 4~5 | 3.7 |

**하네스**: `BALANCE=1 pnpm vitest run packages/engine/test/balance-harness.test.ts
--reporter=verbose` (시드당 몇 분 — CI에서는 건너뛴다). 감독 팀은 "지시하지 않는
감독"이라 훈련·이적·전술 없이 로테이션만 한다 — 그 조건에서 시드에 따라 1위와
8위를 오간다.

**해소된 편향**: 폼·전술 적응도가 감독 팀에만 쌓이던 구조를 없앴다 —

① **간이 시뮬이 상태를 본다** — `overall` 평균 대신 `overall × stateModifier`를
쓴다. 그전에는 남의 팀이 지치든 폼이 바닥이든 결과가 같아서, 리그의 95%가 상태를
모르는 채 굴러갔고 AI의 로테이션도 값을 하지 못했다.
② **폼이 리그 전체에서 오르내린다** — 간이 시뮬의 경기 평점이 그대로 폼에 실린다.
③ **AI 팀도 전술을 익힌다** (`FAMILIARITY_DRIFT_CAP` 80) — 그전에는 기준선 60에
영원히 멈춰 있었다. 감독 팀은 결산 판정으로 95·100까지 가므로 자리는 남는다.
④ **연패·연승이 라커룸에 남는다** (`squad/slump.ts`) — 3연패부터 팀 폼이 깎이고
4연패면 한 명이 등을 돌린다. 3연승은 그 거울(이득이 손해보다 작다).
⚠️ 움직이는 축은 **폼이지 체력이 아니다** — 체력에 걸면 연패한 팀이 회복까지 못 해
악순환에 빠지고 "일주일이면 온전히 돌아온다"는 회복 불변식도 깨진다.

그래서 밸런스를 **xg 지수로 누르지 않는다.**

⚠️ 남은 편향: 감독 팀의 능력치·전술 적응도는 결산 판정(LLM)에만 걸려 있어 mock
모드에서는 자라지 않는다. 그래서 하네스가 `drillUserTactics`로 "훈련하는 감독"을
대신 모델링한다 — 그것 없이 재면 감독 팀만 기준선에 멎은 세계를 재게 된다.

## 8. 경기 화면

경기 중 탭 셋(`apps/web/components/match-view.tsx` · 데이터 `views/views.ts`):
**판세**(존 막대 + 키포인트 — "어디가 밀리나 + 왜"를 한 화면에) · **팀**(전술판 +
명단, 우리/상대) · **대회**(순위표 + 다음 경기까지 며칠). 전술 6축은 전술판에만
(읽는 김에 고칠 수 있는 곳). 상대 쪽은 조작 없음 + 수치 흐림(체력은 구간,
전력은 ±오차 — 명단 화면과 같은 채널 `observationOf`). 전술 6축과 배치 골격은
흐리지 않는다(90분 동안 보이는 사실). 킥오프 전·종료 후엔 탭이 없다.

## 9. 미해결

- **AI 팀의 상태·성장** — §7의 남은 편향. 폼·적응도가 유저 팀에만 쌓인다.
- **상대 감독의 경기 중 대응** — AI 전술이 초기값 그대로다. 0-2로 져도 90분 내내
  같은 멘탈리티라 후반 추격전이라는 리듬이 없다.
- **승부차기 연출이 없다** — 연장까지 비긴 대진은 `competition/shootout.ts`가
  숫자만 굴려 장부에 적는다. 감독이 키커를 정하지도, 중계가 한 발씩 옮기지도
  않는다 — 승부차기가 이 게임에서 가장 이야기가 큰 순간인데 결과만 남는다.
  (연장 30분은 감독이 지휘한다 — §2. 감독의 경기는 구간 시뮬이 120분까지 가고,
  타 팀 경기만 `competition/extra-time.ts`가 한 번에 굴린다. **연장이 필요한
  경기인가는 `needsExtraTime` 하나가 정한다** — 대회마다 따로 판단하면 어느
  하나만 고쳐도 규칙이 조용히 갈리고, `MatchResult.aet`가 이중 적용의 문지기다.)
- **분포 하네스가 CI에 없다** — 시드당 몇 분이라 `BALANCE=1`로 손으로 돌린다.

## 코드 위치

| 무엇 | 어디 |
| --- | --- |
| 전력 패킷·키포인트·공략 | `packages/sim/src/strength-packet.ts` · `key-points.ts` · `exploits.ts` |
| 구간 시뮬레이터·장부 | `packages/sim/src/match-engine.ts` · `match-ledger.ts` |
| 전술 상성·체력 | `packages/sim/src/tactical-counters.ts` · `stamina.ts` |
| 경기 흐름·정지점·마감 | `packages/engine/src/match/match-flow.ts` |
| 연장 판정·타 팀 연장·승부차기 | `packages/engine/src/competition/extra-time.ts` · `shootout.ts` |
| 간이 시뮬·평점·징계 | `packages/engine/src/match/quick-sim.ts` · `ratings.ts` · `discipline.ts` |
| 경기 화면 | `apps/web/components/match-view.tsx` |
