# 구현 노트 (Implementation Notes)

> 📓 **구현 중 판단·기획 편차 기록** · 2026-07-22 시작
> 기획서와 구현이 다르게 간 지점, 기획에 비어 있어 구현에서 정한 것을
> 기록한다. 기획 반영이 필요해지면 해당 design 문서를 갱신하고 여기서 지운다.

---

## 기획 편차 (의도적 — 후속 마일스톤에서 해소)

| # | 항목 | 기획 | 구현 (현재) | 후속 |
| --- | --- | --- | --- | --- |
| 1 | 선수 시드 데이터 | EA FC 데이터셋 적재 (data-sourcing §4) | ✅ **해소·확장 (2026-07-24)** — 실선수 **614명, 팀당 30~34명**(유망주 포함) 적재 (`engine/data/epl-players.ts`). **2025-26 시즌 + 2026 여름 이적** 기준, FC 6축 능력치. 멀티에이전트 워크플로우(팀별 생성 20 → WebSearch 교차검증·적대적 검증 20 → 팀 간 중복 해소 3건)로 생성. GK3+·DF9+·MF9+·FW6+ + 만20세 이하 유망주 4명+/팀. overall·능력치는 코어 공식·[30,99] 클램프로 장부와 일관. 매치데이 벤치는 상위 9인, 나머지는 예비(스쿼드) — 선발11/벤치9/예비N. 유스 유입은 계속 합성 가명. ⚠️ 실명+EA 능력치 라이선스 부채는 data-sourcing §7 원장대로 출시 전 청산. **강등 3팀(레스터·입스위치·사우샘프턴)은 카탈로그 유지 위해 실제 2025-26(챔피언십) 스쿼드로 채움.** 데이터 최신성은 에이전트 WebSearch 결과에 의존(지식 컷오프 2026-01) — 확정 2025-26 스쿼드가 baseline, 방금 성사된 이적은 누락 가능 |
| 2 | 이적·계약 스킬 7종 (overview §5.3 ⑤, 🔽) | 겨울+여름 축소판 | **미구현** — 이번 슬라이스는 ✅ 표시 스킬 전체만 | 다음 마일스톤 1순위 (거래형 판정 포함) |
| 3 | 미디어·보드 스킬 (⑥, 🔽) | 이벤트 대응형 | **미구현** — 평판 변동은 경기 결과로만 | 기자회견·보드 이벤트와 함께 |
| 4 | 저장 방식 | 이벤트 소싱 (overview §2) | **상태 스냅샷 JSON** (`engine/persistence.ts`) — 프로토타입 단순화. **내구성 보강(2026-07-25)**: 원자적 쓰기(tmp→rename)·직전 세이브 `.bak` 백업·손상 시 bak 폴백·로드 시 스키마 마이그레이션(누락 필드 기본값)·목록은 손상 파일 스킵. `.data/`는 gitignore·git 추적 해제 → 재빌드·브랜치 전환·git clean에도 유저 세이브 보존 | 이벤트 소싱 도입 시 리플레이·서사 파생 강화 |
| 5 | 온보딩 능력치 배분 | GM(LLM)이 배경 해석 (결정 #11) | **휴리스틱 규칙** (`engine/onboarding.ts`) — 실모드도 동일 | LLM 판정 + 휴리스틱 폴백. 합계 고정 계약은 동일 |
| 6 | 페르소나 시스템 (personas.md) | 핵심 NPC 풀 페르소나 + 개인 기억 | **수석코치 단일 화자 + 선수 간이 응답** — 페르소나 데이터 구조 미도입 | 페르소나 스키마·생성 파이프라인 다음 마일스톤 |
| 7 | 서사 메모리·아크 (narrative.md) | salience 태깅 + 아크 운영 | **간이 narrative 로그**만 (salience 포함, 아크 없음) | GM 컨텍스트에는 최근 로그 주입 중 |

## 기획에 없던 구현 레이어 (추가 판단)

| # | 항목 | 판단 |
| --- | --- | --- |
| 1 | **mock GM 모드** (`agents/mock-gm.ts`) | LLM 없이 전 시나리오가 도는 규칙 기반 GM/캐스터. e2e·오프라인 개발·CI용. **실모드와 동일한 엔진 스킬 경로만 사용** — 상태 변경 규약은 같다. `LLM_MODE=mock\|real` (API 키 있으면 기본 real) |
| 2 | mock 경기 스크립트 (`engine/quick-sim.ts` generateMatchScript) | mock 모드의 경기 사건 원천 — 패킷 기대 득점을 램다로 써서 전력→결과 연결 유지. 실모드에선 미사용 |
| 3 | 교체 후 스크립트 재매핑 (`match-flow.ts` remapEventsForPitch) | mock 스크립트의 득점자가 교체 아웃된 경우 온필드 선수로 재매핑 — 장부 검증과의 충돌 방지 |
| 4 | 방치 이슈 간이 구현 | 월요일 시드 확률로 고평가 벤치 선수 "출전 불만" 발생 → 사기 하락 지속 → 면담으로 해소 (game-loop §4-5의 최소 구현) |
| 5 | daily tick은 유저 팀만 상태 갱신 | AI 팀 피로·사기는 시뮬에 영향 없어 생략 (성능·단순화). AI 팀은 시즌 전환에서만 변화 |

## 데이터 모델 v6 — 카탈로그/게임 분리 · 일정 축 · 기록 테이블 (2026-07-25)

사용자 요청으로 데이터 구조를 전면 재설계했다. ERD 설계안을 여러 차례 다듬은
결과(v2~v6)이며, 아래가 구현된 최종 구조다. **이전 세이브와 호환되지 않는다**
(SAVE_VERSION=2, 구버전은 로드 거부 — 사용자 승인 후 `.data` 초기화).

### 1) 2-레이어 분리 — 카탈로그 vs 게임

- `PLAYER_CATALOG`(`engine/catalog.ts`)는 시드 데이터에서 **결정적으로 파생**되는
  불변 초기치 DB다. 새 게임 시작 시에만 읽어 `GAME_PLAYER`로 복사하고
  `catalogId`로 출처를 링크한다(유스 등 게임 중 생성 선수는 `null`).
- 덕분에 ① 성장·이적이 카탈로그를 오염시키지 않고 ② 어드민 편집 범위가 명확해지고
  ③ 시드 데이터 교체가 기존 세이브와 충돌하지 않는다.
- 팀 이름·tier는 `TEAM_CATALOG`에만 있다 — `GAME_TEAM`은 id와
  `aiManagerTacticsRating`(AI 감독의 전술 **역량치**, 전술 설정과 별개)만 갖는 얇은 엔티티.

### 2) 선수 — 능력치 7축 + 부속 이력 테이블

- **goalkeeping을 전 선수가 보유**한다(필드는 15~40, 이름 해시로 결정적 파생).
  optional·예외 분기를 없애 GK 퇴장 시 필드 선수가 골문에 서는 경우도 한 공식으로 처리.
- `PLAYER_POSITION` — 가능 포지션 + **포지션 적응도**, `isNatural`이 주 포지션(정확히 1개)이며
  positionGroup·overall 공식의 기준. 인접 포지션은 `POSITION_NEIGHBORS`로 파생(62~80).
- `INJURY` — 부위·심각도·원인·발생일·복귀예상·실제복귀. `returnedOn=null`이 현재 부상이고
  복귀하면 그대로 이력이 된다(선수당 미복귀 최대 1건).
- `BOOKING`(경고·퇴장, 발생 경기 연결) + `SUSPENSION`(정지 이력). 시즌 경고 수는
  저장하지 않고 BOOKING에서 파생 — 5회 누적/퇴장 시 SUSPENSION 생성.
- `PLAYER_GROWTH` — 훈련이든 경기든 성장이 일어나면 출처 일정(`entryId`)·대상·변화량·서술을
  기록한다. **trainXP 폐지**: "마지막 성장 이후 그 능력치를 겨냥한 완료 세션 수"를
  성장 로그에서 파생해 나이 계수(≤21:3, ≤27:4, ≤30:8세션)와 비교한다.
- `SEASON_STAT`은 (선수, 시즌, **팀**) 키 — 시즌 중 이적하면 팀별로 기록이 분리된다.

### 3) 전술이 라인업의 원본

`TACTICS`(팀별 현재 전술) → `TACTIC_ASSIGNMENT`(선수별 `role`(starting/bench)·
전술 내 `position`·**전술 적응도**·개인 지시). `GAME_TEAM`의 startingXI/bench 배열을
제거했고, 배치 없는 선수는 예비 스쿼드다. 전술 적응도가 선수 전역 1값에서
**전술별 값**이 되어 "전술 바꾸면 적응도 하락"이 구조적으로 자연스러워졌다.
전력 패킷도 주 포지션이 아니라 **배치 포지션**으로 존을 계산하고, 낯선 자리는
적응도 팩터(0.88~1.0)로 기여가 깎인다.

### 4) 일정 축 단일화 + 7월 1일 시작

- `SCHEDULE_ENTRY(date, time, type, refId, status)`에 경기·훈련·이적창 개장/폐장이
  모두 등록된다. 킥오프 시간은 요일별 슬롯(토 15:00/17:30, 일 14:00/16:30, 금·월 20:00).
- **훈련 반복 규칙 테이블 없음** — `set_training` 스킬이 지시 즉시 엔트리를 펼쳐서
  생성한다(요일 반복은 지정 주 수만큼). 변경·취소도 미래 엔트리 일괄 조작.
- 게임은 **7월 1일 = 여름 이적창 개장**과 함께 시작해 프리시즌 6주를 보내고
  8월 중순 개막. 시즌 전환도 다음 해 7/1로 이동한다.
  (구현 중 발견: 프리시즌에 "출전 기회 불만"이 발생하면 어색하므로 리그 개막 이후로 제한.
  7/1 개장 엔트리는 게임 시작 시 이미 `done` 상태로 둔다 — 온보딩이 안내.)

### 5) 이적·계약 원장

- `TRANSFER` — 이적·임대·자유계약·**유스 콜업·은퇴**까지 모든 팀 변경이 row로 남는다.
  `GAME_PLAYER.teamId`는 현재값일 뿐이고 이력의 원본은 여기다.
- `CONTRACT`가 **주급의 원본**(이전엔 팀 총액 상수 하나였다). 팀 주급 총액은 활성 계약의
  합(파생)이고, 매주 월요일 tick이 팀 원장에 지출을 기록한다. 주급은 OVR에서 산정
  (`wageForOverall`). 재계약·이적은 새 row라 연봉 변천사도 남는다.

### 6) 소유 관계 정리 · 파생 우선

- `FINANCE`(+`LEDGER_ENTRY`)는 **팀**에 — 20팀 전부 재정을 가져 이적시장의 기반이 된다.
- `SEASON_RECORD`/`TROPHY`/`ACHIEVEMENT`는 **감독**에 (각각 당시 teamId 보존) —
  팀을 옮겨도 커리어가 끊기지 않는다.
- **일지(diary) 저장 제거** — 날짜별 상세는 GROWTH·INJURY·BOOKING·MATCH·TRANSFER·
  일정에서 파생한다. (구현 중 발견: 파생 시 미래 일정까지 일지에 섞여 들어가
  "겨울 이적시장"이 7월 일지에 뜨는 버그 → `date <= state.date` 조건 추가 + 회귀 테스트.)
  `NARRATIVE_NOTE`만 GM 프롬프트에 주입되는 서사 기억으로 남긴다.

### 검증 불변식 (구현된 것)

선발 배치 11명·GK 포지션 1명·부상/정지 배치 불가, 주장 팀당 1명, `isNatural` 선수당 1개,
미복귀 INJURY 선수당 1건, 활성 CONTRACT 선수당 1건(현 소속 일치).
teamId 변경은 TRANSFER 기록과 함께, 성장은 GROWTH 기록과 함께.

---

## (이전) 데이터 정규화 — 선수 DB / 팀 DB 분리 (2026-07-25)


사용자 요청으로 데이터 구조를 정규화. **선수는 팀에 임베딩하지 않고, 평탄
`state.players[]`에 모으며 `teamId` 링크로 팀을 참조**한다. 이적은 곧 `teamId`
변경. 나이는 저장하지 않고 **`birthdate`(YYYY-MM-DD) + 게임 날짜로 계산**
(`domain/player.ts` `ageOf`).

- **도메인**: `Player`에 `teamId`·`birthdate` 추가, `age` 제거. `Team`에서
  `players` 제거(= `{id,name,shortName,startingXI,bench}`). 팀+선수 묶음이 필요한
  곳은 `TeamRoster = Team & { players: Player[] }`.
- **엔진 상태**: `GameState.players: Player[]` 추가. 조회 헬퍼 `playersOf(state,
  teamId)`·`userPlayers(state)`·`playerById(state, id)`(전역)·`teamRoster(state,
  teamId)`. 팀 엔티티는 선발/벤치 **id 목록**만 보유.
- **경기**: `sim` `SideInput`에 `players: Player[]` 주입(선수는 팀에 없으므로).
  `buildStrengthPacket`은 `starters(players, startingXI)`로 선발을 구성.
- **어드민·시드**: `epl-players.ts` 614명을 `birthdate`로 변환(구 age→birthYear
  = 2026−age, 월/일은 이름 해시). 어드민 편집·추가도 `age` 대신 `birthdate`
  (웹은 `type="date"` + 계산 나이 표시).
- **세이브 마이그레이션**(핵심): `persistence.ts` `migrate()`가 옛 임베딩
  포맷(`teams[].players` + 선수별 `age`)을 로드 시 자동 정규화 —
  `birthdate = ${로드연도−age}-01-01`(연도만 복원되므로 1/1로), `teamId` 부여,
  `age` 제거, `teams[].players` 삭제 → 유저 기존 게임이 그대로 살아남는다.
  회귀 테스트 `persistence-migrate.test.ts`로 왕복 보장. **구게임은 birth 연도만
  복원 가능**(월/일 유실) — 신규 게임은 시드의 실제 `birthdate` 사용.

## 다중 리그 — 5대 리그 96팀 + 대회 축 (2026-07-26 — 사용자 요청)

사용자 요청: 선수 DB를 5,000명 규모로 확대 + 팀 카탈로그 생성 + 유럽 대항전.
확인 결과 5,000명은 약 180팀(11개 리그)이 필요하고, 제 지식만으로는 그 규모의
실선수를 정확히 만들 수 없다. **하이브리드**로 확정 — 5대 리그는 웹 검증 실선수,
그 아래는 합성. 순서는 **다중 리그 엔진 먼저**, 대항전은 그 위에.

### 팀 카탈로그 (2026-27, 96팀)
서브에이전트가 리그별 위키·공식 사이트를 교차검증해 승격·강등을 확인했다.
**EPL이 4팀 바뀌었다** — 웨스트햄·울버햄튼(강등)·레스터·사우샘프턴이 나가고
선더랜드·리즈·코번트리·헐이 들어왔다(입스위치는 재승격). 기존 16팀은 id를 그대로
유지해 실선수 시드가 계속 붙는다. 빠진 4팀의 시드는 파일에 남겨 뒀다 —
`buildFromSeed`가 카탈로그를 순회하므로 읽히지 않고, 챔피언십 추가 시 되살린다.
(그래서 실선수 시드가 614 → 491명으로 줄었다.)

미확인·주의: 중위권 순위(세리에 11~15위, 분데스 10~13위)는 위키 외 2차 검증을
하지 못했다 — tier 밴딩에만 쓰이므로 영향은 작다. shortName은 EPL·분데스만 공식
약어이고(분데스는 B04·M05·S04처럼 숫자 포함) 나머지는 통용 표기를 정규화했다.
리그를 넘나드는 충돌만 해소했다(브레스트 BRE→BRS, 모나코는 공식 ASM).

### 대회 축
- `MATCH.competitionId` 추가 (SAVE_VERSION 3). 경기 id도 `m-epl-1-17-arsenal` 형태
- `computeStandings(state, leagueId)` — 순위표는 대회별. 팀·경기를 모두 그 리그로 좁힌다
- `buildAllLeagueMatches` — 전 리그가 같은 캘린더 골격을 공유해 A매치 휴식기·
  박싱데이가 리그를 가로질러 맞는다. 18팀 리그는 34라운드라 `anchorsFor`가
  주중 라운드부터 덜어낸다 (개막·최종 라운드는 유지)
- `allMatchesDone`은 유저 리그 기준 — 전 리그를 기다리면 시즌 전환이 늦춰진다
- **`SCHEDULE_ENTRY`는 유저 대회 경기만 등록한다.** v6 "일정 축 단일화"에서
  벗어나는 판단인데, 일정 축은 실질적으로 *감독의 달력*이고(그래서 `teamId`가
  nullable이다) 타 리그 경기는 세계 상태일 뿐이다. 11개 리그면 엔트리가 4,000개를
  넘고 `entriesOn`이 매일 그걸 훑는다

측정: 96팀 · 선수 1,931명(실선수 491 + 절차 생성 1,440) · 경기 1,752 ·
일정 엔트리 384 · createGame 35ms · 세이브 1.63MB(선수당 약 845바이트).
유닛 테스트 총 실행이 4s → 14.5s로 늘었다 — 성능 정리(다음 단계)의 근거.

### 스쿼드 하한 40명 + 성능 실측 (성능 문제는 없었다)

사용자 요구로 클럽당 **최소 40명**을 맞췄다. `FALLBACK_TEMPLATE`을 18 → **42명**
(1군 28 + 아카데미 14)으로 늘렸다. 아카데미 자원은 16~20세에 능력치를 tier
기준선보다 12~20 낮게 출발하되 **잠재력 폭을 크게**(+8~28) 줘서 유스 발굴의
재미를 만든다. 실선수 시드가 들어와도 아카데미는 계속 합성 가명으로 채운다
(실존 유소년에게 가상 서사를 입히지 않는다는 결정, narrative.md §7).

**성능 정리는 결과적으로 불필요했다.** 목표 규모(선수 3,851명 · 96팀 · 경기
1,752)에서 실측한 값:

| 항목 | 측정 |
| --- | ---: |
| createGame | 43 ms |
| structuredClone(state) (턴 백업) | 17 ms |
| JSON.stringify(state) | 16 ms |
| saveGame / loadGame | 22 / 9 ms |
| playersOf(팀) | ~0 ms |
| advanceTime 1일 (타 리그 시뮬 포함) | 0.5 ms |
| 세이브 크기 | 2.78 MB |

LLM 턴이 20초 이상 걸리는 게임에서 저장 오버헤드 22ms는 무의미하다. 카탈로그
델타 저장이나 선수 인덱싱은 **하지 않았다** — 측정 근거 없는 최적화는 복잡도만
늘리고, 델타 저장은 카탈로그가 코드와 함께 바뀔 때 구세이브를 조용히 왜곡시킬
위험이 있다. 필요해지면 그때 한다.

⚠️ 앞선 턴에 "유닛 테스트가 4s → 14.5s로 늘었다"고 적었던 것은 **오독**이었다.
그 숫자는 vitest가 워커별 시간을 합산한 `tests` 값이고, 실제 벽시계(`Duration`)는
1.1s → 2.96s다. 96팀 세계를 매 테스트가 새로 만드는 비용으로 타당한 수준이다.

**하한 보충**: 실선수 시드가 있는 16팀은 30~34명뿐이어서 하한에 미달했다.
`buildFromSeed`가 부족분을 합성 아카데미로 채운다 — 결과 **정확히 4,000명,
전 클럽 40명 이상**(최대 42). 실선수 스쿼드가 들어오면 1군이 실명으로 바뀌고
아카데미 보충은 그대로 남는다.

부수 발견: `lookup.test.ts`의 안개 유출 검사가 오탐을 냈다. 메시지 전체에서
참값 숫자를 찾는 휴리스틱이라 아카데미 선수의 나이·출전 수와 우연히 겹쳤다.
대상 선수 행만 보고 공개 정보(나이·출전·득점·복귀일)를 먼저 지우도록 고쳤다.

### 유럽 4대 리그 실선수 스쿼드 (2026-07-27 완료)

클럽별 에이전트 76개 + 리그 QA로 조립했다. **실선수 491 → 2,673명**
(카탈로그 총 3,848명 · epl 491 / laliga 567 / seriea 576 / bundesliga 526 / ligue1 513).

두 번에 나눠 돌렸다. 1차(76클럽)는 7.5시간·890만 토큰 지점에서 리그앙 후반
13클럽과 QA 4개가 전부 용량 문제로 죽었고, 2차로 그 13클럽 + QA를 다시 돌렸다.
**교훈: 대규모 팬아웃이 장시간 돌면 뒤쪽이 통째로 죽는다.** 하위 리그를 추가할
때는 리그 단위로 쪼개 돌린다. 또 워크플로우 반환값보다 **디스크가 진실이다** —
1차 보고는 62클럽이었지만 파일은 63개였다(릴이 파일을 쓴 뒤 반환에 실패).

QA 에이전트가 잡은 것: 릴 GK 2→3(출처 근거로 올메타 추가), PSG 중앙 MF 7→9
(측면 자원 재분류), 생년월일 6건·이름 1건 교정(렌 Nagida), 중복 0건 확인
(514명 전수 — 비티냐는 PSG/제노아에 생년월일이 다른 실제 동명이인).

병합 시 기계 검증으로 포지션군 2건(캄비아소·프라스 MF→DF)을 코드에서 재도출해
교정했다. 리그 간 동명 4건은 실제 동명이인이라 유지했다(팀별 슬러그 id라 충돌 없음).

**브레스트는 4개 소스가 일치해 1군이 21명뿐이다.** 아카데미만 붙이면 센터백
2명으로 시즌을 시작하게 되므로, 하한 보충을 **포지션군 인식**으로 바꿨다 —
부족한 군(목표 GK3·DF9·MF9·FW5)을 1군급 합성으로 먼저 메우고 남는 자리를
아카데미로 채운다. 브레스트는 40명(GK4·DF13·MF14·FW9)이 됐다.

⚠️ 능력치는 구단 등급 밴드에 맞춘 **판단값**이지 출처가 있는 수치가 아니다.
QA가 클럽별 top-11 평균을 TIER_BASE와 대조해 정합성만 확인했다(T1 −1.5 ~ T4 −2.0).
리저브·B팀 18명은 위키 교차검증이 안 돼 구단 공식·Transfermarkt 근거로 유지했다.

5,000명까지는 하위 리그 추가가 남았다.

## 시즌 일정 — 실제 EPL 캘린더 골격 재현 (2026-07-26 — 사용자 요청)

사용자 요청: "일정을 실제 EPL 일정을 참고해서 작성해달라". 선택지를 제시했고
**실제 대진표 복제가 아니라 캘린더 골격 재현**으로 확정했다. 실제 2026-27
대진표를 그대로 쓸 수 없는 이유는 **팀 카탈로그가 2024-25 시즌 구성**이기
때문이다 (레스터·입스위치·사우샘프턴 = 2024-25 승격 3팀이자 그 시즌 강등 3팀).
실제 대진을 쓰려면 팀 카탈로그와 선수 명단을 함께 갈아야 한다 — 별도 작업.

개정 전 문제: 38라운드가 예외 없이 정확히 7일 간격이었다. A매치 휴식기도,
박싱데이도, 주중 라운드도 없고 시즌이 4월 말에 끝났다.

재현한 골격 (`calendar.ts`):
- 8월 중순 **금요일 밤 개막전** → 5월 마지막 일요일 최종전
- A매치 휴식기 4회(9·10·11·3월) + 컵 대회 주말 2회 — 그 주말엔 리그 경기 없음
- 박싱데이(12/26) 라운드 고정 + 성탄~신년 주중 연전
- 휴식기로 잃은 라운드를 주중(수요일) 라운드로 메워 38라운드를 맞춘다
- 주말 라운드 10경기를 금(1)·토(5: 12:30/15:00×3/17:30)·일(3)·월(1)로 분산
- 최종 라운드는 10경기 **동시 킥오프**(16:00)

편성 제약 — 테스트로 고정한 불변식:
- 어떤 팀도 홈 3연전·원정 3연전을 하지 않는다 (원형 편성 + 홈/어웨이 교대)
- 경기 사이 최소 하루 휴식 (이틀 연속 경기 금지). 주중 라운드를 하루에 몰고
  `wellSpaced`로 앞뒤 주말과 2일 이상 띄워 보장한다
- 역경기는 최소 8라운드 뒤 — 후반기 순서는 백트래킹으로 찾은 고정 배열
  (`SECOND_HALF_ORDER_20`). 단순 미러/회전은 후반기가 전반기 재방송이 된다
- 8시즌까지 항상 38라운드가 나온다 (휴식기 날짜가 해마다 달라도)

구현 중 발견한 결함 두 개 (둘 다 눈에 보이는 인공물이었다):
1. **킥오프 시간을 "그 날의 몇 번째 경기"로 따로 계산**하고 있었다. 그래서 배열
   앞쪽 팀이 항상 그 날 가장 이른 킥오프(토 12:30)를 받았다 — 한 팀이 38경기 중
   21경기를 같은 슬롯에서 치렀다. 날짜와 시간을 `slotFor`에서 **함께** 정해 해결.
2. 슬롯을 `(i + round) % 10`으로 회전시키니 원형 편성의 순번과 선형으로 얽혀
   특정 팀이 여섯 라운드 내내 월요일 밤 경기만 했다. **라운드별로 슬롯을 새로
   섞어** 해결.
또 팀 순서가 카탈로그 순서로 고정돼 **모든 게임이 매 시즌 똑같은 대진표**를
썼다. `buildMatches(season, teamIds, seed)`로 세이브·시즌마다 새로 추첨한다.

## LLM 입력 3층 재배치 + 정보 비대칭 (2026-07-25 3차 — 사용자 요청)

사용자 요청: ① 타 팀 선수 조회 수단 ② `phase`를 입력에서 제거(라우팅 전용)
③ `buildGmContext`가 매 턴 들어가는 문제 최적화, 읽기 전용 도구로 풀기
④ 안개는 **선수 단위**, 능력치별 오차 rumoured ±6 / seen ±3, 관전 여부로 구분,
스카우팅 완료 시 안개 해제하되 **잠재력만 은폐**.

측정으로 시작했다 (`count_tokens` 실측). 개정 전 GM 턴은 컨텍스트 1,751 +
이력 ~900 토큰을 **매 턴 정가로** 다시 읽었고, 그중 83%가 스쿼드 상위 18명
표였다. 더 큰 문제는 `cache_control`이 `system` 하나뿐이라 **이력이 캐시를 전혀
타지 않았던 것** — 문서에 "history 재사용으로 멀티턴 캐시가 탄다"고 적혀 있었지만
틀린 서술이었다. 또 도구+시스템 프리픽스가 4,357 토큰으로 Opus 4.8의 최소 캐시
프리픽스 4,096을 261 토큰 차이로 겨우 넘고 있었다(미달이면 조용히 캐시가 꺼진다).

해결은 "컨텍스트를 줄이기"가 아니라 **변경 빈도에 따른 재배치**였다 —
고정(도구+프롬프트) / 레퍼런스(명부·패킷) / 이력 / 이번 턴. 상세는
docs/design/llm-io.md. 구현에서 조심한 함정들:

- **이력 정규화** — 문자열 content와 블록 배열을 섞으면 프리픽스 바이트가 달라져
  매 턴 캐시가 깨진다. 어댑터가 한 곳에서 모양을 고정한다.
- **마커 비오염** — `cache_control`을 원본 이력에 남기면 세이브에 누적되어 다음
  턴이 브레이크포인트 상한(4개)을 넘겨 400이 된다. 요청용 복사본에만 붙인다.
- **정렬 안정성** — 명부를 OVR로 정렬하면 훈련 성장으로 순서가 흔들려 캐시가
  깨진다. (포지션, id)로 고정.
- **이력 창 앵커** — `slice(-12,-1)`는 매 턴 한 칸씩 미끄러져 프리픽스가 계속
  달라진다. 시작점을 6턴 단위로만 옮긴다.
- **상태 채널** — 휘발성 상태는 `messages` 끝의 `role:"system"`으로 (Opus 4.8,
  베타 헤더 불필요). 캐시 프리픽스를 건드리지 않고 감독 발화로도 오독되지 않는다.
  미지원 모델은 유저 메시지에 접어 넣는 폴백 — DeepSeek 전환 때 쓰인다.

실측 결과: 매 턴 정가 토큰 **~2,700 → 300~400**. 정보량은 오히려 늘었다
(18명 표 → 전원 명부 + 온디맨드 상세 + 타 팀·순위·일정).

안개는 **테이블을 만들지 않고 파생**한다 — MATCH 결과의 출전 명단 + SCOUT_REPORT.
그래서 벤치에만 앉았던 선수는 `seen`이 아니고, 우리가 뛰지 않은 경기의 선수도
아니다. 오차는 `hash(seed, playerId, 능력치)`로 결정적이다 — 매번 새로 뽑으면
GM이 어제 한 말과 오늘 한 말이 달라져 스카우팅 정보를 신뢰할 수 없게 된다.
안개가 낀 선수에겐 숫자를 아예 주지 않고 라벨만 준다(주면 모델이 그대로 읊는다).

`GM_SYSTEM` 철칙 5를 뒤집은 것도 이 작업의 일부다. 개정 전 "조회 질문은
컨텍스트로 즉답한다 — 도구 불필요"는 컨텍스트에 없는 것을 지어내라는 지시였다.

## 수치 초안 (balance.md 대상 — 코드가 현재 원본)

- 판정 공식: 팀토크 base −3~+3 × intensity/2 × 리더십 계수(0.7+lead/99×0.6), 이벤트당 사기 한도 ±6 (면담 ±8)
- 감독 XP: 100당 +1 (상한 90). 승리 리더십+10, 전술 기인 골 tactics+12/골, 팀토크 성공 8×강도
- 훈련 성장(v6): 세션 누적 임계 → 능력치 +1, 필요 세션 수 3/4/8/없음 (≤21/≤27/≤30/31+). XP 통 없음 — 성장 로그에서 파생
- 경기 반영: 폼 승+1/패−1(득점자 +1), 사기 승+4/무+1/패−4, 선발 피로 +34
- 시즌 전환: 30+ pace −1~3 · physical −0~2, 은퇴 35+ 또는 33+&OVR<72, 팀당 신인 max(1, 은퇴 수). 다음 시즌 7/1로 이동
- 주급: OVR 기반 (max(40,OVR)/40)^4.2 × £6k, 활성 계약 합이 팀 주급 총액
- 간이 시뮬: 홈 어드밴티지 ×1.06, λ = 1.4×(전력비)³ clamp 0.35~3.4
- 스카우팅(2026-07-25 확정): 관측 오차 rumoured ±6 / seen ±3 / scouted·own 0.
  파견 소요 `SCOUT_DAYS = 7`일, 동시 파견 `SCOUT_CONCURRENT_LIMIT = 3`명, 비용 없음
  (**잠정** — 파견 비용·스카우트 직원 능력치는 미설계). 잠재력은 own만 공개

## 기술 결정

- TypeScript 6.x 고정 — TS 7(네이티브)을 typescript-eslint가 미지원
- 워크스페이스 상대 임포트는 확장자 없이 (Bundler 해석) — Next webpack 호환
- Next.js 15 App Router + React 19, 워크스페이스 패키지는 `transpilePackages`
- e2e: Playwright + mock GM 모드 (LLM 무의존). 실모드 검증은 스모크로 별도

## 선수 카탈로그 어드민 — 게임과 무관한 초기치 편집 (2026-07-25, v6 확정)

사용자 요청: **"어드민은 게임과 관련없는 카탈로그만 편집할 수 있는 어드민이어야 한다."**
초기 구현은 진행 중인 세이브의 `GAME_PLAYER`를 편집했는데, v6의 2-레이어 분리 취지와
맞지 않아 **카탈로그 전용**으로 재구성했다.

- **편집 대상** — `PLAYER_CATALOG`(모든 게임이 공유하는 초기치). 게임 선택 UI가 없다.
- **저장** — 편집 결과는 데이터 디렉터리의 `player-catalog.json`에 원자적으로 저장되고,
  이후 `playerCatalog()`가 그 파일을 우선 읽는다(없으면 시드 파생). 캐시는 데이터
  디렉터리 단위로 무효화한다(테스트가 임시 디렉터리를 쓰므로).
- **격리 보장** — 게임은 시작 시 카탈로그를 복사해 `GAME_PLAYER`로 인스턴스화하므로,
  카탈로그 편집이 **진행 중인 세이브에 영향을 주지 않는다**. 새로 시작하는 게임만 반영.
  (엔진 테스트 + 웹 API 테스트 양쪽에 이 격리를 검증하는 케이스를 뒀다.)
- **되돌리기** — `시드 기본값으로` 버튼이 오버라이드 파일을 삭제해 원본 복귀.
  UI에 `편집됨` 배지로 현재 상태를 표시한다.
- **파생값은 저장하지 않는다** — 카탈로그에 나이·overall이 없다. 표시용으로만
  `age`(2026-08-15 기준)·`overall`(주 포지션 공식)을 계산해 내려준다.
- **안전장치** — 팀 최소 14명·GK 2명 미만이 되는 삭제는 반려(새 게임의 라인업을
  채울 수 없게 되는 것을 막는다).
- API: `GET/POST/DELETE /api/admin/catalog`, `PATCH/DELETE /api/admin/catalog/player/[playerId]`
  (게임 id를 받지 않는다). `dataDir`는 순환 참조를 피해 `engine/src/paths.ts`로 분리.

> 진행 중인 게임의 선수 상태를 직접 만지는 경로는 의도적으로 없다 — 게임 중 변화는
> 플레이(스킬)로만 일어나야 한다는 원칙(코어=장부)에 맞춘 결정.

## 훈련·전술 시스템 재설계 (2026-07-25 2차 — 사용자 요청 7건)

| # | 요청 | 구현 |
| --- | --- | --- |
| 1 | 달력 UI 지정 불필요, 채팅 스킬로만 | 달력 훈련 편집기 제거(읽기 전용 표시). `/training` API 삭제. 훈련은 채팅 `set_training` 스킬로만 |
| 2 | 특정 날짜·요일 훈련 지정 | `set_training(weekly['0'~'6'], byDate['YYYY-MM-DD'])`. 해석: byDate > weekly |
| 3 | 하루 오전·오후 따로 | 각 날에 `{am, pm}` 세션 분리. tick이 세션별로 적용 |
| 4 | 시작 시 훈련 미등록 | createGame `training: {weekly:{}, byDate:{}}`. 미지정 세션은 무훈련(자율 회복) |
| 5 | 기본 훈련 개념 제거 | `teamFocus` 폴백 삭제. 등록된 세션만 효과 |
| 6 | 경기 일정 주말 분산 | buildFixtures가 라운드 토요일 기준 금~월로 결정적 분산(WEEKEND_OFFSETS). buildSeasonCalendar가 개막을 토요일로 스냅 |
| 7 | 자유서술 훈련 | 세션 = `{label(자연어), focus[]}`. GM(LLM)이 자연어를 label+focus로 해석(코어=장부), focus: 6대 능력치+tactical+recovery. 훈련일마다 label 포함 결과를 일지에 기록 |
| — | 전술 적응도 | `state.familiarity`(선수별 0~99, 기본 60). setTactics 변경 폭(포메이션 −25·슬라이더 단계당 −4·패스 −6)만큼 하락, 경기 출전 +8·tactical 훈련 세션 +2 상승. strength-packet에 적응도 팩터(0.85~1.0) 반영. 스쿼드 뷰·GM 컨텍스트에 노출 |
| — | 전술판→채팅 미전송 | `/lineup` 저장의 채팅 append 제거. GM은 buildGmContext의 "현재 전술·선발 평균 적응도"로 읽어 반응(전술 변경 시 적응도 하락을 경고) |

브라우저 실검증: 자연어 "월 오전 세트피스/오후 회복 러닝, 수 종일 전술훈련"이 label+focus로
정확히 등록, 3-5-2+압박 변경 시 적응도 60→27 하락 및 수석코치가 대가를 경고, 새 게임 일정이
일·토·금·월로 분산됨을 확인. 옛 세이브는 마이그레이션으로 훈련 리셋·적응도 기본값 로드(옛
게임의 일정은 생성 당시 스케줄 유지).

## 훈련 스케줄·스쿼드 관리 (2026-07-25 — 사용자 요청 5건, 아래 재설계로 대체된 부분 있음)

| # | 요청/질문 | 구현·답변 |
| --- | --- | --- |
| 1 | 훈련을 일자별·요일별로 설정 | `TrainingPlan.weekly`(요일 0~6)·`byDate`(YYYY-MM-DD) 오버라이드 + `rest`(휴식일). tick이 당일>요일>팀기본 순으로 해석. `setTrainingSchedule` 스킬(+GM 도구 `set_training_schedule`)과 `POST /training` API, 달력 상세의 드롭다운(이 날 하루)·"요일마다 적용"·"기본으로" 버튼으로 설정 |
| 2 | 기록 없는 날의 표시 제거 | 훈련 점(gold)은 **오늘 이후 예정 훈련일**(평일·비경기·휴식 아님)에만. 지난 훈련은 일지 점(blue)으로만 → 시작 전 날짜·주말·휴식일·오프시즌엔 아무 표시 없음 |
| 3 | 지난 훈련 결과 표시 | 훈련일마다 일지에 결과 한 줄(테마·향상된 선수·능력치 변화 또는 "변화 없음") 기록 → 달력에서 지난 날짜 클릭 시 확인. 휴식일은 "😴 휴식일" |
| 4 | 벤치/예비 자원 설정 (질문) | (기존) 자동 = 비선발 중 OVR 상위 9=벤치. (신규) 전술판에서 각 비선발 칩의 **벤치/예비 배지**로 매치데이 18인(선발11+벤치9)을 직접 지정 |
| 5 | 스쿼드 편집의 채팅 반영 (질문) | (기존) `/lineup`이 상태만 바꿔 GM이 다음 턴 컨텍스트로 인지. (신규) 저장 시 수석코치 확인 대사를 채팅에 append + 서사 로그 → 편집이 메인 채팅에 남고 GM 컨텍스트에도 반영 |

## UI·시스템 확장 2차 (2026-07-24 — 사용자 요청 8건)

| # | 요청 | 구현 |
| --- | --- | --- |
| 1 | 스킬 파라미터 클릭 확인 | `ToolCallRecord.input` 기록(실모드 wrap + mock 주요 지점) → 채팅 칩 클릭 시 요약+JSON 파라미터 펼침 |
| 2 | 대사 글자 타이핑 효과 | 시간 기반(60cps, 밀린 만큼 가속) rAF 리빌 + 깜빡이는 캐럿. rAF는 백그라운드 탭에서 멈추므로 **300ms 인터벌 이중 틱**으로 커밋 보장 (브라우저 검증에서 발견한 정지 버그 수정) |
| 3 | 스트리밍 파싱 깨짐 | 미완성 `@화자` 줄(콜론 미도착)은 다음 델타까지 보류, 미닫힘 `*연출`은 연출로 간주, NDJSON 라인 파스 실패는 스킵 후 회복 |
| 4 | 전술판 스쿼드 보드 | 포메이션 5종별 슬롯 좌표 레이아웃(피치 렌더), 탭 선택-스왑 + HTML5 DnD, 포메이션 변경 시 그룹 우선 재배치, 포지션 세분화(RCB/LCB/RWB/LWB/CDM/CAM/RCM/LCM/SS 추가 — 슬롯 코드가 선수 포지션이 되고 OVR 재산정). `/lineup`에 formation 지원 |
| 5 | 달력 세부 일정 | `state.diary`(일자별 일지: 훈련 성과·부상·복귀·불만·카드·경기 결과) + 달력 일자 클릭 상세 — 경기(결과·득점자), 예정 훈련(팀 테마·개인·회복조), 지나간 기록 |
| 6 | 재정 월별 상세 | `finance.ledger` 원장(잔고 변동의 유일 경로) — 주급(금), 중계권·스폰서·운영비(월초, tier 기준), 홈 입장 수입(경기별). 재정 탭에 월별 수입/지출 항목·합계·순익 |
| 7 | 순위 팀명 한글 | 순위표가 shortName 대신 한글 name 표시 |
| 8 | 채팅 선수 id 노출 | mock의 `@id:` 화자 수정, GM·캐스터 프롬프트 화자/지칭 규칙을 이름으로, 경기 컨텍스트 id(이름) 병기, 서버 `humanizePlayerIds` 후처리 + 클라이언트 `playerNames` 맵 치환(스트리밍 델타까지 커버) |

수치 초안(재정, £/월): TV 중계권 tier1~4 = 13/12/11/10M · 스폰서 6/4/2.5/1.5M ·
운영비 6/5/4/3M · 홈 입장(경기당) 4.5/3.5/2.5/1.8M · 주급은 기존 주 단위 유지.
일지는 시즌 전환 시 리셋, 원장은 2000행 롤링.

## UI 확장 (2026-07-22 — 사용자 요청 4건)

| # | 기능 | 구현 |
| --- | --- | --- |
| 1 | **채팅 스트리밍** | `TurnRequest.onText` → anthropic 어댑터가 `messages.stream()`으로 텍스트 델타 방출(실모드=진짜 스트리밍, mock=완성 텍스트 줄단위 청크). 신규 `POST /turn/stream`이 NDJSON(delta/done/error)로 흘려보내고, 클라이언트가 진행 중 모델 턴을 실시간 렌더. 잠금·롤백은 `lib/turn-runner.ts`로 공유(JSON 턴과 동일 뮤텍스) |
| 2 | **달력 뷰** | `views.calendar`(유저 38경기 일정·결과·다음 경기, 이적창) + 클라이언트 월별 그리드. 평일 훈련은 점으로 표시, 오늘 하이라이트 |
| 3 | **스쿼드 편집** | `setPlayerPosition`(포지션 라벨+그룹+OVR 재산정) + `POST /lineup`(포지션 변경 후 `setLineup`). 스쿼드 탭에 편집기(선발/교체/제외 토글·포지션 드롭다운·11/GK 실시간 검증). 경기 중(phase=match)엔 채팅 교체만 허용 |
| 4 | **모바일·전체화면 탭** | 사이드바 오피스 → 상단 탭바(채팅·스쿼드·달력·재정·순위·커리어) 전체화면 전환. `@media(max-width:640px)` 반응형(탭바 가로 스크롤, 편집기 2열 재배치, `.hide-sm`) |

> e2e 전용 포트를 3399로 분리(플레이 서버 3311 재사용 방지). 실모드·모바일에서
> 4기능 직접 검증 완료(스트리밍 중간 렌더·달력·라인업 저장·손흥민 포지션 변경).

## 적대적 리뷰 결과 (2026-07-22 — 52 에이전트 워크플로우)

5개 차원(코어 규칙·엔진 상태·LLM 경계·API/동시성·기획 정합) 파인더 + 발견별
반박 검증으로 47건 발견 → 39건 확인(중복 제거 시 ~25건). **확인된 결함은 전부
수정**했고, 각 수정에 회귀 테스트를 남겼다.

### 수정된 주요 결함

| 분류 | 결함 | 수정 |
| --- | --- | --- |
| 시즌 소프트락 | 유스에 GK가 없어 ~17시즌 후 GK 전멸 → 라인업 확정 불능 | 시즌 전환 시 그룹별 최소 인원(GK2·DF5·MF4·FW4) 강제 보충 (`season.ts`) |
| 성장 미반영 | 훈련·쇠퇴 후 overall 미재계산 → XI 선발·전력 패킷 왜곡 | `recomputeOverall` 도입, 훈련 +1·시즌 쇠퇴 직후 호출 |
| 유스 ID 충돌 | `season*10+i` 공식이 대량 은퇴 시즌에 ID 중복 생성 | `-y{season}-{i}` 형식으로 시즌 명시 |
| 출전 규칙 우회 | 부상·징계 선수가 선발/벤치/교체로 출전 가능 | `sanitizeUserLineup`(자동 대체) + `substitutePlayer` 경계 검증 |
| 카드 누적 부재 | 퇴장·경고 누적이 다음 경기에 무영향 | `seasonYellows`(5장→1경기)·`suspensions` 도입, `finalizeMatch`에서 정산 |
| 퇴장자 반영 누락 | 퇴장 선수가 경기 후 피로·폼 반영에서 빠짐 | `ledger.sentOff`를 출전자 집합에 합류 |
| 부상 RNG 채널 | `injury:{round}`에 시즌 미포함 → 시즌 간 동일 난수열 | `injury:{season}:{round}` |
| 경기 장부 | 하프타임 뒤 같은 배치 사건 허용, 킥오프 중복 허용, 교체 창 무제한 | 하프타임 강제 정지점, 킥오프 1회, 교체 창 3회(하프타임 제외) |
| 서사 스킬 | 부분 적용·무제한 호출 가능 | 전체 검증 후 일괄 적용(원자성) + 일일 3회 한도 |
| LLM 히스토리 | 턴 중단 시 tool_use 블록이 닫히지 않아 다음 턴 API 오류 | 합성 tool_result로 마감 (`anthropic-adapter.ts`) |
| 실모드 GM | idle GM에 대화 히스토리 미주입 → 맥락 단절 | `state.chat` 최근 12턴 주입, 경기 컨텍스트에 onPitch·교체 현황 포함 |
| mock 의도 오인 | 질문("언제 경기야?")이 advance_time을 발동 | 의도 정규식 강화 + 질문 감지 시 진행 차단 |
| API 경합 | 같은 게임 동시 턴이 저장을 상호 덮어씀 / 턴 실패 시 부분 커밋 | 게임별 뮤텍스 + 턴 시작 스냅샷 롤백 (`turn/route.ts`) |
| 시간 흐름 | 부상·불만 발생을 감독이 모른 채 지나감 | `advanceTime`이 `attention`으로 정지, GM이 보고 |

### 확인했지만 수정하지 않은 것 (by design / 허용)

- 실모드에 `advance_match` 스킬 없음 — 결정 #14 이후 캐스터 턴 자체가 진행
  단위라 별도 스킬이 불필요.
- mock GM은 한 메시지의 다중 의도 중 첫 분기만 처리 — mock은 테스트·오프라인용
  간이 구현이므로 허용.
- 경기 총 12골(팀당 6골 상한) 가능 — 극단값이지만 규칙 위반은 아님.
- 분당 이벤트 수 한도 없음 — 배치당 20개 한도로 갈음.

## 실모드(Opus) 스모크 절차 (✅ 2026-07-22 실행 — 통과)

```bash
# 키는 apps/web/.env.local 의 ANTHROPIC_API_KEY (gitignore 포함, 커밋 금지)
PORT=3311 pnpm dev                # LLM_MODE 미지정 시 키 있으면 real
# 확인 항목: ① @문법 준수 ② 스킬 tool call 정확성(반문 포함)
# ③ 판정 outcome의 설득력 ④ 경기 장면 log_match_events 정합
```

1차 스모크 결과 (온보딩 + 훈련 지시 턴): ①②③ 통과 — @화자/`*연출*` 문법 준수,
`set_training_focus` 정확 호출, 다중 의도(훈련+수비 점검+주장 질문)를 한 턴에
소화하며 숫자 노출 없이 스카우트식 서술(결정 #2), 주장 공석을 짚고 반문으로
마무리. ④(경기 장면)는 실플레이에서 확인 예정.
