# 데이터 소싱 전략 (Data Sourcing)

> 🚧 **초안** · 선수/팀 데이터를 어디서 어떻게 가져올 것인가

**한 줄 요약: 능력치는 사올 수 없다 → 생성한다.**
무료·유료 라이브 API 중 FM·FIFA식 능력치(스피드·슈팅·패스)를 파는 곳은
하나도 없고, 능력치를 가진 소스(SoFIFA/EA, Football Manager DB)는 전부 게임사
IP라 상업적으로 못 쓴다. 따라서 데이터 전략의 본질은 *"능력치를 구해오는
문제"*가 아니라 **"실측 스탯에서 자체 능력치를 생성하고 LLM으로 서사를 입히는
문제"** 다 — 이는 [AGENTS.md](../../AGENTS.md)의 "내러티브 우선 · 코어 vs LLM
분리" 비전과 정확히 같은 방향을 가리킨다.

> 조사 범위: 5개 축 · 47개 소스 → 38개를 가격·라이선스·운영상태까지 교차검증.

---

## 1. 전제 (현 단계 결정)

| 항목      | 결정                                      |
| --------- | ----------------------------------------- |
| 단계      | MVP / 프로토타입                          |
| 선수 표기 | **실명** (정식 출시 시 가명 전환 검토)    |
| 라이선스  | MVP에서는 보류, **§7 부채 원장에 기록만** |

→ 프로토타입 단계라 §2에서 🔴로 분류한 능력치 소스도 **개발 입력**으로는 바로
쓴다. 단 그것이 정식 출시 전 청산해야 할 "라이선스 부채"임을 명시한다(§7).

---

## 2. 라이선스 신호등 — 소스 3등급 분류

기준은 데이터를 "구할 수 있느냐"가 아니라 **"상업 게임에 탑재할 수 있느냐"**.

### 🟢 그대로 상업 사용 OK — 법적 코어

| 소스                                         | 제공 데이터                                                              | 라이선스                                  | 한계                               |
| -------------------------------------------- | ------------------------------------------------------------------------ | ----------------------------------------- | ---------------------------------- |
| **OpenFootball** (`github.com/openfootball`) | 리그/팀/일정/결과/클럽/스타디움. EPL\~4부·5대리그·월드컵·UCL, 2010\~2026 | **CC0 (퍼블릭도메인)** — 재배포·수정 자유 | 선수 능력치/상세스탯 전무          |
| **Wikidata** (`wikidata.org`)                | 선수 메타(생년·국적·포지션·소속이력), 이적 이력, ID 허브                 | **CC0**                                   | 경기 스탯·능력치 없음, 정확도 편차 |

### 🟡 유료 구독 시 사용 — 실측 스탯 (단 IP·초상권은 별도)

| 소스                    | 가격                                               | 데이터                                                                | 주의                                                                        |
| ----------------------- | -------------------------------------------------- | --------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| **API-Football**        | 무료 100req/일 · Pro $19 · Ultra $29 · Mega $39/월 | 1,200+ 리그, fixtures, 선수 시즌 실측스탯, 이적, 라인업, 라이브스코어 | ⚠️ "상업 사용 허용"은 과장 — 약관상 데이터 상업 권리를 부여하지 않음        |
| **Sportmonks**          | 무료 2리그 · €29\~249/월                           | 라이브/이벤트/스쿼드/선수·코치 통계                                   | 상업 허용, raw 재판매 금지                                                  |
| **Entity Sport**        | **$250\~750/월**                                   | 판타지 강점, 선수 통계 50+타입                                        | 단일 브랜드 라이선스                                                        |
| **Football-Data.co.uk** | **무료 CSV**                                       | 1993년\~ 경기결과+배당, 27개국                                        | 약관 자체가 없는 **회색지대**, 선수 데이터 없음. AI 모델 학습/검증엔 최상급 |

### 🔴 학습/참고만 — 게임 탑재 금지

| 소스                        | 가진 것                            | 왜 못 쓰나                                                            |
| --------------------------- | ---------------------------------- | --------------------------------------------------------------------- |
| **SoFIFA / EA FC (Kaggle)** | ⭐유일하게 FM식 능력치(0\~99)      | Kaggle CC0 태그여도 **원천은 EA IP** — CC0는 업로더 기여분만          |
| **Football Manager DB**     | 가장 정교한 능력치(CA/PA, 371속성) | SI/SEGA가 **적극 법적 집행** (FMInside 강제삭제 전례)                 |
| **Transfermarkt**           | 시장가치·이적의 사실상 표준        | 공식 API 없음, 약관이 스크래핑+**AI 학습까지 명시 금지**              |
| **FBref**                   | (과거)xG 등 고급스탯               | ⚠️ **2026-01 Opta 계약 종료로 고급스탯 전부 삭제** — 현재 기본 스탯만 |
| **StatsBomb Open Data**     | 최고급 이벤트 데이터(좌표·xG)      | 약관 1.2.2조 **"상업적 이용 명시 금지"** (자유롭다는 건 오해)         |

---

## 3. 라이선스 지뢰밭 — 4개 권리 레이어

실명 선수/클럽을 쓰는 순간 **독립된 4개 권리**를 동시에 건드린다.

| #   | 권리                      | 리스크                | 핵심                                                                                                                        |
| --- | ------------------------- | --------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| 1   | 선수 이름·초상권 (NIL)    | 🔴 높음               | FIFPRO가 65,000명 집단 라이선스. **Keller v. EA 판례: 이름 빼고 아바타만 써도 식별되면 침해** — "가명이면 안전"은 틀린 가정 |
| 2   | 클럽명·엠블럼·킷·스타디움 | 🔴 높음 (특히 비주얼) | 맨유 v. SEGA 소송 → FM은 'Man UFC'로 개명 합의                                                                              |
| 3   | 리그명·로고               | 🟡 중\~높음           | 독점계약 많음 (EA가 EPL \~$590M, LaLiga 네이밍라이츠 독점)                                                                  |
| 4   | 데이터(사실 스탯) 자체    | 🟢 낮음               | 미국(NBA v. Motorola)·EU(BHB) 모두 **사실은 비보호**. 단 타사 DB 통째 추출은 EU DB권+계약 위반                              |

→ **데이터(사실)는 자유롭지만, 실명 선수에 붙이는 순간 1번(NIL)으로 회귀.**
그래서 "자체 산정 스탯 + 가명/합성" 조합이 안전성을 살리는 길(→ §7).

---

## 4. MVP 데이터 적재 레시피 — 실명 · 즉시 가능

다운로드 즉시 가능한 소스로 실명 데이터를 빠르게 구성한다. 🔴는 §7 청산 대상.

| 역할                                   | 소스 / 데이터셋                                                                             | 제공                                                                                      | 취득                                      | MVP 라이선스        |
| -------------------------------------- | ------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | ----------------------------------------- | ------------------- |
| ⭐ **선수 능력치·신체** (게임 핵심)    | **fcratings.com** (EA FC 27 공개 등급 미러)                                                 | 1.6만 선수 · 6축 + 세부 33축 · OVR · 생년월일 · 키 · 체중 · 국적 · 주발 · 약발 · 스킬무브 | 클럽 페이지 651개 + 선수 페이지 (JSON-LD) | 🔴 원천 EA IP       |
| **리그·팀·일정** (구조)                | OpenFootball                                                                                | 5대리그+EPL 4부+월드컵, 2010\~2026                                                        | `git clone` / JSON·TXT                    | 🟢 CC0              |
| **선수 메타·ID 허브**                  | Wikidata                                                                                    | 생년·국적·포지션·소속이력, 외부 ID 매핑                                                   | SPARQL / 덤프                             | 🟢 CC0              |
| **시장가치·이적**                      | Kaggle Transfermarkt 미러 (`davidcariboo/player-scores`, `dcaribou/transfermarkt-datasets`) | 시장가치, 이적료·이력, 계약                                                               | CSV                                       | 🔴 TM 약관          |
| **실측 경기 스탯** (시뮬 보정·AI 학습) | Football-Data.co.uk (CSV, 무료) + API-Football (라이브)                                     | 경기결과+배당(1993\~), 선수 시즌 스탯, 라인업                                             | CSV / REST                                | 🟡 회색 / 유료 약관 |

### 4.1 시드 갱신 절차 — 두 축을 선수 단위로 잇는다

명단과 능력치는 **서로 다른 시점의 소스**라 클럽으로 이을 수 없다. 위키는 매일
갱신되지만 EA는 게임 출시 시점의 스쿼드에 묶여 있어서, 여름 이적이 끝난 8월에
EA의 소속을 믿으면 이미 떠난 선수가 그대로 남는다. 그래서 **소속은 위키, 사람은
EA**로 나누고 (이름 + 생년월일)로 조인한다.

| 축                        | 소스                                  | 무엇을 정하나                          |
| ------------------------- | ------------------------------------- | -------------------------------------- |
| 명단·등번호               | 위키백과 구단 문서 `{{Fs player}}` 표 | 누가 이 클럽에 있는가, 몇 번인가       |
| 능력치 6축·신체·주발·약발 | fcratings (EA FC 27)                  | 그 사람이 어떤 선수인가                |
| **위키데이터 QID**        | 위키백과 구단 문서 링크 → 문서 제목   | **조인 키** — 동명이인을 가른다        |
| **생년월일**              | 위키데이터 `P569` (없으면 위키백과)   | 나이, 그리고 QID가 없는 선수의 조인 키 |
| 주급                      | salarysport (EPL만)                   | 초기 계약                              |

조인 규약 — **성립하는 조인만 값을 덮는다.**

| 이름                                     | 생년월일                  | 판정                             |
| ---------------------------------------- | ------------------------- | -------------------------------- |
| 성명 완전 일치                           | 연·월·일 일치             | ✅ EA 값을 받는다                |
| 성명 완전 일치                           | 월·일 일치, 연도만 1 차이 | ✅ 받는다 (연도 오타 계열)       |
| 성명 완전 일치                           | 그 외                     | ❌ **다른 사람이다** — 조인 실패 |
| 약칭 한 토막 (`Gabriel`·`Kepa`·`Álvaro`) | 연·월·일 일치             | ✅ 받는다                        |
| 약칭 한 토막                             | 그 외                     | ❌ 조인 실패                     |

⚠️ **대조하는 생년월일은 위키(위키데이터)의 것이지 시드의 것이 아니다.** 시드의
생년월일은 지난 갱신이 남긴 파생물이라 그 자체로는 근거가 못 되고, 근사치가 섞여
있다. 그래서 조인이 성립하면 **생년월일도 위키 확인값으로 함께 바로잡는다.**
그러지 않으면 다음 갱신에서 같은 조인이 또 어긋난다.

#### 위키데이터 QID — 조인 키의 첫째 축

`RealPlayerSeed.wikidataId`가 있는 선수는 **조인 키가 QID다.** 위 표의 (이름 +
생년월일)은 QID가 없는 선수에게만 남는다 — 그 조합에는 사람을 가르지 못하는 자리가
있기 때문이다(§4.1.1).

채우는 경로는 하나뿐이고, 한 번에 여러 선수를 처리한다.

| 단계 | 무엇을                                                                                 |
| ---- | -------------------------------------------------------------------------------------- |
| 1    | 구단 문서 위키텍스트에서 **1군 절**의 `{{Fs player}}`(별칭 `football squad player`) 행 |
| 2    | 행의 `name=[[문서 제목\|표시명]]`에서 **문서 제목**                                    |
| 3    | 제목을 시드 행에 잇는다 — 같은 구단 안에서 `nameEn`이 정규화 후 일치하는 행            |
| 4    | 제목 → `action=query&prop=pageprops&ppprop=wikibase_item` (50개씩) → QID               |

**문서 제목이 동명이인을 이미 갈라 둔다** — `Vitinha (footballer, born March 2000)`과
`Vitinha (footballer, born February 2000)`은 서로 다른 문서다. 그래서 3단계의 이름
일치가 구단 안에서 유일하면 QID까지 1:1이 성립한다.

⚠️ **표시명이 같아도 제목이 다르면 다른 사람이다 — 시드의 `nameEn`은 제목을 쓴다.**
세비야 35번은 표에 `[[Iker Ortín Muñoz|Iker Muñoz]]`로 실려 있어 표시명이 오사수나의
`Iker Muñoz`(Q116603156)와 같다. 표시명으로 실으면 두 사람이 (이름 + 생년월일) 키를
나눠 갖고, 한쪽 생년월일이 바로잡히는 순간 서로를 덮는다 (`seed-join.test.ts`의
중복 축이 잡는다). 제목이 붉은 링크면 QID는 없는 채로 남지만, 제목은 그때도 있다.

⚠️ **없으면 없는 채로 둔다.** 위키 문서가 없는(붉은 링크) 아카데미 자원과 링크가
동음이의 문서를 가리키는 행은 QID 없이 남는다 — 시드 2,847명 중 93명이 그렇다.
주발·부상 이력과 같은 원칙이다(§7). 지어낸 QID는 없는 것보다 나쁘다: 이 키의
목적이 위키 재대조 없이 중복을 잡는 것인데, 가짜 키는 정확히 그 반대를 한다.

⚠️ **QID를 검증하는 것은 생년월일이 아니라 그 행이다.** QID의 근거는 구단이 자기
문서에 실은 그 링크다. 생년월일은 반대 방향으로 흐른다 — QID가 정한다(바로 아래).

#### 생년월일은 `P569`가 정한다

**QID가 있으면 그 선수의 `birthdate`는 위키데이터 `P569`다.** 여기에는 이름 매칭이
끼지 않으므로 오조인 위험이 없고, 시드의 옛 값은 지킬 근거가 없다 — 지난 조인이
남긴 근사치가 섞여 있어 몇 살씩 어긋난 값도 그 안에 있다.

조회는 QID를 묶어 던진다 — SPARQL `VALUES` 250개씩, `p:P569/psv:P569`로 **시간값과
정밀도와 순위를 함께** 받는다. `wdt:P569` 한 줄로는 아래 표의 두 예외가 보이지
않는다.

| `P569`의 상태                        | 시드가 하는 일                             |
| ------------------------------------ | ------------------------------------------ |
| 일(日) 정밀도 값 하나 (순위 반영 후) | **덮는다**                                 |
| 문장이 없다                          | 옛 값 유지                                 |
| 정밀도가 연(年)·월(月)               | 옛 값 유지 — `YYYY-01-01`은 정보가 더 적다 |
| 서로 다른 값이 같은 순위로 둘 이상   | 옛 값 유지 — 근거가 하나로 좁혀지지 않는다 |

순위는 `PreferredRank`가 있으면 그것만 보고 `DeprecatedRank`는 버린다.

⚠️ **예외 셋을 추정으로 메우지 않는다.** 연도만 아는 값으로 덮으면 월·일이 있던
자리가 자리표시자로 바뀌고, 갈리는 두 값 중 하나를 고르면 그 선택은 근거가 아니라
취향이다. QID가 없는 93명도 같은 이유로 이 절이 닿지 않는다.

지금 QID 2,754명 중 **2,718명이 `P569` 그대로**이고, 나머지 36명(문장 없음 2 ·
정밀도 미달 10 · 값이 갈림 24)은 옛 값으로 남아 있다.

⚠️ **생년월일을 덮으면 `potential`을 다시 잰다** — 나이가 대역을 정하기 때문이다
(아래 "그리고 `potential`을 다시 잰다").

#### 자리표시자 생년월일 — `birthdateApprox`

`YYYY-01-01`은 **연도만 알고 월·일은 조사가 닿지 않았을 때 채운 자리**인 경우가
있다. 실제 1월 1일생과 생김새가 같아서, 표시하지 않으면 둘을 영영 가르지 못한다.

그래서 **`-01-01`인 시드는 `RealPlayerSeed.birthdateApprox`를 반드시 명시한다.**

| 값      | 뜻                                             |
| ------- | ---------------------------------------------- |
| `true`  | 자리표시자 — 연도만 근거가 있고 월·일은 모른다 |
| `false` | 대조해서 실제 1월 1일생임을 확인했다           |
| 없음    | 허용하지 않는다 (`seed-join.test.ts`가 잡는다) |

표식이 없으면 "조사했는데 1월 1일이더라"와 "조사하지 않았다"가 같은 값으로
보인다. 주발·부상 이력과 같은 원칙이다(§7): **모르는 것은 모른다고 적는다.**

⚠️ **`birthdateApprox: true`인 생년월일은 조인 키가 아니다.** 위 조인 표의 판정에
쓰지 않고, 조인 실패의 기본값인 "옛 값 유지"도 이 값에는 적용하지 않는다 — 지킬
사실이 없기 때문이다. 확인된 날짜(위키·위키데이터, 없으면 EA)가 나오면 **그대로
덮고 표식을 지운다.**

자리표시자는 나이만 흐리는 게 아니라 성장·노화 곡선의 입력까지 어긋나게 한다
(`squad/development.ts`) — 연도까지 틀린 경우가 있어 두 살이 어긋나기도 한다.
확인 기준은 좁다: 리그·TM 기록의 생일이 **출생지 공란인 DB 자리표시자 형태**면
확인으로 치지 않는다. 지금 시드에 `-01-01`은 14명이고 그중 13명이 확인된 실제
1월 1일생, 마디 마칼루(헤타페) 한 명만 `true`다.

**조인 실패의 기본값은 옛 값 유지다.** 이름이 같다는 이유로 값을 덮으면 동명이인
한 사람의 레코드가 다른 사람에게 실린다. 옛 값은 낡았을 뿐이지만, 남의 값은 틀렸다.

⚠️ **월·일까지 어긋나는데 연도만 맞다고 받는 완화는 쓰지 않는다.** 동명이인은
생일의 월·일에서 갈리므로, 그 완화는 6축이 통째로 20 가까이 뒤집히는 오조인을
그대로 통과시킨다.

⚠️ **옛 값이 이미 남의 값인 경우가 있다.** 조인 실패라고 무조건 되살리면 안 된다 —
옛 6축 자체가 다른 나라 하위 리그 동명이인의 레코드일 수 있다. 옛 값을 되살리기
전에 그 값도 실물과 대조한다.

⚠️ **EA의 생년월일에도 오류가 있다.** 그래서 조인 실패가 곧 시드가 틀렸다는 뜻은
아니다 — 시드가 실제 생일을 갖고 EA가 틀린 경우, 그 선수는 앞으로 EA 갱신이 닿지
않는다. **예외 목록으로 뚫지 않는다.** 예외 목록은 위 완화 규칙의 다른 이름이고,
낡은 값이 남의 값보다 낫다.

⚠️ **EA의 신체도 틀린다.** 조인이 성립했더라도 **실물과 어긋나는 게 확인되면 위키
확인값이 이긴다** — 사비 시몬스 체중 58kg(키 179에 BMI 18.1로 프로 대역 밖),
고스기 게이타 168cm(실제 172). 신체는 사람을 가리는 축이기도 해서, 자리에 있을 수
없는 값은 조인 성공의 증거가 아니라 실패의 증거다(비카리오 160cm 골키퍼).

조인 실패로 EA 값을 버릴 때도 **위키에서 확인한 신체는 넣는다.** 값이 없으면
카탈로그가 능력치로 파생하는데(`catalog.ts` `physiqueOf`), 확인된 실측이 있는데
지어낸 값을 쓸 이유가 없다.

마지막에 **"한 EA 레코드는 한 선수만"**이라는 1:1 제약으로 남은 충돌을 끊는다.

**그리고 `potential`을 다시 잰다.** EA가 덮는 것은 6축이고 잠재력은 그 자리에
남는데, 6축이 바뀌면 파생 OVR이 움직여 간격이 혼자 벌어지거나 뒤집힌다.
**생년월일도 같은 것을 움직인다** — 대역은 나이별이라 하루 차이로도 칸이 바뀐다.
갱신이 끝나면 나이 대역(→ [player.md](player.md) §6.5)으로 전수를 재고 **벗어난
선수만** 경계로 접는다 — 대역 안의 값은 사람이 매긴 판단이라 일괄 재계산하지 않는다.

조인이 성립한 뒤에도 **값이 뒤집히면 조인을 의심한다.** 갱신 뒤에 자동으로 걸리는
가드가 넷이다 (`packages/engine/test/seed-join.test.ts`) — 전부 순수 데이터
불변식이고, 임계는 **정상 시드에서 위반 0명**이 되는 자리에 선다.

| 무엇을 보나                             | 왜 그 축인가                               | 잡히는 예                            |
| --------------------------------------- | ------------------------------------------ | ------------------------------------ |
| 주급 백분위 − 파생 OVR 백분위           | 주급(salarysport)은 EA와 **무관한 출처**다 | 외데고르 주급 98%·OVR 5%             |
| 자리와 프로필의 모순                    | 윙어의 최고축이 수비일 수 없다             | 칸첼리에리가 수비수 프로필로 뒤집힘  |
| 잠재력−OVR 간격이 나이 대역 상한을 넘음 | 잠재력도 6축 조인과 독립인 수기 판단값이다 | 산투스 OVR 61 · 잠재력 87            |
| 자리별 키 하한                          | 160cm 골키퍼는 존재하지 않는다             | 비카리오 194→160cm, 고메스 188→167cm |

⚠️ **셋째 축은 갱신 뒤에 `potential`을 함께 재야 성립한다.** `potential`은 그때의
6축에 맞춰 손으로 매긴 값이라, EA 실측이 들어와 OVR이 정당하게 내려간 선수는
잠재력만 낡은 채 남는다. 그 낡은 값을 나이 대역으로 접어 두어야
(→ [player.md](player.md) §6.5) 이 축이 **주급 괴리와의 AND 없이 단독으로** 선다 —
AND로 좁히면 주급 축이 없는 EU 2,192명이 통째로 사각지대가 된다.
**갱신 절차의 마지막 단계가 대역 재측정인 이유가 이것이다** — 건너뛰면 가드가
자기가 만든 잡음에 묻힌다.

#### 국적은 `P1532`가 정한다 — 갈리면 대표팀 기록이 가른다

**축구에서 국적은 나라가 아니라 협회다.** 잉글랜드·스코틀랜드·웨일스·북아일랜드는
한 시민권을 나눠 갖는 네 협회이고, 홈그로운도 비EU 쿼터도 대표팀도 전부 협회 단위로
걸린다. ISO 3166-1은 그 넷을 담지 못하므로(`GBR` 하나뿐이다) 코드는 **FIFA 3자 협회
코드**를 쓴다 (`domain/nationality.ts`) — 대부분의 나라에서 ISO alpha-3와 같고,
갈리는 자리에서 축구 쪽을 따른다(`GER`·`NED`·`SUI`·`POR`·`DEN`·`CRO`·`KVX`).

조회 경로는 QID 하나뿐이라 이름이 끼지 않는다 — 생년월일 `P569`와 같은 결이다.
`scripts/fill-nationality.ts`가 돌리고(`pnpm fill-nationality [--write]`), **시드의
`nationality`를 적는 것은 이 스크립트뿐이다.** 손으로 고친 값은 다음 실행이 되돌린다.

사다리는 네 칸이고, 위 칸이 답하면 아래로 내려가지 않는다.

| 사다리 | 무엇이 정하나                                                  | 지금 시드           |
| ------ | -------------------------------------------------------------- | ------------------- |
| ①      | 스포츠 국적 `P1532` — 순위 반영 후 값이 하나                   | 2,637명             |
| ②      | 갈리면 **성인 대표팀 출전 수**(`P54` → `Q135408445` · `P1350`) | 29명                |
| ③      | 그래도 갈리면 시민권 `P27`이 확인된 협회로 좁힌다              | 35명                |
| ④      | 그래도 갈리면 **코드 오름차순**                                | 28명                |
| —      | `P1532`도 `P27`도 없음 · QID 없음 → 시드를 비운다              | 25명 + 93명 = 118명 |

⚠️ **연령별 대표팀은 세지 않는다**(②). 프랑스 U-21을 거쳐 세네갈 A대표가 된 선수의
협회는 세네갈인데, 연령별을 함께 세면 유스 경기 수가 A매치를 이긴다. 그래서 성인
대표팀 항목(`Q135408445`)만 본다 — 그 통계가 아예 없으면 ③으로 내려간다.

⚠️ **④까지 온 선수는 A매치를 뛴 적이 없다** — 현실에서도 어느 협회를 택할지 정해지지
않은 자리다. 그래서 코드 오름차순은 주장이 아니라 **표기 규칙**이고, 두 협회를 모두
싣는 것이 그 자리에서 할 수 있는 전부다. 판정에는 영향이 없다: `isEuNational`이 두
칸을 함께 보므로 등록 자격은 순서와 무관하다.

**둘째 국적은 하나만 싣고, 여럿이면 EU 협회를 먼저 싣는다.** 이 칸이 게임에서 하는
일이 정확히 등록 자격 판정이라, 하나만 실을 수 있다면 규정을 가르는 쪽이 실린다.
영국 시민권(`Q145`)은 협회가 아니므로 이미 고른 협회가 영국 안이면 그 협회의 몫으로
접히고, 아니면 `ENG`로 선다.

⚠️ **협회가 아닌 항목은 버린다** — 카탈루냐·과들루프·마르티니크(FIFA 회원이 아니다)와
유고슬라비아 연방 공화국(사라진 나라)이 `P27`·`P1532`에 섞여 온다. 지어낸 코드보다
나쁜 것이 **아무 규정도 읽지 못하는 코드**다. 리포트가 버린 항목을 건수와 함께 낸다.

**빈 채로 두지 않는다 — 이것만 생년월일과 다르다.** 등록 규정도 대표팀도 "국적을
모르는 선수" 갈래를 따로 들 수 없어서, 하나가 비면 그 위의 규칙이 통째로 서지 못한다.
그래서 위 118명은 **시드를 비워 두고 카탈로그가 그 클럽 협회로 세운다**
(`deriveNationality` — `world/catalog.ts`). 절차 생성 선수도 같은 규칙이다: 이름을
이미 리그 국적의 풀에서 받으므로(`data/names.ts`), 국적만 따로 굴리면 잉글랜드 이름을
단 세네갈 사람이 스물몇 명씩 선다. **이것은 실측이 아니라 부채다**(§7) — 시드가
채워지면 파생은 물러난다.

전수는 시드 2,847명 중 2,729명(둘째 국적 462명)이고, 카탈로그 5,317명은 파생까지
합쳐 전원이 국적을 갖는다 (`checkPlayerNationality`가 새 게임 앞에서 센다).

#### 절이 명단을 정한다 — 등번호가 아니라

시드에 들어가는 것은 **1군 절(`현 스쿼드`·`First-team squad`)의 행 전부**다.
그 밖의 절 — 임대(`Out on loan`)·2군(`Reserve team`)·유스(`Under-21s and
Academy`·`Primavera`·`B team`)·`Other players under contract`·`Returning from
loan` — 의 인원은 넣지 않는다.

⚠️ **등번호가 없다고 유스가 아니다.** 1군 표 맨 아래에는 아직 번호를 못 받은
1군 선수가 `no=`를 비운 채 실린다 — 래시퍼드(맨유)·헨더슨(첼시)·퓔크루크
(웨스트햄)가 그 자리에 있다. 번호로 거르면 이들이 통째로 사라진다. **번호가
없는 것은 번호일 뿐 소속이 아니다** — 그 행은 명단에 넣고 번호만 비운다
(§4.1.1 마지막 항목이 그때의 번호 규칙이다). 세리에A 네 구단(아탈란타·라치오·
파르마·사수올로)은 아예 표 전체가 번호 없는 알파벳순이다.

유스를 거르는 것은 **절**이지 번호가 아니다. 1군 절에 없는 선수는 그 클럽에
없다 — 다만 위 절들을 모두 훑은 뒤에 판정하고, 그래도 안 보이면 선수 개인 문서의
`currentclub`으로 한 번 더 확인한다 — 아카데미에 실명을 쓰지 않는 이유는
[people.md](people.md) §2에 있다.

#### 새로 넣는 선수 — 생년월일이 문턱이고, 능력치는 자리 기준치다

**생년월일이 확인되지 않으면 넣지 않는다.** 위키 개인 문서에도 위키데이터에도
없는 사람은 조인 키가 없어서, 다음 갱신이 그를 같은 사람으로 알아보지 못한다.
그 자리는 카탈로그가 클럽당 40명 하한을 메우며 합성 가명으로 채우므로 스쿼드
깊이는 그대로다 — 확인되지 않은 실명을 넣는 쪽이 잃는 것이 더 크다.

**능력치는 fcratings가 원본이고, 닿지 않으면 자리 기준치다.** 자리 기준치는
**그 구단 같은 포지션군 실선수의 축별 중앙값**이다 — 지어낸 값이 아니라 이미
시드에 있는 값에서 나오고, 같은 입력이면 같은 값이 나온다. 이때 신체·주발·약발은
**비운다**: 카탈로그가 파생하는 자리이고, 비어 있다는 것 자체가 "조사가 닿지
않았다"는 표식이다(`birthdateApprox`와 같은 원칙). `potential`은 중앙값을 쓰되
파생 OVR과의 간격을 나이 대역 안으로 접는다(→ [player.md](player.md) §6.5).

⚠️ **자리 기준치는 실측이 아니라 부채다.** 다음 fcratings 갱신이 덮어야 할
자리이고, 덮이기 전까지 그 선수의 6축은 "이 구단 이 자리의 보통"일 뿐이다.

#### 4.1.1 소속 판정 — 한 선수는 한 구단에만 있다

**임대 선수는 임대처(host) 스쿼드에 넣는다.** 시드에는 임대 관계를 담는 필드가
없어 소속이 곧 그 시즌 그가 뛰는 곳이고, 그것을 정하는 표는 임대처 구단 문서의 현
스쿼드다(`other=on loan from …`). 모구단 문서의 `other=at X until …` 행은 계약의
기록이지 소속의 근거가 아니다 — 그 행을 따라 모구단에 남기면 임대처 스쿼드에서
그만큼 깊이가 빠지고, 모구단은 뛰지 않는 선수의 급여를 이중으로 진다.

**두 구단 문서가 같은 선수를 함께 실으면 한쪽이 낡은 것이다.** 갱신일이 문서마다
달라 이적한 선수가 옛 구단 표에 며칠 더 남는다. 이때는 선수 개인 문서의
`currentclub`이 가른다 — 구단 문서 둘의 다수결이 아니다.

⚠️ **이름이 같아도 생년월일이 다르면 다른 사람이다.** 비티냐 둘(2000-02-13 PSG ·
2000-03-15 제노아), 무사 디아라 둘(2000-11-10 알라베스 · 2003-01-22 말라가),
알렉스 히메네스 둘(2005-05-08 피오렌티나 · 2005-04-19 오사수나)이 실재한다. 생일이
19일밖에 안 떨어진 동명이인이 있어서 **(이름 + 생년월일)은 중복을 다 잡지 못한다** —
생일이 어긋난 중복이 그 검사를 그대로 통과한다. QID가 그 자리를 메운다: 세 쌍 모두
서로 다른 위키 문서이고, 곧 서로 다른 QID다. `seed-join.test.ts`가 지키는 불변식은
넷이다.

| 불변식                                  | 무엇을 잡나                                                                                      |
| --------------------------------------- | ------------------------------------------------------------------------------------------------ |
| 한 QID는 시드 전체에서 한 행뿐이다      | 생일이 어긋난 중복 — 두 구단에 걸친 것도, 한 구단 안의 것도. **시드만 읽어서 갈린다**            |
| QID는 `Q` + 숫자다                      | 오타 난 키는 다른 사람의 키가 아니라 아무의 키도 아니라서, 중복 검사를 조용히 빠져나간다         |
| 같은 (이름 + 생년월일)이 두 구단에 없다 | QID가 없는 93명에게 남은 축 — 복붙으로 생긴 중복                                                 |
| 한 클럽 안에서 등번호가 겹치지 않는다   | 선수를 옮길 때 조용히 깨지는 자리 — `ensureSquadNumbers`는 빈 번호만 채우고 충돌은 그대로 남긴다 |

**옮긴 선수의 등번호는 옮긴 구단 표의 번호를 받는다.** 표가 번호를 주지 않으면
(`no=`가 빈 임대 행) 옛 시드 번호를 유지하되, 그 번호를 이미 표에 실린 선수가
쓰고 있으면 **표 쪽을 남기고 옮겨온 쪽을 비운다**(§7 등번호 항목과 같은 규칙).

**주급은 구단을 따라가지 않는다.** `weeklyWage`는 salarysport가 EPL 선수 단위로
집계한 추정치라, 선수가 EPL 밖으로 나가면 그 축이 닿지 않는다 → 값을 지우고
모델 추정(`estimateSquadWages`)에 맡긴다. EPL 안에서 구단만 바뀌면 값을 유지한다.

---

## 5. 데이터 통합 — 조인 키 전략

소스마다 선수 ID 체계가 달라 "같은 선수"를 잇는 게 통합의 핵심 작업이다.
**Wikidata를 ID 브리지**로 쓴다.

| 소스                        | 식별자                           | 연결 방법                                         |
| --------------------------- | -------------------------------- | ------------------------------------------------- |
| EA FC 데이터셋              | `sofifa_id`                      | Wikidata 보유 외부 ID(`P`속성)로 1차 매핑         |
| Transfermarkt               | `tm_player_id`                   | 〃                                                |
| OpenFootball / API-Football | 팀명·선수명 문자열               | 없는 건 **이름 + 생년 + 소속클럽** 퍼지 매칭 보강 |
| **Wikidata**                | `QID` (+ sofifa/TM ID 종종 보유) | **허브 역할** — 여기를 기준으로 나머지를 연결     |

**시드가 이미 그 축을 들고 있다** — `RealPlayerSeed.wikidataId`가 위키백과 구단
문서에서 온 QID이고(§4.1), 시드 2,847명 중 2,754명이 갖는다. 새 소스를 붙일 때
이름 매칭부터 다시 시작하지 않고 이 열에서 출발한다.

→ 최종 통합 키는 자체 `internal_player_id` 발급. 매핑 테이블은
`packages/domain`에서 단일 관리.

---

## 6. 3+1 계층 아키텍처

데이터를 라이선스 안전도와 역할에 따라 계층화한다. `packages/` 구조에 매핑.

| 계층        | 내용                                                                                                       | 소스                                      | 매핑 패키지                      |
| ----------- | ---------------------------------------------------------------------------------------------------------- | ----------------------------------------- | -------------------------------- |
| 🟢 ① 구조   | 리그/팀/일정/선수 메타 = 골격                                                                              | OpenFootball + Wikidata (CC0)             | `domain`                         |
| 🟡 ② 실측   | 경기·시즌 퍼포먼스 스탯, 정기 갱신                                                                         | API-Football / Sportmonks / Football-Data | `domain` + 적재 파이프라인       |
| 🔵 ③ 능력치 | **실측 → 능력치 변환 AI 모델 (자체 학습)**. EA/FM 능력치는 교사 데이터로 참고만, 출시엔 자체 산정값만 탑재 | (③에서 생성)                              | build-time 파이프라인 + `domain` |
| 🟣 ④ 서사   | LLM 생성: 선수 전기·인터뷰·스카우트 리포트·보드진/기자 드라마                                              | LLM                                       | `agents` + `narrative`           |

> ④는 [AGENTS.md](../../AGENTS.md) 1장 "내러티브 우선" 비전이 사는 계층이자
> story-fm의 차별점. ③은 코어(결정적)에 들어가는 정적 데이터를 생성하는
> 단계이므로 LLM 비의존 파이프라인으로 두고 결과를 캐싱한다(코어 vs LLM 경계).

---

## 7. 라이선스 부채 원장 — 정식 출시 전 청산

MVP에선 실명·🔴소스를 쓰되, 상업 출시 전 해소한다. **여기는 무엇을 어떻게 갚을
것인가를 정해 둔 자리**이고, 갚는 장치는 코드에 서 있다 — 지금 없는 것은 **실행
시점** 하나다(§7.4).

### 7.1 청산은 세 갈래다 — 갈래가 다르면 갚는 방법도 다르다

| 갈래       | 무엇이 문제인가          | 어떻게 갚는가                              | 장치                                                     |
| ---------- | ------------------------ | ------------------------------------------ | -------------------------------------------------------- |
| **가명화** | **이름** — NIL·상표      | 문자열을 바꾼다. 숫자는 그대로 둔다        | `data/pseudonym.ts` · `scripts/pseudonymize.ts`          |
| **생성**   | **숫자** — 남의 저작·DB  | 코어가 제 모델로 낸다. 남의 값을 안 읽는다 | `world/synthesis.ts` ([player.md](player.md) §13)        |
| **미탑재** | **그림·인격** — 이중보호 | 아예 싣지 않는다. 필요하면 절차 생성한다   | 표를 비운다 · 문장(crest) 생성 ([team.md](team.md) §3.1) |

세 갈래가 갈리는 기준은 **그 자리를 비웠을 때 게임이 무엇을 잃는가**다. 이름을
바꾸면 세계의 얼굴이 바뀌고(플레이는 그대로), 숫자를 새로 내면 밸런스가 움직이고,
그림·인격은 비워도 잃는 것이 거의 없다. 그래서 **미탑재가 가장 싸고 가명화가 가장
비싸다** — 위험의 크기와 순서가 반대라는 것이 이 표의 요점이다.

### 7.2 우선순위 — 위험 ÷ 비용

숫자가 낮을수록 먼저다. 근거는 "법적 위험이 큰가"가 아니라 **"위험에 비해 갚는
값이 싼가"**다.

| 순위 | 자리                                          | 갈래   | 갚는 값                                              |
| ---- | --------------------------------------------- | ------ | ---------------------------------------------------- |
| 1    | 엠블럼·킷·리그 로고·구장 비주얼               | 미탑재 | **0** — 애초에 담은 적이 없다. 담지 않기로 못 박는다 |
| 2    | 세계 인물 명부 · 수석코치 시드                | 미탑재 | **0** — 표를 비우면 코드가 가상 이름으로 돌아간다    |
| 3    | 능력치 · 시장가 · 주급 · 키/체중              | 생성   | 모델은 이미 서 있다 — 남은 값은 밸런스 재검증이다    |
| 4    | 실명 선수 · 클럽명 · 리그명 · 구장명 · 등번호 | 가명화 | 파이프라인 한 번 — **세계의 얼굴이 통째로 바뀐다**   |
| 5    | 부상 이력 · 홈그로운 자격                     | 딸림   | 0 — 4가 끝나면 근거가 사라져 함께 정리된다           |

5의 갈래가 셋 중 어느 것도 아닌 것은 **따로 갚을 것이 없기 때문**이다. 실존
선수를 가리켜야만 성립하는 값이라, 4가 끝나면 가리킬 대상이 사라져 함께 닫힌다.

1·2가 먼저인 것은 **위험이 가장 큰데 값이 0이기 때문**이다. 실존 인물에게 게임이
지어낸 성격·대사를 입히는 일(인물 명부)과 미술저작물+상표로 이중보호되는 그림
(엠블럼)은 이 원장에서 가장 위험한 두 자리인데, 둘 다 **표를 비우는 것만으로**
끝난다. 반대로 4는 위험이 3보다 낮지 않은데도 마지막인데, 갚는 값이 기술이 아니라
**제품 결정**이기 때문이다(§7.4).

### 7.3 갈래별 방침

**가명화 — 문자열만 바꾸고 조인은 지킨다.**

- 바꾸는 것은 **표시 이름**(`nameKo`·`nameEn`·클럽 `name`/`shortName`·`stadium`·
  리그 `name`)뿐이다. **id는 손대지 않는다** — id를 바꾸면 세이브·오버라이드·
  일정·기록이 전부 끊긴다.
- 매핑은 **결정적**이다. 같은 입력은 언제나 같은 가명을 내므로, 파이프라인을 두 번
  돌려도 세계가 흔들리지 않고 두 실행의 diff가 곧 시드 변경분이다.
- 이름 풀은 **`data/names.ts` 그것**이다 — 절차 생성 선수가 이미 쓰는 풀이라, 가명
  전환 뒤에도 실명 자리와 합성 자리가 같은 모양으로 읽힌다. 나라를 따라가므로
  세리에A 명단이 통째로 잉글랜드 사람이 되지 않는다.
- ⚠️ **Keller 판례상 가명이어도 식별되면 위험하다.** 등번호·나이·국적·능력치가
  그대로면 "9번 노르웨이인 스트라이커"는 여전히 그 사람이다. 그래서 가명화는
  **생성(3순위)과 함께** 끝나야 효력이 있다 — 이름만 바꾸는 것은 절반이다.
- ⚠️ **클럽명 가명화는 구장명·리그명과 한 묶음이다.** 클럽만 바꾸면 "가명 클럽이
  안필드를 쓴다"가 되어 오히려 더 또렷하게 지목된다.

**생성 — 남의 숫자를 안 읽는 것이 목표다.**

- 능력치는 **`world/synthesis.ts`**가 낸다. 입력은 실측이 아니라 **자리·체급·
  스쿼드 깊이·나이** 넷이고, 그 넷은 전부 게임이 스스로 아는 값이다
  ([player.md](player.md) §13).
- 분포는 **`pnpm balance attribute-model`**이 지금 시드와 견준다. 밴드는 절대값이
  아니라 **두 분포의 간격**에 걸려 있어, 시드가 바뀌어도 밴드가 낡지 않는다
  (→ [../simulation/balance-harness.md](../simulation/balance-harness.md)).
- 시장가·주급은 **이미 자체 모델이 있다**(`marketValueOf`·`wageExpectationOf`).
  부채는 시드의 `weeklyWage` 한 필드뿐이고, 그 필드를 지우면 모델이 대신 답한다.
- 키·체중은 `physiqueOf`가 능력치에서 파생한다 — 시드 값을 지우면 자동으로 넘어간다.

**미탑재 — 담지 않는 것으로 끝난다.**

- **엠블럼·킷·리그 로고·구장 비주얼은 어떤 형태로도 저장소에 들어오지 않는다.**
  실물을 참조한 재현도 포함이다. 화면이 클럽을 그려야 하면 **클럽 정체성에서
  절차 생성한 문장(紋章)**을 쓴다 ([team.md](team.md) §3.1) — 색과 도형을 클럽
  id에서 결정적으로 뽑으므로 자산 파일이 필요 없고, 실물과 닮을 이유도 없다.
- **실존 인물 명부는 표를 비우는 것으로 끝난다.** `data/world-figures.ts`와
  `data/coach-seeds.ts`는 비면 코드가 `inventPersonName`의 가상 이름으로 돌아가도록
  이미 짜여 있다 — 청산에 코드 변경이 필요 없다는 것이 이 두 표의 설계다.

### 7.4 ⚠️ 전환 시점은 기술 결정이 아니다

**장치가 서 있는 것과 실행하는 것은 다른 일이다.** EPL 704명을 비롯한 5대 리그
2,797명의 실명은 지금 이 세계의 정체성이고, 가명 전환은 **유저가 아는 세계를
통째로 바꾸는 일**이다. 그래서 이 원장은 방침까지만 정하고, 실제 시드 교체는
**유저가 시점을 정하는 결정**으로 남긴다.

전환할 때 함께 움직여야 하는 것:

- **한 번에 간다.** 이름만·능력치만 바꾸면 식별 가능성은 그대로인데 세계만 어색해진다.
- **밸런스를 다시 잰다.** 능력치가 모델 산출로 바뀌면 이적 시세·주급·경기 결과가
  전부 따라 움직인다 — `pnpm balance`가 그 자리다.
- **세이브 호환은 깨진다.** 표시 이름이 세이브에 복사돼 있으므로 기존 세이브의
  선수는 옛 이름을 그대로 들고 있다. 새 게임부터 적용할지, 마이그레이션으로 덮을지가
  전환 결정의 일부다.

### 7.5 원장

각 줄의 갈래는 §7.1의 셋 중 하나다.

- [ ] **EA/SoFIFA 능력치** _(생성)_ → `world/synthesis.ts`가 자리·체급·깊이·나이에서
      낸다. 장치는 섰고 남은 것은 시드 교체다 ([player.md](player.md) §13)
- [ ] **실명 선수 (이름·초상권/NIL)** _(가명화)_ → 가명 전환 + 초상 미사용 + 실명은
      사용자 모드로 분리. ⚠️ Keller 판례상 가명이어도 식별되면 위험 → 텍스트/스탯
      중심 설계로 회피. ⚠️ LLM이 실명 선수에게 **가상 성격·대사를 입히는 것**
      (페르소나)은 리스크를 더 키운다 — 부정적 전개는 장부의 사실에서만 나온다(people.md §2)
- [ ] **실명 수석코치 (코칭스태프 NIL)** _(미탑재)_ → `data/coach-seeds.ts`. 선수와
      같은 NIL 대상이고, 여기에 **게임이 지어낸 성격·동기·말투를 입히므로** 위 항목의
      리스크가 그대로 적용된다. 실존 인물 페르소나엔 `real` 표식이 붙는다 — 이 장부가
      세는 표식이고 프롬프트에는 실리지 않는다(docs/llm/prompts.md §5-3). 부정적
      전개는 장부의 사실에서만 나온다(people.md §2). 청산은 표를 비우기만 하면 된다 — **비면 자동으로 국적 기반
      가상 이름으로 대체**되므로 코드 변경이 필요 없다. **지금 33팀**(EPL 13·라리가 2·
      세리에A 13·분데스 2·리그앙 3)이고 나머지 63팀은 가상이다. 수집 경로는
      **각국 위키의 raw 위키텍스트**다(`?action=raw` — 렌더된 문서는 본문이 잘려
      감독만 보인다): 세리에A·리그앙·라리가는 시즌 문서, **EPL·분데스는 네덜란드어
      위키의 클럽 문서 `Staf` 표**. 영어 위키엔 코칭스태프 절이 아예 없다.
      ⚠️ `Staf` 표는 갱신일이 제각각이라 **감독 교체가 확인된 팀은 넣지 않는다** —
      낡은 스태프 이름은 곧 틀린 실명이다
- [ ] **실명 구단주 (`data/owner-seeds.ts`)** _(미탑재)_ → 수석코치 표와 같은
      구조다 — 이름만 실명이고 성격·동기는 원형 추첨이 낸 것이며, 여기에도 지어낸
      대사가 붙는다. **지금 17팀**이고 나머지는 리그 국적에 맞는 가상 이름이 선다.
      청산은 같다: **표를 비우면 코드가 가상 이름으로 돌아간다.**
      ⚠️ 구단주는 한 사람이 아닌 경우가 많아(컨소시엄·국부펀드·지주회사) 표가
      **감독이 실제로 마주 앉는 얼굴**을 적는다 — 그만큼 특정 개인을 또렷하게
      가리킨다
- [ ] **세계 인물 명부 (구단 밖 실명 — 감독·에이전트·해설)** _(미탑재)_ →
      `data/world-figures.ts`. 위 두 항목 중에서도 **가장 위험한 자리**다: 코치·
      구단주 시드는 이름만 실명이고 성격은 원형 추첨이 낸 것이지만, 이 명부는
      원형으로 뽑을 수 없는 사람들이라 **성격·동기·말투·예시 대사를 표가 직접
      적는다**. 즉 지어낸 인격이 실존 인물에게 통째로 붙는다. 여기 오르는 이름들은
      초상권이 가장 강하게 관리되는 축이기도 하다.
      전원 `real` 표식이다 — 카드에 가드 문장은 없고(docs/llm/prompts.md §5-3), 표가
      적는 원형이 직무 안의 사람을 그린다. 담는 기준은 셋이고
      첫째가 절대 조건이다: **살아 있다 · 지금 축구계에 있다 · 감독이 마주칠 자리에
      있다**(people.md §2-1).
      **지금 23명**(현직 감독 16 · 에이전트 3 · 해설 4)이고, 구단 임원은 열지
      않았다 — 이름값에 비해 부채만 는다.
      청산은 **표를 비우는 것으로 끝난다**: 명부가 비면 타 팀 감독은 다시
      `inventPersonName`의 가상 이름으로 돌아가고, 에이전트·해설은 세계에서
      사라진다. 코드 변경이 필요 없다.
      ⚠️ **표는 시간이 지나면 낡는다** — 은퇴·이적·사망. §4.1의 시드 갱신과 같은
      주기로 함께 본다
- [ ] **키·체중** _(생성)_ → 5대 리그 시드 2,797명 중 **2,357명에 키가 있고**(대부분
      EA FC 27 등재값, 일부는 위키 확인값), 나머지 440명은 자리별 기준치 + 능력치로
      파생한다(`catalog.ts` `physiqueOf`). EA IP 부채는 능력치와 함께 남는다.
      **시드 값을 지우면 파생이 대신 답하므로 청산에 새 장치가 필요 없다.**
      안 채워진 440명은 **대부분 EA에 없는 아카데미 자원**이다 — EA가 구단당 25명
      안팎만 싣기 때문이고, 조사 부족이 아니라 소스의 한계다(21세 이하가 246명).
      ⚠️ **출처가 단일하지 않다** — EA 등재값이 실물과 어긋나는 사례가 확인돼
      위키 확인값으로 덮은 선수가 있다(§4.1)
- [x] **주발 (`foot`)** → 5대 리그 시드 2,797명 중 **2,421명(87%)이 실측**
      (EA FC 27 공개 `Preferred Foot`·`Weak Foot` 별점 →
      `RealPlayerSeed.foot`/`weakFoot`). 클럽 페이지가 아니라 **선수 페이지의
      JSON-LD**를 읽으므로 슬러그 추측이 필요 없고, 생년월일이 함께 와서 동명이인이
      갈린다(§4.1).
      나머지 376명(주로 EA에 없는 아카데미 자원)은 **더 잘 쓰는 발 5 · 반대 4**로
      두고 좌우만 자리로 추정한다.
      ⚠️ **모르는 실존 선수의 약발은 지어내지 않는다** — 해시로 5\~1을 뿌리면 실제로는
      멀쩡한 선수가 우연히 2로 떨어진다.
      절차 생성 선수는 대조할 실물이 없으므로 실측 분포를 표집한다(`syntheticFoot`).
      실명 선수의 좌우 추정은 여전히 부채이므로 가명 전환과 함께 정리한다
- [ ] **등번호 (`squadNumber`)** _(가명화)_ → 실선수 카탈로그 2,797명 중 **2,709명**에
      값이 있고, 그 기준은 **위키 구단 문서의 현 스쿼드 표**(`{{Fs player}}`)다.
      표가 번호를 준 선수는 전원 그 번호를 그대로 받는다. 표가 번호를 비운 1군 행은
      옛 시드 번호를 유지하되 팀 안에서 겹치면 **표 쪽을 남기고 밀린 쪽을 비운다**.
      **표가 스스로 같은 번호를 두 번 주는 일도 있다**(랑스 #31 두 명) — 표가 자기를
      가르지 못하므로 표 순서의 앞 행이 남고 뒤 행이 비워진다.
      비어도 게임 생성 시 포지션 관례와 팀의 빈 번호로 결정적 배정되므로 클럽 명단
      UI에 빈 번호는 없다. **가명 전환 때 번호는 비운다** — 이름을 바꿔도 번호가
      남으면 그 선수를 그대로 지목한다(§7.3 Keller)
- [ ] **부상 이력 (`data/injury-history.ts`)** _(4에 딸린다)_ → 부임 직전 2시즌
      (2024-07\~2026-06)의 실제 결장 기록이 **14명분만** 들어 있다(Transfermarkt
      선수별 injury history · 일부 Soccerway). 새 게임이 시작될 때 `INJURY` 행으로
      펼쳐지고 초기 부상 성향(`injuryProneness`)을 정한다. **표의 키는
      `wikidataId`다** — 시드에 동명이인이 네 쌍 있어(§4.1.1) 이름으로 이으면 이력이
      남에게 붙는다.
      담는 기준은 좁다 — **부상만**(질병·휴식·컨디션 난조는 결장이지 부상이 아니고,
      출전 정지는 더더욱 아니다), **부위가 확인된 것만**(`Knock`·`Injury`처럼
      뭉뚱그린 행은 버린다), **일수가 아니라 날짜**를 옮긴다(둘 다 적으면 포함
      계산에서 하루씩 어긋난다).
      ⚠️ 표에 없는 선수는 "이력이 없다"가 아니라 **"조사하지 않았다"**이고 성향
      1.0(평균)에서 출발한다 — 주발과 같은 원칙이다. 703명을 추정으로 채우면
      멀쩡한 선수가 우연히 유리몸이 된다.
      ⚠️ **창이 2시즌이라 그보다 오래된 평판은 안 잡힌다** — 리스 제임스는 2020년
      이후 햄스트링만 열 번이지만 이 창 안에서는 145일이라 사카(182일)보다 낮게
      나온다. 창을 넓히려면 그만큼 옛 기록을 더 조사해야 한다.
      **가명 전환과 함께 표가 통째로 사라진다** — 가리킬 실존 선수가 없어지기 때문이다
- [ ] **사우디·MLS 레전드 시드** _(가명화 + 생성)_ → 5대 리그 시드와 같은 실명·능력치
      부채를 진다 (`data/market-leagues.ts`). 명단은 §4.1과 같은 경로를 지난다 —
      위키 구단 문서의 현 스쿼드에 실제로 있는 50명만 남기고 능력치·신체는 EA FC 27에서
      가져온다. 경기를 하지 않는 클럽이라 스쿼드 전체가 아니라 "감독이 데려올 만한
      이름"만 담고, 나머지는 절차 생성이다
- [ ] **홈그로운 자격 (등록 명단용)** _(4에 딸린다)_ → 지금은 **결정적 대체 규칙**이다. 실제
      판정 근거는 "만 21세 이전에 그 협회 클럽에서 3시즌 등록"이라는 경력 이력인데
      시드에 그 이력이 없어, id 해시로 나라별 목표 비율(1부 40% · 2부 75%)만 맞춘다
      (`catalog.ts` `deriveHomegrownCountry`). 실명 선수에게 사실과 다른 자격이
      붙을 수 있다 — **가명 전환이 이 부채를 없앤다**: 가리킬 실물이 사라지면 대체
      규칙이 곧 사실이 된다
- [ ] **Transfermarkt 시장가치·이적** _(생성)_ → 자체 추정 모델로 교체 (약관상 AI
      학습도 금지). 시장가는 이미 코어가 능력치·나이·계약에서 낸다(`marketValueOf`) —
      **시드에 시장가 필드가 없다**. 남은 부채는 그 모델을 조율할 때 참고한 눈금뿐이다
- [ ] **클럽 엠블럼·킷·리그 로고·스타디움 비주얼** _(미탑재)_ → **담지 않는다.**
      미술저작물+상표 이중보호라 이 원장에서 가장 위험하고, 동시에 갚는 값이 0인
      자리다. 화면이 클럽을 그려야 하면 클럽 id에서 결정적으로 뽑은 절차 생성 문장을
      쓴다 ([team.md](team.md) §3.1)
- [ ] **구장명·수용인원 (96팀)** _(가명화)_ → 재정 모델의 매치데이 수입 입력
      (`packages/engine/src/data/club-profile.ts`, ../simulation/finance.md §4.6).
      수용인원은 **위키 구단 인포박스의 `capacity`와 전수 대조**한 값이다.
      **시설명만 상표 대상이고 수용인원은 사실 정보라 위험이 낮다** — 그래서 가명화는
      이름만 갈고 숫자는 그대로 둔다(§7.3). 클럽명과 **한 묶음으로** 처리한다.
      ⚠️ 재건축·개보수로 홈이 바뀌는 클럽이 있다(베티스는 비야마린 공사로
      2025-26·2026-27 두 시즌을 라 카르투하에서 쓴다) — 구장을 갱신할 때는 이름과
      수용인원을 **함께** 봐야 한다
- [ ] **리그/클럽명** _(가명화)_ → 가명화(FM의 'Man UFC' 방식) 또는 트랙션 후 리그
      단위 라이선스 협상 (FIFPRO·리그). 가명화 쪽을 기본으로 두고 파이프라인이 이미
      셋(선수·클럽·구장)을 함께 간다 — 리그명은 클럽·구장과 한 묶음이다(§7.3)
- [ ] **선수 주급 (EPL 703명 중 667명)** _(생성)_ → 공개 급여 집계 사이트
      (salarysport)의 추정치가 시드에 들어 있다(`RealPlayerSeed.weeklyWage`). 계약
      실액이 아니라 언론 보도 기반 추정이고 사이트 약관도 재배포를 허용하지 않는다 →
      **재정 모델을 흔들지 않는 범위의 자체 추정치로 교체**한다. 코어에 이미
      `wageExpectationOf`가 있으므로 **필드를 지우면 그쪽이 답한다** — 값은 밸런스
      재검증뿐이다. 지금은 "실제에 가까운 분포"를 얻는 데 의미가 있어 그대로 쓴다

> ⚖️ 위 분석은 일반 정보이며 법적 조언이 아니다. 정식 출시 전 관할권별
> (한국·미국·EU) 변호사 검토 필요.

---

## 8. 자주 틀리는 소스 정보

- **FBref** — 2026-01 Opta 계약 종료로 xG 등 고급스탯 전부 삭제됨. 현재 기본 스탯만. 고급스탯 의존 설계 불가.
- **StatsBomb Open Data** — "무료=자유"는 오해. 약관 1.2.2조가 상업적 이용을 명시 금지(비상업 R&D 한정).
- **Kaggle EA FC의 CC0 태그** — 업로더 기여분에만 적용. 원천 EA IP는 별개라 "CC0니까 안전"은 틀림.
- **Entity Sport 가격** — $250\~750/월. 요청한도는 1M\~3M/월.
- **API-Football "상업 사용 허용"** — 과장. 약관상 데이터 상업 권리 미부여, 제3자 IP·재배포는 사용자 책임.
- **FM DB 470k/371속성** — 비공식 FUTEK 추출본 수치. Kaggle `ajinkyablaze` 링크는 실제로 FM2017·15만 명.

---

## 부록 — 소스 레퍼런스

| 소스                           | URL                                                                                     | 비고                                  |
| ------------------------------ | --------------------------------------------------------------------------------------- | ------------------------------------- |
| OpenFootball                   | https://github.com/openfootball                                                         | CC0, 활성                             |
| Wikidata                       | https://query.wikidata.org                                                              | CC0, SPARQL                           |
| API-Football                   | https://www.api-football.com                                                            | self-serve 유료                       |
| Sportmonks                     | https://www.sportmonks.com/football-api                                                 | 무료티어+유료                         |
| Football-Data.co.uk            | https://www.football-data.co.uk/data.php                                                | 무료 CSV                              |
| **fcratings (EA FC 27)**       | https://www.fcratings.com                                                               | 🔴 EA IP · 현 능력치·신체 시드의 원본 |
| **위키백과 구단 문서**         | `en.wikipedia.org/wiki/{클럽}?action=raw`                                               | CC BY-SA · 현 명단·등번호·구장의 원본 |
| Kaggle EA FC (stefanoleone992) | https://www.kaggle.com/datasets/stefanoleone992/ea-sports-fc-24-complete-player-dataset | 🔴 EA IP                              |
| Kaggle TM (player-scores)      | https://www.kaggle.com/datasets/davidcariboo/player-scores                              | 🔴 TM 약관                            |
| FIFPRO (라이선스)              | https://www.fifpro.org/en/who-we-are/commercial                                         | 선수 NIL 집단 라이선스                |
