# story-fm

> LLM을 코어 엔진으로 삼는 차세대 풋볼 매니저 게임.
> 플레이어는 **AI 감독**이 되어 자연어로 팀을 지휘하고, 경기는 LLM 에이전트가
> 시뮬레이션하며, 모든 순간은 AI가 생성하는 **내러티브**로 살아 움직인다.

이 문서는 프로젝트의 **기획 비전**과 **개발 규약**을 함께 담는 단일 소스(single
source of truth)다. AI 에이전트와 사람 기여자 모두 이 문서를 기준으로 작업한다.
`CLAUDE.md`는 이 문서를 참조한다.

---

## 1. 비전 (Vision)

전통적인 풋볼 매니저는 숫자 슬라이더와 확률 테이블의 게임이었다. story-fm은
그것을 **언어의 게임**으로 바꾼다.

- **자연어 감독**: 플레이어는 슬라이더 대신 말로 지시한다. "후반엔 측면을 더
  적극적으로 쓰고, 9번한테 내려와서 연결하라고 해." → AI가 전술로 해석·실행.
- **살아있는 시뮬레이션**: 경기·선수·구단주·기자·팬이 모두 고유한 성격과 동기를
  가진 LLM 에이전트. 같은 라인업도 맥락에 따라 다르게 반응한다.
- **내러티브 우선**: 결과(스코어)보다 **이야기**가 핵심 재미. 라커룸 갈등,
  유망주의 성장 서사, 라이벌과의 악연 — 매 시즌이 하나의 드라마.

### 디자인 원칙

1. **언어가 인터페이스다** — 핵심 상호작용은 자연어. UI는 그것을 보조한다.
2. **납득 가능한 결과** — AI의 판정/서사는 항상 "왜 그렇게 됐는지" 설명 가능해야
   한다. 블랙박스 난수는 지양.
3. **창발적 전개, 정확한 장부** — 경기와 서사는 LLM이 창발적으로 만들되,
   상태·규칙·수치 반영은 코어가 결정적으로 지킨다. (→ 6장 LLM 규약)
4. **품질이 우선, 비용은 그다음** — LLM 호출은 낭비 없이 설계하되(캐싱·
   컨텍스트 위생), 비용 절감이 경험 품질을 깎게 하지 않는다. 역할에 맞는
   모델을 티어로 나눠 쓴다.

---

## 2. 핵심 시스템 (정체성)

> 🚧 **미정 — 기획 중.** 핵심 시스템은 전면 변경될 수 있어 아직 확정하지 않는다.
> 1장 비전("LLM 시뮬레이션 · AI 중계 · AI 감독")이 현재의 방향성이며, 구체적인
> 시스템 정의는 `docs/design/`에서 다듬은 뒤 확정되면 이 섹션에 옮긴다.

---

## 3. 기술 스택

- **언어**: TypeScript (풀스택, strict 모드)
- **런타임/프레임워크**: Next.js (App Router) + Node.js
- **패키지 매니저**: pnpm (모노레포 워크스페이스 권장)
- **LLM**: 역할별 멀티 프로바이더 — 현재 실행 어댑터는 Anthropic Claude와
  Google Gemini이며 코드 기본값은 전 티어 Claude(Opus). 경기 진행의 장기 후보는
  DeepSeek 계열이다. 티어·모델 ID는 `packages/llm`에서 단일 관리
  (→ docs/design/economy.md)
- **상태/검증**: Zod로 LLM 입출력 스키마 검증 (구조화 출력 강제)
- **테스트**: Vitest
- **린트/포맷**: ESLint + Prettier

> 스택 세부는 초기 단계라 유동적이다. 변경 시 이 문서를 먼저 갱신한다.

### 권장 디렉터리 구조 (모노레포)

```
docs/
  design/         # 기획서(GDD) — 게임 컨셉·시스템·밸런스 문서. 단일 소스.
  decisions/      # ADR — 중요한 설계/기술 결정 기록
apps/
  web/            # Next.js 프론트엔드 + API 라우트
packages/
  sim/            # 경기 시뮬레이션 엔진 (LLM 비의존 코어 + 에이전트 어댑터)
  agents/         # LLM 에이전트 정의 (선수/감독/중계/NPC 페르소나)
  narrative/      # 이벤트 스트림 → 내러티브 생성
  domain/         # 도메인 모델 (선수, 팀, 전술, 경기) + Zod 스키마
  llm/            # LLM 클라이언트 래퍼, 프롬프트, 토큰 예산, 캐싱
```

> **기획서는 `docs/design/`에서 관리한다.** 게임 컨셉·핵심 시스템·밸런스 등
> "무엇을 만들 것인가"는 모두 여기에 쌓는다. 확정된 내용만 AGENTS.md에 요약·반영.

---

## 4. 아키텍처 방향

- **코어 = 판정과 장부, LLM = 이야기**: 상태 기록·규칙 검증·수치 반영(시간 진행,
  스탯, 성장, 재정)은 **결정적 순수 함수**로 두어 LLM 없이 테스트 가능하게 한다.
  **경기 결과도 코어가 정한다** — 기대 득점(xg)에서 구간 시뮬레이터가 사건을
  굴려 확정하고, LLM은 그것을 중계·연출·대화로 옮긴다. 감독의 지시는 **전력 분석
  패킷을 통해서만** 결과에 닿으며, 이득은 소화율만큼만 반영되고 대가는 온전히
  치른다 — "말했더니 그대로 됐다"는 경로를 남기지 않는다
  (→ docs/design/match-sim.md).
- **구조화 출력 우선**: LLM에 자유 텍스트를 파싱시키지 말고, Zod 스키마로
  도구 호출/JSON을 강제한다. 자유 텍스트는 내러티브 등 "사람이 읽는" 출력에만.
- **이벤트 소싱**: 경기는 이벤트 로그로 표현한다. 내러티브·리플레이·디버깅이
  모두 이 로그에서 파생된다.
- **페르소나 = 데이터**: 에이전트 성격/기억은 코드가 아니라 데이터(프롬프트와
  상태)로 다룬다.

---

## 5. 개발 규약

### 코드 스타일

- TypeScript `strict: true`. `any` 금지(불가피하면 `unknown` + 좁히기).
- 함수형·불변 우선. 부수효과는 경계(API/IO)에 격리.
- 도메인 타입은 `packages/domain`에 모으고, 다른 패키지는 이를 import한다.
- 파일·디렉터리는 kebab-case, 타입·컴포넌트는 PascalCase, 변수/함수는 camelCase.

### 커밋 / PR

- Conventional Commits (`feat:`, `fix:`, `refactor:`, `docs:`, `test:`, `chore:`).
- 한 PR은 한 가지 관심사. LLM 프롬프트 변경은 별도 커밋으로 분리해 추적 가능하게.
- 커밋/푸시는 사용자가 요청할 때만. `main` 직접 커밋 금지 — 브랜치에서 작업.

### 테스트

- 시뮬레이션 코어는 **LLM 없이** 단위 테스트(시드 고정, 결정적).
- LLM 의존 로직은 응답을 모킹하거나 스키마 검증 수준에서 테스트.
- 새 기능엔 테스트 동반. 실패하는 테스트는 숨기지 말고 그대로 보고.

### 문서

- **기획서는 `docs/design/`** 에서 관리한다. 게임 컨셉·시스템·밸런스의 단일 소스.
- 기획·아키텍처 결정이 바뀌면 **코드보다 문서를 먼저** 갱신한다.
- 중요한 설계 결정은 `docs/decisions/`에 ADR로 남긴다.
- 확정된 핵심 시스템만 AGENTS.md 2장에 요약해 옮긴다.

---

## 6. LLM 통합 규약 (가장 중요)

이 게임의 코어가 LLM이므로, LLM 사용 규칙을 엄격히 지킨다.

1. **모델 ID는 하드코딩하지 말고** `packages/llm`의 설정에서 단일 관리한다.
   역할별 티어로 라우팅한다 — GM 서사·판정은 Claude(Opus), 경기 진행은
   DeepSeek 계열. 프로바이더 차이는 어댑터가 흡수한다 (→ docs/design/economy.md).
2. **구조화 출력은 Zod 스키마로 검증**하고 실패 시 재시도. 파싱 실패를 그대로
   게임 상태에 반영하지 않는다.
3. **컨텍스트 위생**: 모든 LLM 호출은 입력 컨텍스트 구성(캐시 프리픽스
   안정성)과 출력 분량을 의식해 설계한다. 단 비용 최적화는 품질보다 후순위 —
   비용 상한이 게임 진행이나 연출을 깎지 않는다.
4. **결정성 경계**: 장부(상태 전이·수치 공식·검증)는 결정적으로, 전개(경기·
   서사)는 창발적으로. 창발 출력이 검증을 거치지 않고 게임 상태가 되는 일은
   없다. 밸런스는 시드 재현 대신 분포 모니터링으로 검증한다.
5. **프롬프트는 코드처럼 관리**: 프롬프트는 버전·diff가 추적되도록 별도 파일/
   상수로 두고, 매직 스트링을 코드 곳곳에 흩지 않는다.
   ⚠️ **프롬프트 수정은 지우는 방향이 기본이다.** 문제가 보이면 지시를 덧붙이지
   말고 **그 행동을 시키고 있는 기존 문구를 먼저 지워라.** 규칙만 남기고 이유·
   비유·수사·중복 예시 같은 부차적 설명은 넣지 않는다 — 모델은 설명을 읽고
   설명대로 쓴다. 체크리스트를 주면 항목 수만큼 문단을 만들고("최소 두 가지를
   짚어라" → 매번 같은 골격의 브리핑), 어떤 도구를 막으면 대신 허락을 구한다
   ("advance_time을 부르지 마라" → "날짜를 넘길까요?"). 새 지시로 증상을 덮으면
   원인은 그대로 남고 프롬프트만 길어진다.
6. **캐싱**: 동일 입력의 LLM 호출은 캐싱한다(프롬프트 캐시 포함). 비용·지연
   둘 다를 위해.
7. **안전장치**: LLM 출력이 게임 규칙을 깨지 않도록(불가능한 전술, 존재하지 않는
   선수 등) 검증 레이어를 둔다.

> Claude API 사용 세부(모델 ID, 가격, 캐싱, 도구 사용)는 추정하지 말고
> 항상 최신 레퍼런스를 확인한다. (Claude Code에선 `claude-api` 스킬 참고)

---

## 7. AI 에이전트 작업 가이드

이 저장소에서 작업하는 AI 에이전트(Claude Code 등)를 위한 지침:

- **작업 전**: 이 문서로 비전·규약을 먼저 파악한다. 관련 패키지의 기존 패턴을
  읽고 그 스타일을 따른다.
- **작업 중**: 코어(결정적)와 LLM(비결정적)의 경계를 흐리지 않는다. LLM을
  새로 호출하는 코드를 추가할 땐 6장 규약(스키마 검증·토큰 예산·캐싱)을 반드시
  적용한다.
- **불확실하면 질문한다**: 기획 방향이 모호하면 임의로 정하지 말고 확인한다.
  특히 게임 밸런스·핵심 루프에 영향을 주는 결정.
- **범위를 지킨다**: 요청받은 것만. 큰 리팩터링·새 의존성 추가는 먼저 제안.
- **결과를 정직하게 보고한다**: 테스트가 실패하면 출력과 함께 그대로 알린다.

---

## 현재 상태

🚧 **플레이 가능한 프로토타입 (데이터 모델 v6).** 게임은 **7월 1일 = 여름
이적시장 개장**과 함께 시작해 프리시즌(이적·훈련)을 보내고 8월 중순 개막전에
들어간다. 온보딩 → 채팅 지시(훈련·전술·라인업·면담·팀토크) → 경기(전력 패킷→
진행→장부 검증→반영) → 시즌 전환(은퇴·유스·커리어) → 멀티시즌이 엔드투엔드로 돈다.

### 데이터 모델 (v6) — 2-레이어 정규화

**카탈로그(불변 초기치)와 게임 세이브(가변 상태)를 분리한다.**

- `PLAYER_CATALOG` / `TEAM_CATALOG` — 모든 게임이 공유하는 초기치. 새 게임
  시작 시에만 읽고, `GAME_PLAYER`/`GAME_TEAM`으로 인스턴스화된다
  (`catalogId`로 출처 링크, 유스 등 생성 선수는 null).
- **선수** — `GAME_PLAYER`는 **능력치 15축**(신체 4 · 기술 5 · 정신 5 · GK 1 —
  전 선수가 15축 전부 보유, 예외 분기 없음)·컨디션·주장 여부를 갖는다.
  축의 의미는 **포지션 가중치**(`POSITION_WEIGHTS`)가 정한다 —
  `overall`·자리 적합도(`roleFit`)·시뮬 존 점수가 모두 이 테이블 하나에서 나온다.
  값은 **단계가 아니라 소수(0.05~3.0, 0.05 해상도)** 다. **3.0은 자리마다 하나**(그
  자리를 정의하는 축)이고, `roleFit`이 가중 평균이라 **합은 의미가 없다** —
  자리 안의 상대 비율만 결과에 닿고 자리·역할 간 수준은 `ROLE_PIVOT`이 흡수한다.
  ⚠️ 무게는 "이 자리에 중요한가"만으로 정하지 않는다 — **그 축이 자리를 실제로
  가르는가**를 함께 본다. 안 그러면 표가 평평해져 같은 선수의 자리 간 폭이
  중앙값 6점밖에 안 나온다(홀란이 센터백 85였다). 특히 `positioning`은 파생 축이라
  자리 간 σ가 2.5로 꼴찌인데 CB·ST에서 최고 무게를 받고 있었다 — 자리를 못 가르는
  축에 3.0을 주면 그 자리 점수가 "대체로 얼마나 좋은가"로 수렴한다. 지금은 폭이
  **중앙값 10 · p90 18**이고, EA 대조도 함께 좋아졌다(σ 3.05 · ≤2 72%).
  **같은 자리도 세부 역할로 갈린다** — 이름은 짓지 않고 **풋볼 매니저의 역할 체계**를
  그대로 쓴다(50종: 커버·스토퍼·리베로·인버티드 윙백·레지스타·라움도이터·폴스 나인 …).
  감독이 이미 아는 말이라 설명 없이 전달되기 때문이다. FM의 **듀티**는 두지 않는다 —
  역할과 곱하면 백 가지가 넘고, 그 축은 전술 6축이 이미 갖고 있다. 다만 듀티가 담던
  것 중 **요구 역량이 실제로 갈리는 짝은 역할로 되살린다**: 센터백의 `커버`(라인 뒤를
  덮는다)와 `스토퍼`(앞으로 나가 끊는다). 자리 기본값의 스피드가 낮은 건 판별력이
  없어서지 "느린 센터백이 공짜"라서가 아니고, 그 대가는 **라인을 올릴 때** 역할이
  받는다 — 스피드 가중치가 **커버 2.5 > 스토퍼 1.1 > 기본 0.65**다. 스토퍼도
  기본값보다 높다: 나갔다가 등 뒤로 털리는 게 그 역할의 고유한 실패 방식이라
  나가는 첫 발이 필요하다. 역할은 전체 표가
  아니라 **기본값에서의 차이**로 정의해 "인버티드 풀백도 여전히 풀백"이 지켜진다.
  ⚠️ **역할마다 기준점을 따로 둔다** — 없으면 역할 선택이 곧 공짜 능력치가 된다
  (높은 축만 얹는 컴플리트 포워드는 누구를 넣어도 오른다). 기준점을 두면 평균적인
  선수는 어느 역할에서도 69이고, 갈리는 건 "이 선수가 그 역할에 맞느냐"뿐이다.
  **표시용 종합(`overall`)은 역할을 타지 않는다** — 가장 잘 맞는 자리의 기본 역할
  기준(`bestOverall`)이고, 실제로 맡은 자리·역할의 전력은 `roleFit`이 따로 낸다.
  **중앙 라인도 좌우가 갈린다** — LCB/RCB뿐 아니라 LDM/RDM · LAM/RAM · LF/RF ·
  LST/RST까지. 요구 역량과 적응도는 **좌·중·우가 같고**(부르는 이름만 다른 같은
  자리라 묶음 감점이 없다 — **선호 여부도 좌우를 함께 본다**: `isNaturalAt`),
  갈리는 건 **주발**(`GAME_PLAYER.foot` — 편한 쪽 +1 ·
  반대쪽 −2)뿐이다. 주발·약발은 **5대 리그 2,334명(78%)이 실측**(EA FC 27
  `Preferred Foot`·`Weak Foot` — EPL 650 · 유럽 4대 리그 1,669). 조사가 닿지 않은
  **실존 선수는 5/4로 두고 지어내지 않는다** — 해시로 흩으면 실제로는 멀쩡한 선수가
  우연히 나빠진다(브루노 페르난데스·틸레망스가 그랬다). 반대로 **절차 생성 선수는
  실측 분포를 표집한다**(`syntheticFoot` — 대조할 실물이 없으므로). 실측은 3성이
  60%로 최빈값이고, 이걸 4로 접었더니 카탈로그의 90%가 4가 되어 좌우 배치 판단이
  밋밋해진 적이 있다.
  왼발 센터백은 LCB가 CB·RCB보다 조금 편하다
  (attribute-model.md §좌우 분화).
  **골키퍼는 골키핑이 지분 65%** 다 — 39%였을 때 골키핑 79인 선수의 종합이 82로
  나왔다(나머지 61%가 밀어 올렸다). 지금은 종합−골키핑이 85%가 ±2 안이고 EA 일치도
  95%다. 배급·커맨드는 값을 움직이되 뒤집지는 못한다.
  **표의 하한은 0이 아니라 0.05** (`FLOOR_WEIGHT`) — 스트라이커의 태클도 전방 압박·
  역습 지연으로 쓰인다. 0으로 두면 태클 30인 9번과 60인 9번이 같은 선수가 된다.
  0.5로 뒀던 때는 그게 바닥이 아니라 "보조의 절반"이라 GK 가중치 합의 17%가 무관한
  축이었다 — 자리의 색이 흐려졌다. 지금은 1% 남짓이다.
  ⚠️ **눈금이 다른 축도 바닥으로만 넣는다** — `leadership`은 평균 42인데 다른 축은
  66이라(대부분의 선수는 리더가 아니다), 예전처럼 GK·CB·DM·CM **네 자리에만**
  보조로 얹으면 그 자리가 통째로 −1.7 낮아진다. 문제는 축이 아니라 자리마다 다르게
  섞인 것이었다 — 아홉 자리에 똑같이 들어가면 자리별 기준점(`RAW_PIVOT`)이 흡수한다.
  리더십의 주 통로는 여전히 주장 지명·팀토크·라커룸이지 전력이 아니다.
  15축 중 8축은 시드 6축에서 만들어내는데(`deriveAxes`), 그 계수는 **EA 공개 등급과
  전수 대조해 맞췄다** — OVR(가중 합)뿐 아니라 **세부 스탯도 축별로** 맞춘다.
  대조 전엔 침착성이 평균 14 낮아 `overall`이 −2.5로 벌어져 있었고, 골키퍼의
  공중볼·힘은 상관 +0.07로 사실상 무작위였다(맛내기 값인 `physical`에서 파생하고
  있었다 — `goalkeeping`으로 바꿔 +0.41). **가중치가 0인 축은 OVR에 흔적을 안
  남기므로 합만 봐서는 못 잡는다.** 지금은 편차 +0.1 · |오차|≤2가 65%다
  (implementation-notes 참고).
  히든 능력치는 두지 않고 대신 **축별 관측 가능성**(관측형/분석형)으로 안개를
  만든다 — 스카우팅을 마쳐도 판단 계열(결정력·시야·위치선정·침착성·리더십)은
  오차가 남는다 (→ docs/design/attribute-model.md). 나이는 저장하지 않고
  `birthdate`에서 계산(`ageOf`). 부속 테이블: `PLAYER_POSITION`(가능 포지션 +
  포지션 적응도, `isNatural`이 주 포지션 — **하나 이상**, 두 자리를 다 자기 자리로
  삼는 선수가 있다. 화면 한 칸·포지션군처럼 하나만 쓸 수 있는 자리는
  `naturalPositionOf`가 적응도 높은 쪽을 대표로 고른다)·`INJURY`(부위·심각도·원인 이력,
  `returnedOn=null`이 현재 부상)·`BOOKING`/`SUSPENSION`(징계 이력)·
  `PLAYER_GROWTH`(훈련·경기 성장 로그)·`SEASON_STAT`(시즌×팀 — 출전·득점·도움·
  평점 합계). **폼은 −1~+1의 실수이고 시간 축을 갖는다** — 1이 곧 절정, −1이 곧
  바닥이다. 경기 평점(중립점 6.3)이 개인별로 올리고 내리며, 기복의 폭은
  `composure`가 정하고, **매일** 평균으로 조금씩 끌린다. 절정에 가까울수록 더
  오르기 어렵다. 예전엔 −3~3 정수에 승 +1·패 −1 누적이라 강팀은 시즌 내내
  상한에 못박혀 있었다 (attribute-model.md §4.1).
  **경기 평점**은 코어가 장부 사실(골·도움·카드·실점·승패)로 6.0
  기준선에서 앵커를 박고, 경기가 끝나면 **LLM이 사건 목록·출전 시간까지 읽어
  앵커 ±1.2 안에서 다시 매긴다**(근거 한 줄 포함 — 실패하면 앵커가 남는다).
  시즌 평점은 `ratingSum ÷ apps` 파생이다 (match-sim.md §6).
- **전술이 라인업의 원본** — `TACTICS`(팀별 현재 전술) → `TACTIC_ASSIGNMENT`
  (선수별 역할 starting/bench·전술 내 포지션·**전술 적응도**·개인 지시).
  팀 엔티티에 선발/벤치 배열은 없다. 배치 없는 선수 = 예비 스쿼드.
  **적응도는 선형으로 오르지 않는다** (`applyFamiliarityGain`) — 판정이 낸 값이
  현재 위치만큼 깎여 들어간다. 아래 구간은 판정 그대로라 한 달이면 80에 닿고,
  위로 갈수록 무뎌진다. 그 구간에서 갈리는 건 "전술을 이해했나"가 아니라
  "몸이 먼저 반응하나"라서다. **훈련과 경기가 다른 곡선을 탄다** — 훈련만으로도
  **95까지는 간다**(한 전술을 파고든 감독의 보상) 하지만 **100은 경기의 몫**이라
  한 전술로 시즌을 뛴 주전만 닿는다(벤치는 시즌 말 95 · 로테이션 99 · 주전 100).
  **적응도의 천장은 100이다** — 능력치의 99는 "세계 최고"라는 상대적 자리지만
  적응도의 100은 "이 전술이 몸에 완전히 붙었다"는 상태라 닿을 수 있어야 한다.
  **선수마다 속도가 다르다** (`tacticalUptake` — 시야·위치선정·침착성) — 판정은
  "그 훈련이 팀에 얼마를 남겼나"이고 그중 얼마를 가져가는지가 개인의 몫이다.
  흡수율은 **1을 넘지 않는다**(0.68~1.0): 넘으면 판정 상한(훈련 3 · 경기 8)이
  뚫려 "한 번에 게임을 크게 흔들 수 없다"는 계약이 흐려진다. 잘 읽는 선수가
  같은 훈련에서 1.5배를 가져가고, 그래서 80까지 훨씬 빨리 붙는다. 하락 폭
  (`shiftFactor`)이 쓰던 것과 **같은 함수**다 — 빨리 익히면서 잘 잊으면 앞뒤가 안 맞는다.
  ⚠️ 그래서 `familiarity`는 **소수**다(`FamiliaritySchema` — 옛 세이브의 정수도
  그대로 통과해 버전을 올리지 않는다): 정수로 자르면 85 위에서 판정 +3이 0이 되어
  값이 멎는다. 화면은 반올림해 보여 주고, 성장 로그도 **눈금이 실제로 넘어갔을 때만**
  남긴다 — 87.4 → 87.7은 감독의 화면에서 아무 일도 아니다. **내려가는 건 깎지
  않는다**: 전술을 갈아엎은 대가는 지금 잘 아는 팀일수록 크다.
  ⚠️ 판정 프롬프트에는 이 곡선을 **적지 않는다** — 모델이 "높으니 조금만"이라고
  또 깎으면 이중으로 줄어든다. 모델은 얼마나 스몄는지만 말하고, 얼마가 남는지는
  코어가 정한다.
- **시작 전술은 구단마다 다르다** — `DEFAULT_XI`(EPL 20팀)가 선발 11인을 정하고,
  모양은 **그 11명의 포지션별 전력으로 고른다**(`pickFormation`). 카탈로그의
  리서치 값(`TeamCatalogEntry.formation`, 1부 96팀)은 **선입견이지 결론이 아니다**
  — 5개 프리셋을 다 채워 보고 다른 모양이 뚜렷하게 셀 때만(`PRIOR_EDGE`) 뒤집힌다.
  채점은 시뮬의 존 기여와 같은 잣대(`roleFit` × 적응도)이고, **채점 전에 자리를
  제대로 배치한다** — 그냥 `roleFit` 그리디로 채우면 라이스가 라이트백에 서는
  라인업이 나오고 그 합으로 모양을 고르게 된다(96팀 중 40팀이 5-4-1이 됐다).
  프리셋이 5종뿐이라 실제 백3 계열(3-4-2-1·3-4-3·5-3-2)은 `3-5-2`로 접힌다.
  **XI는 순서가 아니라 구성**이다 — 어느 슬롯에 서는지는 엔진이 적응도로 정하고,
  **포지션군이 맞는 자리에만** 가산이 붙는다(4백 명단을 3-5-2에 넣어도 스트라이커가
  윙백에 서지 않는다). 슬롯을 앞에서부터 채우면 그 자리를 볼 줄 아는 유일한 선수가
  앞 슬롯에 끌려가므로, 배치 뒤 **짝 교환 패스**로 합이 오르면 자리를 맞바꾼다.
  **본업 가산이 따로 있다**(`NATURAL_SLOT_BONUS` 30) — 포지션군만으로는 4-2-3-1의
  볼란치와 10번을 못 가른다(둘 다 미드필더군이라 가산이 상쇄된다). 그래서 브루누
  페르난데스(주 AM)가 수비형 미드필더에 섰다: 적응도는 **파생값이 보유값을 넘을 수
  있어**(마이누의 CM 95에서 파생된 CAM 89 > 페르난데스가 보유한 CAM 88) 눈금이
  흔들리는데, "그 선수의 본업인가"는 그와 무관한 사실이다. 단 **그 자리를 볼 수는
  있을 때만** 준다(적응도 70) — `weightSlotOf`가 요구 역량이 같아서 좌우를 합치므로,
  문턱이 없으면 주 RB가 반대쪽 LWB에서도 가산을 받아 진짜 왼쪽 자원을 밀어낸다.
  **지정 선발은 반드시 1군이다** — 등록 순서에서 코어를 맨 앞에 세워, 뎁스 자원이
  홈그로운 상한을 먼저 먹고 정작 지정 골키퍼가 2군에 남는 일이 없게 한다.
- **1·2군 스쿼드 + 등록 명단** — `GAME_PLAYER.squadLevel`로 1군(`first`)과
  2군(`reserve`)을 나누되, **1군 인원의 상한은 임의의 숫자가 아니라 실제 리그의
  등록 명단 규칙**이다 (`packages/domain/src/squad-rules.ts`).
  **만 21세 초과 25명**까지 올릴 수 있고 그중 **비홈그로운은 17명**이 한계다
  (= 홈그로운 8명 조건의 실제 형태 — 홈그로운이 7명뿐인 구단은 24명만 올린다).
  **U21은 명단을 차지하지 않아 무제한**이며 경기에도 그대로 나간다. U21 기준은
  시즌 시작 연도 −21년 1월 1일 이후 출생(시즌 도중 생일이 지나도 안 바뀐다).
  강등의 하한은 매치데이 명단(선발 11 + 벤치 9 = **20명**)에서 온다.
  홈그로운은 **협회 기준**이라 `GAME_PLAYER.homegrownCountry`와 클럽의 리그 국가를
  비교한다 — 잉글랜드에서 자란 선수는 잉글랜드 안에서 이적해도 홈그로운이지만
  라리가로 가면 아니다. 등록 현황은 저장하지 않고 1군 명단에서 파생한다
  (`squadRegistrationOf`). 명단이 찬 채로 영입하면 그 선수는 **2군으로 들어온다**.
  2군 경기는 생략한다. **성장의 갈래는 소속이 정한다** — 감독 팀 1군은 훈련·경기
  **결산 판정**(LLM)이 능력치·전술 적응도를 함께 움직이고, **2군과 모든 타 팀**은
  코어의 **월간 성장**(나이별 노화 곡선 × 잠재력 여유 × 난수, 매월 1일 최대 2축 ±1)을
  받는다 (`development.ts`). 4,000명분을 매달 모델에 태울 수 없기 때문이고, 그래서
  같은 질문에 코어가 답한다. 2군 선수는 승격 후에만 라인업에 배치된다.
  ⚠️ **판정이 "한 칸"이라 해도 그대로 오르지 않는다** (`attributeGainScale`) —
  열여덟의 한 칸과 서른의 한 칸은 같은 사건이 아니다. 실제 성장을 가르는 셋을
  곱한다: **잠재력 여유**(천장에 닿으면 0) × **나이**(스물넷까지 빠르고 스물여덟부터
  급감, 축마다 시계가 다르다 — 다리는 먼저 죽고 머리는 늦게까지 큰다) ×
  **현재 수준**(85→86은 60→61보다 무겁다. 잠재력 여유와 겹쳐 보이지만 다른
  이야기다: 잠재력 90인 두 선수라도 지금 70과 85의 다음 한 칸은 무게가 다르다).
  18세 60(잠재 85)은 판정 한 번에 한 칸이고, 30세 85(88)는 열세 번을 받아야 한다.
  못 채운 몫은 **`GAME_PLAYER.growthCarry`에 쌓인다** — 이 그릇이 없으면 곡선이
  늘 1보다 작은 노장은 아무리 훈련해도 영영 그대로다(구 세이브엔 없어 optional).
  **한 번에 한 칸까지만** 반영해 밀린 캐리가 장부를 두 칸 뛰게 하지 않는다.
  **내려가는 건 깎지 않는다** — 오히려 이미 꺾이는 축은 더 크게 받는다
  (`attributeDeclineScale`). 서른셋의 스피드가 나빠졌다는 판정은 그대로가 맞다.
- **일정 축 단일화** — `SCHEDULE_ENTRY(date + time + type)`에 경기·훈련·
  이적창 개장/폐장·**컵 대진 추첨**이 모두 등록된다. type별로 `MATCH`/
  `TRAINING_SESSION`/`TRANSFER_WINDOW`를 가리키고, 추첨만 별도 엔티티 없이
  `refId="facup:r16"`처럼 대회·단계를 직접 가리킨다. 훈련 반복 규칙 테이블은
  없다 — 스킬이 지시 즉시 엔트리를 펼쳐서 생성한다.
- **개인 훈련은 팀 훈련 위에 얹힌다** (`set_player_training`) — 축(`axis`)도
  자리(`position`)도 **훈련 결산(LLM)이 판정한다**. 자리는 `positionGain`(0~2)으로
  오르고, 새 자리가 본업을 넘어서면 코어가 주 포지션을 옮긴다(장부 정리).
  코어가 날짜를 세어 올리던 방식을 걷어낸 이유는 전술 적응도·능력치가 이미 결산의
  몫이라 같은 축을 두 주체가 나눠 쥐면 안 되기 때문이다.
- **기본 훈련은 달력에 미리 깔려 있다** — 감독이 아무 말도 하지 않아도 팀은
  훈련한다. 배치는 요일이 아니라 **경기와의 거리**가 정한다(실제 구단의
  마이크로사이클 그대로): MD+1 회복 · MD+2 **완전 휴식** · MD-2 전술 · MD-1 경기
  준비 · 그 외 본훈련(15축을 도는 보편 메뉴). MD+2를 쉬는 건 근거가 있다 —
  비접촉 부상률이 다른 배치보다 2~~3배 낮다. 그래서 주중 경기가 끼면 본훈련이
  저절로 사라진다: **연전 예외가 따로 없다.**
  **쉬는 날도 규칙의 일부다** — ① 7월 1일 부임 시점엔 선수단이 아직 **여름 휴가**
  중이고 소집은 **7월 둘째 월요일**이다(`preseasonReturnDate`). 실제 EPL 복귀는
  7월 초~~하순에 흩어져 있고(2026년 에버턴 7/10 · 다수 7/13 · 맨시티 7/20),
  FIFPro는 최소 4주 회복을 권고한다. 감독에겐 빈 열흘이 아니다 — 이적시장이
  7/1에 열려 있어 영입·매각과 시즌 계획이 선수단보다 먼저 움직인다.
  ② 경기가 걸리지 않는 **주말 이틀은 오프** — 경기 없는 주는 평일 5일만 훈련한다.
  ③ 프리시즌은 3단계다: **복귀 주**(메디컬·체력 측정으로 열고 강도를 올리지
  않는다) → **기초 체력기 2주**(러닝·웨이트·스프린트·경합, 화·목 이중 세션) →
  **전술 통합기**(단일 세션, 개막 준비).
  능력치를 겨냥한 focus는 **본훈련에만** 둬서 특정 축으로 성장이 쏠리지 않는다.
  배치는 날짜만으로 결정돼 몇 번을 다시 깔아도 같고(멱등), 이미 훈련이 있는
  슬롯은 건드리지 않아 **감독의 지시가 언제나 이긴다**
  (`TRAINING_SESSION.auto`가 둘을 가른다). **일정이 움직이면 훈련도 따라 움직인다** —
  컵 대진은 경기 몇 주 전에 편성되고 리그 경기는 컵에 밀려 다른 날로 연기되므로,
  tick이 매일 **남은 시즌 전체의 기대 배치를 다시 계산해 실제와 비교**하고 어긋난
  자리의 기본 세션만 걷어내 다시 깐다(`syncDefaultTraining`). 창을 좁히면 두 달 뒤
  대진이 잡혀도 그 주가 한동안 틀린 채 보이고, 규칙을 흉내 낸 부분 검사는
  MD−1·MD+2를 놓친다 — 설치와 검사가 `planWindow` 하나를 공유해야 어긋나지 않는다.
  경기일 훈련은 tick이 취소한다 (`packages/engine/src/training-plan.ts`).
  ⚠️ **비우기는 지우기가 아니다** (`clear_training`). 휴식은 원래 **엔트리 부재**로
  표현되므로(MD+2·주말) 훈련을 지우기만 하면 `syncDefaultTraining`이 그 자리를
  "아직 안 깐 날"로 읽고 다음 tick에 기본 훈련을 도로 깐다 — 감독의 지시가 하루도
  못 간다. 그래서 비운 자리에는 **휴식 세션**(`TRAINING_SESSION.rest`)을 세워
  기본 배치가 못 들어오게 하고, 달력에도 "휴식"으로 보이게 한다(아무것도 안 보이면
  지시가 먹혔는지 알 수 없다). 이 세션은 훈련으로 소화되지 않는다 — 성장도 부상도
  없고 회복은 훈련 없는 날과 같다(`tick.ts`의 `idleDay`). "내가 잡은 특별 훈련만
  취소하고 평소대로"는 `rest: false`로 자리만 비워 기본 훈련을 되돌린다.
  **범위는 좁은 쪽이 기본**이다(`to` 없으면 하루) — 시즌 전체가 깔려 있어서
  넓은 기본값은 "내일 쉬자" 한마디에 시즌을 통째로 비운다.
- **추첨은 장식이 아니라 상태 기계의 일부** — `scheduled`인 추첨이 남아 있다는 건
  "그 라운드는 아직 안 열렸다"는 뜻이라 시즌 종료 판정도 이걸 본다
  (`hasPendingDraw` — 안 보면 결승 없는 대회를 남긴 채 시즌이 넘어간다).
  결승은 준결승 승자 둘이 곧 대진이라 추첨하지 않고 바로 편성한다.
- **상대는 몰라도 날짜는 안다 — 단 확보한 자리만** — 추첨 전 컵 라운드는
  `cup-round` 엔트리로 달력에 오른다(실제 협회도 시즌 전에 전 라운드 날짜를
  공표한다). 다만 **컵당 많아야 한 칸**이다 — 전 클럽이 나가는 첫 라운드는 부임
  첫날부터, 그 뒤는 **직전 라운드를 이긴 순간** 다음 칸이 열린다. 진행 중인
  대진은 확보가 아니다. 7월에 "FA컵 결승 5/16"이 박혀 있으면 거짓 약속이고,
  같은 이유로 대항전 녹아웃도 넣지 않는다(리그 페이즈를 통과해야 우리 경기다).
- **시즌 일정은 실제 EPL 캘린더 골격** — 8월 중순 금요일 밤 개막 → 5월 마지막
  일요일 동시 킥오프 최종전. A매치 휴식기 4회·컵 주말 2회엔 리그가 쉬고,
  박싱데이 라운드와 성탄 연전이 고정이며, 잃은 라운드는 주중(수) 라운드로
  메운다. 주말 10경기는 금·토·일·월의 실제 중계 슬롯으로 흩어진다.
  편성 불변식(테스트로 고정): 홈·원정 3연전 없음, **경기 간 48시간 휴식**,
  역경기는 8라운드 이후, 후반기는 전반기의 미러가 아니다. 대진은 세이브
  시드로 추첨하므로 게임·시즌마다 다르다.
- **휴식은 날짜가 아니라 킥오프 시각으로 잰다** (`MIN_REST_HOURS = 48`,
  `restHours`·`tooClose`). 날짜로 세면 목요일 21:00 유로파 다음 토요일 12:30이
  "이틀 뒤"로 통과하지만 실제 휴식은 39시간 30분이다 — 실제 리그가 목요일 유럽
  원정 클럽을 일요일로 미루는 이유가 그것이다. 대항전과 붙은 리그 슬롯을 같은
  라운드에서 맞바꿀 때도 **개수가 아니라 모자란 시간의 합**으로 세야 23시간짜리를
  44시간으로 바꾸는 교환이 "개선"으로 잡힌다. 컵 편성·리그 연기도 같은 자를 쓴다.
  완전한 48시간은 강제하지 않는다 — 43~47시간(목요일 유로파 → 주말 리그)은 실제
  프리미어리그에서 매 시즌 벌어지고, 그것까지 없애면 라운드 골격이 무너진다.
- **유럽 대항전 — 리그 페이즈** — UCL 24 · UEL 16 · UECL 10팀(5대 리그 배정분만).
  티켓은 **지난 시즌 리그 최종 순위 + 국내 컵 우승**으로 배정되고(첫 시즌만 구단 등급),
  컵 우승팀이 순위로 못 들어왔으면 그 대회 마지막 자리를 내주고 밀린 팀은 한 단계
  아래 대회로 내려간다 — 리그별 티켓 수는 그대로라 정원·짝수 제약이 흔들리지 않는다.
  배정 결과는
  파생으로 되돌릴 수 없으므로 `state.euroEntrants`에 저장한다. 추첨은 원형 편성의
  **자리 배치**를 언덕오르기로 고르는 방식이라(같은 리그 회피 100점 · 포트 편차 1점)
  편성 불변식을 그대로 두고 대진 품질만 올린다.
  단일 순위표라 리그와 같은 `computeStandings(state, cupId)`로 계산된다.
  리그 주중 라운드는 예약된 대항전 주중 8개를 비켜 가고, 그래도 붙는 주말
  금·월 슬롯은 **같은 라운드 안에서 맞바꿔** 푼다 — 어떤 팀도 이틀 연속 뛰지
  않는다. `buildSeasonFixtures(season, seed)`가 시즌 편성의 단일 입구이고,
  감독의 달력엔 우리 리그 전체 + 우리 팀 대항전만 오른다.
- **대항전 녹아웃** — 리그 페이즈 순위로 직행·플레이오프가 갈리고, 플레이오프 →
  본선 → 결승까지 2차전제(합계 득점, 같으면 승부차기)로 진행된다. 결승은 중립
  단판. 대진은 미리 못 짜므로 **직전 단계가 끝나면 다음 단계를 편성하는** 진행형
  상태 기계다(`advanceEuroKnockouts`). 우승·트로피는 시즌 리뷰가 확정한다.
  상금은 참가비·승무 수당·단계 진출·우승으로 나뉘고 중복 지급은 원장 항목명으로
  막는다 (금액은 `CupCatalogEntry.prize` — 밸런스 임시값). 주중 경기의 대가는
  피로다 — AI 팀도 경기당 피로가 쌓이고 지친 선발은 자동 로테이션된다.
- **세계의 모든 경기는 코어가 굴린다** — 시즌 2,100여 경기 중 감독이 치르는 50여
  경기만 xg 구간 시뮬레이터(`sim/match-engine.ts`)를 지나고, 나머지는 **간이 시뮬**
  (`quick-sim.ts`)이 한 번에 처리한다. LLM은 한 경기도 결정하지 않는다.
  ⚠️ **간이 시뮬의 장부도 얇지 않다** — 결과·득점자·도움에 더해 **골의 분·카드·
  퇴장·교체**가 여기서 나온다. 카드 상수(`CARDS_PER_MATCH`·`STRAIGHT_RED_CHANCE`·
  `BOOKED_AGAIN_WEIGHT`)는 구간 시뮬과 **공유**하고, BOOKING·SUSPENSION은 **같은
  문**(`discipline.ts`)을 지난다. 안 그러면 누적 경고 정지가 우리 팀에만 걸려
  리그가 우리만의 규칙으로 돈다(실제로 그랬다). 퇴장은 남은 시간만큼 결과에
  닿고(자기 −30% · 상대 +35%), 교체는 최대 4명이며 **피로가 뛴 시간만큼** 나뉘어
  AI의 로테이션이 값을 한다. 측정: 경기당 골 2.80 · 경고 3.30 · 퇴장 0.233 ·
  교체 3.7/팀 (match-sim.md §7).
  **같은 날 경기는 킥오프 순서를 지킨다** — tick은 우리 킥오프보다 먼저 시작하는
  경기까지만 굴리고, 나머지는 `finalizeMatch`가 우리 경기 뒤에 이어 굴린다.
  하루치를 통째로 굴리면 12:30에 뛰는 감독이 17:30 경기 결과를 미리 안다.
  ⚠️ **회복도 리그 전체가 받는다** (`recoverOtherClubs`) — 예전엔 하루 회복이
  `userPlayers`만 돌아서 남의 팀은 경기로 깎이기만 했다. 시즌 중반이면 우리 선발은
  늘 100인데 상대 상위 14명은 평균 77 · 최저 30이었고, 그게 "상대는 늘 지쳐 있다"의
  정체였다. 회복량 눈금은 감독 팀의 기본 마이크로사이클과 같다(MD+1 회복 16 ·
  MD+2 휴식 13 · 그 외 훈련 8). **AI는 로테이션으로 안배한다** — 체력 70 이하면
  15 이상 신선한 자원(기량 −8 안쪽)에게 자리를 내주고, **35 이하면 기량과 무관하게**
  뺀다(그 갈래가 없으면 대체 불가한 스타가 0까지 내려갔다).
- **국내 컵 — 나라별 32강 순수 녹아웃** — FA컵·카라바오컵(잉글랜드) · 코파 델 레이 ·
  코파 이탈리아 · DFB-포칼 · 쿠프 드 프랑스. **순위표가 없다** — 대회 탭은 표 대신
  브래킷을 보여주고 `computeStandings`는 빈 배열을 돌려준다.
  ⚠️ **대회는 1부가 들어오는 라운드에서 시작한다** — 실제 컵은 하부리그가 먼저 몇 달을
  싸우고 1부는 한참 뒤에 합류하는데, 그 앞부분이 우리 세계엔 없다. 32팀을 "1라운드"에
  몰아넣으면 아스날이 리그컵 1라운드에 나오고 맨유가 8월에 컵을 시작한다 — 그래서
  우리 첫 라운드가 곧 **그 진입 라운드**다(리그컵·FA컵 3라운드, 코파 32강, 포칼·코파
  이탈리아 1라운드, 쿠프 32강). 이름도 날짜도 그 라운드의 실제 값이고(`stageNames`·
  `windows`), 8강부터는 규모와 이름이 다시 맞는다. **리그컵과 코파 델 레이는 규모까지
  실제와 같다**(그 라운드가 32팀). 결승 요일도 규정이라 넷은 주말, **코파 이탈리아만
  수요일 밤**이다(`finalMidweek` — 올림피코). 32팀을 채우기 위해
  나라마다 **2부 클럽**(챔피언십·세군다·세리에 B·2.분데스리가·리그 2, 총 64팀)을
  두는데, 2부는 **리그전을 돌지 않고 컵에만 나온다**(`league-catalog`의 `division: 2`).
  1부↔2부 대진은 2부가 홈이고 전력은 `SECOND_DIVISION_PENALTY`만큼 낮다 — 이변이
  가끔 나오되 기본은 1부가 이기는 간격이다.
  **추첨 방식이 실제처럼 두 갈래다** — FA컵·리그컵·코파·포칼·쿠프는 라운드마다
  새로 뽑고(`per-round`), 코파 이탈리아만 시즌 초에 대진표(tabellone) 전체가
  확정돼 이후 추첨이 없다(`fixed-bracket`, 시드 8팀을 브래킷에 흩는다).
  **홈 배정도 대회 규정을 따른다** — 포칼·코파·쿠프는 하부 클럽 홈, 코파 이탈리아는
  시드 클럽 홈, FA컵·리그컵은 먼저 뽑힌 팀 홈(`homeRule`).
  대항전처럼 직전 라운드가 끝나면 다음 라운드를 편성하는 진행형이지만
  (`advanceDomesticCups`) **날짜가 다르다** — 컵은 실제 대회 날짜(`windows`)를 먼저
  차지하고, 그 자리에 걸린 **리그 경기가 연기된다**(`reschedule.ts` — 실제 리그의
  "컵 진출로 인한 연기"). 대항전·다른 컵 경기와 리그 **최종 라운드**는 못 옮긴다.
  옮겨야 할 경기 중 하나라도 자리를 못 찾으면 전부 되돌리고 컵이 대신 비켜준다.
  한 라운드는 **이틀에 흩고**(실제 컵의 화·수 / 토·일), **결승은 주말만** 선다.
  ⚠️ 날짜 탐색의 마지막 관문은 "**같은 날 겹치지 않는다**"이다 — 요일 제약을 풀어서라도
  지킨다. 한 팀이 하루에 두 경기를 가지면 tick이 그중 하나를 영원히 남겨 시즌이
  넘어가지 않는다. 그래서 `reservedEuroDates`에는 **대항전 결승일까지** 들어 있다. 단판이 비기면 곧장
  승부차기, 2차전제는 리그컵·코파·코파 이탈리아의 준결승뿐이고 결승은 전부 중립이다.
  우승팀은 트로피 + 상금 + **다음 시즌 유로파**(리그컵만 컨퍼런스)를 얻는다.
  ⚠️ 2부 클럽이 없는 옛 세이브는 **로드 시 채워 넣는다**(`addMissingClubs`) — 세이브
  버전을 올리지 않는다. 시즌 중반에 붙은 세이브는 그 시즌 컵을 건너뛴다.
- **이적 시장 전용 리그** — 사우디 프로 리그·MLS 8클럽은 **경기를 하지 않는다**
  (`league-catalog`의 `kind: "market-only"`). 일정·순위표·컵·재정 원장 어디에도
  없지만 선수 검색과 이적 협상에는 그대로 잡힌다 — 레전드를 보관하고 돈으로
  선수를 흡수하는 곳이다. **이적창이 우리와 다르다**(사우디 10/6, MLS 2~4월):
  등록은 사는 쪽 협회 규정을 따르므로 우리 창이 닫힌 뒤에도 그들은 우리 주전을
  사 갈 수 있고, 팔면 대체 영입은 못 한다. 사우디는 지르고(이적료 ×1.45 · 주급
  ×3.5) MLS는 아끼며 둘 다 노장을 반긴다. 역수입은 나이 곡선 + **복귀 저항**으로
  잡는다 (docs/design/transfers.md §4.5).
- **이적·계약** — `TRANSFER`가 이적·임대·자유·유스 콜업·은퇴까지 모든 팀 변경을
  기록하는 원장(`GAME_PLAYER.teamId`는 현재값일 뿐). `CONTRACT`가 주급의
  원본이고, 팀 주급 총액은 활성 계약의 합(파생).
  **나가는 문은 셋이다** — 매각 · **방출**(`release_player` — 잔여 주급의 절반을
  위약금으로 즉시 물고 원장에 남아 PSR까지 간다) · **임대**(`loan_player` —
  계약은 우리 것으로 남고 `GAME_PLAYER.loan{fromTeamId, until, wageShare}`이
  복귀를 파생시킨다. 주급은 분담 비율만큼 임대 팀이 내고 그 계산도 파생이다).
  **임대는 양쪽 다 흥정이다** — 빌려 오든(`send_offer` kind=loan) 빌려주든
  (kind=loan_out) 상대가 받아 줘야 성립하므로 같은 협상 테이블을 탄다. 흥정이
  아닌 것은 부르기뿐이다(`recall_loan`). 영입의 관문은 이적과 다르다: 구단은
  "내보내도 되나"(주전이면 안 준다), 선수는 "가서 뛸까", 돈은 이적료가 아니라
  **임대료 + 주급 분담**이다.
  **판정을 기다리는 협상은 주의 줄 맨 앞에 선다**(`pendingVerdicts` → `❗`).
  답이 도착한 그 턴에 GM은 그 사실을 모른다 — 시간은 장면 헤더로 흐르고 결과는
  다음 턴 입력에 실리기 때문이다. 그 틈에서 협상이 잊히지 않게 기한 하루 전에도
  한 번 더 세운다. ⚠️ 도구를 합치면 **프롬프트의 지목도 함께 옮긴다** — 철칙 8이
  사라진 도구 이름을 부르고 있었다.
  **세계는 감독 없이도 이적한다** (`ai-market.ts`) — 예전엔 한 시즌을 다 돌려도
  AI↔AI 이적이 **0건**이었다. 창이 열리고 닫히는 동안 리그의 스쿼드가 한 명도
  움직이지 않아, 라이벌은 여름 내내 아무도 사지 않는 구단이었다. 지금은 tick이
  매일 시장을 굴린다 — **1부 클럽당 시즌 이적 3.4건 · 임대 1.9건**(실제 5~7건에서
  우리가 모델링하지 않은 리그와의 거래를 뺀 규모), 여름:겨울 = 7:3, 마감 한 주 전엔
  시도가 2.2배로 몰린다. 협상 과정은 두지 않는다 — 오퍼·역제안·설득은 감독의 것이고
  AI끼리는 **결과만** 남긴다(하루 수백 번의 판정을 볼 사람이 없다). 거래는 **수준과
  자리**로 맞추고(상위 15명 평균 ±7 · 그 자리 1군 인원이 얇을 때), 임대는 **어린
  자원이 한 단계 아래에서 뛰는** 실제 임대의 모양이다(주급 분담은 받는 쪽 형편이
  정한다 — 1부 50% · 2부 20%).
  ⚠️ **두 문턱이 리그를 살린다** — ① 인원은 **1군으로 센다**(전체 스쿼드는 40~49명이라
  상한 34로 두면 아무도 못 사고, 포지션군도 중앙값 12라 문턱 8이면 1부가 전멸해
  성사되는 딜이 죄다 2부의 쇼핑이 된다) ② **2부는 돈을 쓰지 않는다** — 리그전이
  없어 수입이 없는데 이적료는 한 방향 유출이라 세리에 B 중간 잔고가 £25M에서
  마이너스로 갔다(재정 기준선 테스트가 잡았다). 파는 것과 임대로 받는 것은 그대로다.
  사우디도 **현금 바닥**을 지킨다(예산만 보고 지르게 뒀더니 −£180M까지 갔다).
  하루 스쿼드 색인(`indexSquads`)이 없으면 tick이 37→86ms로 뛴다.
  ⚠️ **계획은 주 1회, 실행은 날짜별로 흩는다** (`state.aiDeals` — 옛 세이브엔 없다).
  매일 굴리면 하루 tick이 무겁고, 한 날에 몰아 실행하면 "8월 3일에 리그가 통째로
  이적한 날"이 된다. 그래서 주 단위로 결정하고 그 주의 각 날짜로 배분한 뒤, 그 날의
  tick이 장부를 쓴다 — 실행 시점에 다시 검사하므로 그사이 다치거나 이미 옮긴 딜은
  조용히 무산된다(실제 시장의 결렬이다). **마감 주에는 날짜가 뒤로 쏠린다**(제곱 분포).
  결과: 시즌 이적일이 90일에 흩어지고 하루 중앙 4건, 마감일에 54건이 몰린다.
  **재정도 함께 본다** — 이적 예산 · 현금 바닥 · **주급 여력**(`clubWageBudget`의 1.1배).
  주급을 안 보면 이적료는 못 내도 주급은 낼 수 있는 것처럼 보이는데, 파산의 실제
  경로는 매주 나가는 그쪽이다.
- **벤치의 사람도 바뀐다 — 감독 경질·선임** (`manager-market.ts`). 예전엔 12월에
  6연패를 한 구단이 이듬해 5월까지 같은 벤치로 앉아 있었다. 지금은 등급별 문턱
  (tier 1 10위 · 2 14위 · 3 18위 · 4 20위) 아래로 처진 구단이 하루 9% 확률로 결단하고,
  후임은 이름·부임일과 새 전술 역량치를 갖는다. **새 감독 효과**도 있다 — 선수단의
  체력·폼이 조금 오른다(실제로 관측되는 반등이다). 시즌 96구단 중 24건 안팎.
  ⚠️ **기대와의 차이로만 재면 안 된다** — 잔류가 기대인 구단(목표 17위)은 꼴찌를 해도
  차이가 3이라 **강등권 감독이 영원히 안 잘렸다**. 그래서 등급마다 자리를 직접 적는다.
  **감독도 잘린다 — 다만 경고가 먼저다** (`reviewUserSeat`). 보드가 한 달에 한 번까지
  경고하고(3회), 경고를 안은 채 **리그 최하위권(19·20위)**이고 보드 신뢰가 25 아래일
  때 경질된다(`state.dismissal` — 시계가 멈춘다). AI보다 훨씬 관대한 문턱인데,
  **아직 새 구단에 부임하는 길이 없어 경질이 곧 세이브의 끝**이기 때문이다. 순위를
  기대 위로 올리면 경고가 하나 지워진다 — 되돌릴 수 있어야 압박이 이야기가 된다.
  **무소속은 리그가 아니라 리그 밖이다** (`league-catalog`의 `free` · 클럽
  `freeagents`). `teamId`가 필수라 자리를 하나 두되 **클럽이 아닌 것으로**
  다룬다(`isClubTeam` — 초기 스쿼드 없음 · 일정·순위표 없음 · 스쿼드를 논하는
  순회에서 제외). 방출·계약 만료는 전부 여기로 오고, **이적창이 열린 날** 시장이
  데려간다 — 그 포지션군이 얇고 수준이 비슷한(상위 15명 평균과 OVR 차 8 이내)
  구단이 확률로 손을 든다(`signFreeAgents`, 하루 2건). **우리 팀은 이 경로로
  선수를 받지 않는다** — 감독이 직접 데려와야 한다.
  **계약 만료는 유저 팀도 예외가 아니다** — 재계약하지 않으면 시즌 뒤 무소속으로
  떠나고, tick이 180·90·30일에 한 번씩 경고한다. **다른 구단도 계약을 관리한다**
  (`runAiRenewals`) — 만료 240일 안의 AI 계약을 매일 검토해 주전일수록·어릴수록
  서둘러 잡고, 재계약이 성사되면 **우리가 노리던 협상은 그 자리에서 끝난다**.
  기다림에 대가가 있어야 "계약 1년 남은 선수를 싸게" 판단이 성립한다.
- **협상은 원장과 분리** — `NEGOTIATION`(+오퍼 라운드)이 진행 중 흥정을 갖고,
  합의가 확정될 때 비로소 `TRANSFER`·`CONTRACT`가 쓰인다. 영입·매각·재계약 3방향.
  **매각의 시작은 세 갈래다** — 감독이 값을 부르며 내놓는 **이적 리스트 등재**
  (`TRANSFER_LIST` · 등재 선수는 하루 34%로 오퍼가 붙고 호가가 비쌀수록 더디 붙는다),
  특정 구단에 직접 넣는 **매각 제안**(`offer_player` — `send_offer`의 거울상),
  그리고 AI가 먼저 노리는 길(하루 8%)이다. 예전엔 마지막 하나뿐이라 감독이 팔기로
  해도 할 수 있는 일이 없었고, 그 빈자리를 GM이 2군 강등 + 예산 증액으로 메워
  이야기와 장부가 갈라졌다. 같은 이유로 `adjust_transfer_budget` 한도를 **하루
  누적**으로 고쳤다 — 건당이면 나눠 불러 얼마든지 넘는다. **매각의 역제안은 깎아
  부르는 것**이다(시장가 55% 이상 · 우리 호가 미만).
  **코어가 딜 성공 확률과 근거를 결정적으로 계산**하고(`market.ts`), 수락·역제안·
  결렬 **판정은 LLM**이 그 확률을 앵커로 한다 — 코어는 가능한 판정만 받는다
  (docs/design/transfers.md · ADR 0002).
- **합의와 계약 사이에 메디컬이 있다** (`medical.ts`). 예전엔 `accept_deal` 하나가
  합의를 그 자리에서 이적으로 바꿔서, 오퍼가 도착한 날 도장을 찍고 기자회견까지
  여는 장면이 한 턴에 담겼다 — 이적이 서류 한 장으로 읽혔다. 지금은 그 도구가
  **검진 일정을 잡을 뿐**이고(합의 다음 날부터 이틀 안 · 창 마감일에 걸리면 그날),
  계약은 **결과가 나온 날** 코어가 옮긴다. 통과는 감독을 부르지 않는다 — 이미
  결정한 일의 확인이라, 주의 줄에 세워 두면 GM이 매 턴 같은 도구를 다시 부른다.
  **소견(`flagged`)은 불합격이 아니라 판단거리**다: 데려가는 쪽이 강행(`accept_deal`
  재호출 — 대가로 그 선수의 부상 성향이 오른다)·철회(`withdraw_offer`)·값 재협상을
  고른다. 소견을 보고 물러선 것은 **결렬이 아니어서**(`expired`) 그 창이 닫히지
  않는다 — 실제 이적의 가장 흔한 결말이 "조건을 다시 짜기"인데 `rejected`로 적으면
  그 길까지 막힌다. **우리가 파는 쪽이면 결정권은 상대에게 있다** — 사는 구단이
  소견을 보고 15% 깎아 다시 부르고 협상이 `open`으로 돌아온다. 판정은 협상 시드에
  묶여 결정적이고, 확률은 **부상 성향 × 나이 × 현재 부상**에서만 나온다(부상 중인
  선수를 사면 거의 확실히 소견이다). 유리몸 데이터가 이적시장에서 값을 하는 자리다.
- **기자회견 — 세계가 감독에게 먼저 말을 거는 유일한 자리** (`press.ts`).
  감독의 다른 손잡이는 전부 감독이 먼저 손을 뻗는 것이지만, 회견은 세계가 부른다 —
  아무것도 하지 않아도 사건이 생기고 **답하지 않는 것조차 하나의 답**이다.
  **자리는 코어가 열고, 질문은 기자가 쓴다** — 경기가 끝나면 매번(실제 리그의 의무
  회견이고, 이겼을 때만 열리면 회견이 상이 된다), 큰 이적이 확정되면 그때.
  ⚠️ 코어가 넘기는 건 **사실 카드**(`PressFact`)지 질문 문장이 아니다 — 스코어·최근
  무승·폼이 바닥인 선수·영입에 밀리는 선수. 문장을 코어에 박아 두면 시즌 내내 같은
  말이 반복되고 기자의 성격도 그날의 맥락도 문장에 닿지 못한다. **코어가 지켜야 할
  것은 사실이지 문장이 아니다** — 모델은 그 카드 밖의 사실을 묻지 못한다
  (→ docs/design/approaches.md).
  **판정은 LLM이 한다** — 감독 발화를 5스탠스(감싸기·책임 인정·공개 비판·도발·
  말 아끼기) 중 하나로 옮긴다. 스탠스를 보기로 고르게 하지 않는 건 이 게임의
  인터페이스가 말이기 때문이다. **공짜인 스탠스는 없다** — 선수를 감싸면 라커룸을
  얻고 언론을 잃고, 날을 세우면 그 반대다. 효과는 평판 3축(보드·언론·선수단) +
  **지목된 선수의 사기**(팀 전체 위에 더 얹힌다 — 공개적으로 감싸이거나 잘린
  당사자는 같은 말을 남의 이야기로 듣지 않는다) + 팀 전체 사기이고, 폭은 회견의
  `weight`(1~3)에 비례하는 코어 한도 안이다.
  ⚠️ **회견은 하나만 열려 있고, 방치는 거절과 같은 대가를 치른다** — 답하지 않은
  채 다음 회견이 오면 앞의 것이 `declined`로 닫힌다. 무시가 공짜면 아무도 답하지
  않는다. 옛 세이브엔 없어 로드 시 빈 배열로 채운다(세이브 버전 유지).
- **설득 — 돈으로 못 넘는 벽을 넘는 수단** (`persuasion.ts`). 감독이 쥔 손잡이가
  이적료·주급 둘뿐이면 불가능한 이적은 영원히 불가능하다. 그래서 감독의 설득
  발화를 LLM이 **주장(claim)**으로 옮기고, 코어가 상태와 **사실 대조**한다.
  **무게 표는 두지 않는다** — "이 선수에게 고향 복귀가 얼마나 큰가"는 계산할 수
  없는 질문이라, 코어는 사실 여부만 가리고 **무게는 선수를 연기하는 LLM이 판정**한다.
  코어가 쥐는 건 둘뿐이다: **거짓의 대가**(확인 안 되는 주장은 확률을 깎는다 —
  남용 방지라 결정적이어야 한다)와 **판정 여유**(확인된 논거가 하나라도 있으면
  코어는 더 이상 수락을 막지 않는다 — 확률 3%짜리 딜도 선수가 마음먹으면 성사된다).
  복귀 저항 같은 계수는 없앤 게 아니라 **넘어서는** 것이라 근거에 둘 다 남고,
  감독이 실제로 한 말은 `NegotiationRound.pitch`로 판정자에게 그대로 전달된다.
- **재정은 실제 구단 구조** — 수입은 중계권(균등·성적 수당·생중계 수당)·매치데이
  (수용인원×점유율×단가+호스피탈리티)·상업(스폰서 성과 조항)·상금, 지출은 주급·
  스태프 급여·성과 보너스·경기 운영·시설·원정의료·에이전트 수수료로 나뉜다.
  **이적료는 현금에서 즉시, 장부에는 계약기간 상각**으로 잡혀 두 축이 갈린다 —
  그래서 PSR(3시즌 누적 손익 £105M 한도 → 보드 경고 + 이적 예산 동결)이 성립하고
  이적 예산이 재정 성과에 연동된다. 상세 원장은 **유저 팀 최근 3개월**만 남고
  **매월 1일 발행되는 월간 보고서**(`FINANCE_REPORT`)가 영구 요약을 갖는다.
  상수·공식은 `packages/engine/src/finance.ts` 한 곳에 있다
  (docs/design/club-finance.md · ADR 0004).
- **감독 능력치 5축** — 리더십·전술·**훈련**·협상·**분석** (`ManagerAttributes`).
  능력치이던 `media`는 뺐다: 대응 스킬 하나에만 걸린 축이라 역량으로 읽히지 않았고,
  상시로 도는 루프(스카우팅·상대 분석)가 축이 되는 편이 플레이와 붙는다.
  ⚠️ **평판의 `media`는 그대로다** — 능력치는 감독이 가진 것, 평판은 세계가 보는 눈이다.
  옛 세이브는 로드 때 `media → analysis`로 옮기고 `training`을 채운다(버전 안 올림).
  화면은 **오각형**이다 — 막대 다섯 줄은 값을 하나씩 비교하게 만들 뿐이고, 도형은
  모양 하나로 성향을 말한다(넓적하면 만능형, 뾰족하면 전문가형).
  ⚠️ **카드는 아래 섹션과 같은 폭을 쓴다** — 카드만 좁혀 두면 카드는 왼쪽에,
  트로피·업적은 화면 폭 전체에 놓여 두 축이 어긋난다. 한 화면 안에서 왼쪽 끝은
  하나여야 한다. 대신 왼쪽 열에 **평판 막대 세 줄**을 채워 도형과 균형을 맞춘다 —
  한 줄로 이어 쓰면(`보드 50 · 언론 50 · 선수단 50`) 열의 절반이 빈 채 남아
  카드 가운데가 구멍처럼 보인다. **빈 상태도 왼쪽 정렬**이다(가운데로 두면 제목과
  축이 어긋나 화면이 지그재그로 읽힌다).
- **소유 관계** — `FINANCE`(+원장·보고서)는 팀에, `SEASON_RECORD`/`TROPHY`/
  `ACHIEVEMENT`는 감독에 소속. 일지(diary)는 저장하지 않고 기록 테이블에서
  파생하며, `NARRATIVE_NOTE`만 GM 기억으로 남는다.
- **세계가 먼저 말을 건다** (docs/design/approaches.md) — 지금 세계는 감독이 말을
  걸어야만 반응한다. 유일한 예외가 기자회견이고, 그 구조를 **선수·주장·구단주·
  에이전트로 넓히는 설계**가 approaches.md에 있다: 사실 카드 → 관심(누가 반응하나)
  → 압력(말할 거리는 쌓인다) → 말 걸기 → 무시하면 오르는 사다리(코치가 대신 전한다
  → 복도 → 감독실 → 에이전트 전화 → 언론 → 이적 요청). 1단계(사실 카드)만 구현됐다.
- **인물은 데이터다 — 수석코치와 구단주** — `PERSONA`(이름·원형·성격·동기·말투+예시
  대사)가 세이브에 남고, GM·캐스터의 **레퍼런스 층**(캐시 프리픽스)에 인물 카드로
  실린다. **구단주가 카드로 서 있지 않으면 GM이 즉흥으로 연기해 수석코치 말투를
  복사한다** — 예산을 쥔 사람이 "보고드리겠습니다"라고 말하는 장면이 그렇게 나왔다.
  원형 6종(산업가·투자자·축구광·국부펀드·지역 유지·흥행가)은 **무엇으로 감독을
  평가하고 돈을 어떻게 다루며 얼마나 참는가**로 갈린다. 시드 채널이 코치와 달라
  한 세이브에서 두 사람이 같은 원형으로 겹치지 않는다.
  ⚠️ **카드가 서 있다고 그 사람이 말하지는 않는다.** 부임 첫 장면이 수석코치의
  인사라(온보딩 지시) 모델이 그 뒤로 **자기 이력을 흉내 내며** 모든 소식을 그의
  입으로 옮겼다 — 선수도 구단주도 기자도 카드로 서 있는데 세계가 비서 한 명으로
  좁아진다. 그래서 프롬프트가 **장면을 여는 사람은 그 일에 가장 가까운 사람**이라고
  못 박고, 그 자리에서 **수석코치는 훈련장과 전술의 사람이지 모든 소식이 지나가는
  통로가 아니라고** 지운다. 화면은 이미 준비돼 있다 — `speakerRoles`가 구단주·기자
  (매체명)·주장·선수를 다 알고 있어 누가 말하든 이름 옆에 자리가 붙는다.
  **이름은 구단이, 사람됨은 시드가** 정한다 — 실제 수석코치를 아는 구단은
  그 사람이 나오고(`data/coach-seeds.ts`), 모르면 리그 국적에 맞는 가상 이름을
  쓴다. 원형 6종은 시드로 갈리므로 같은 세이브는 언제 열어도 같은 사람이고, 부임한
  구단이 다르면 만나는 사람도 다르다. **화자 태그는 직책이 아니라 이름이다** —
  선수가 `@손흥민:`으로 말하듯 코치도 `@스티브 홀랜드:`로 말하고, **직책은 화면이
  붙인다**(`speakerRoles` → `스티브 홀랜드 (수석코치)`). 이름 왼쪽에는 **모든 화자가
  아이콘 하나씩**을 갖는다 — 수석코치 클립보드 · 구단주 넥타이 · 주장 완장(C) · 선수 유니폼 · 중계
  마이크, 그리고 세이브가 자리를 모르는 화자(기자·에이전트·남의 팀 사람)는 **사람**이다.
  자리는 화면이 추측하지 않는다: `speakerRoles`가 `{kind, label}`을 실어 보내고
  **글자와 그림이 갈린다** — 대화마다 `(선수)`가 따라붙으면 시끄럽지만 유니폼 하나는
  조용히 알린다. 사전에 전 리그 4,000명을 담지는 않는다(남의 팀 3군까지 넣으면
  동명이인이 늘어 정작 우리 선수가 사라진다) — **우리 선수단 + 협상 테이블에 앉은
  상대 선수**까지다. 모델 출력에 기대지 않으니
  어떤 턴에서도 빠지지 않고, **소개할 때 한 번만** 붙어 같은 화자가 이어 말하면
  이름만 남는다. 자리를 아는 화자는 다 알려 주되(페르소나 + 주장) 이름이 겹치면
  **아무것도 붙이지 않는다** — 틀린 직책보다 없는 게 낫다 (personas.md §6-1). 생성이 결정적이라 옛 세이브는 로드 때 조용히 채우고 태그도
  이름으로 고쳐 주면 되므로(`ensurePersonas`) **세이브 버전을 올리지 않는다**.
  ⚠️ 실명은 NIL 부채다 — 실존 인물엔 `real` 표식이 붙고 인물 카드에 "평판을 해칠
  서사 금지"가 함께 실린다. 표를 비우면 전부 가상 이름으로 돌아간다
  (data-sourcing.md §7). 페르소나는 시뮬 숫자에 직접 손대지 않는다 — 사기 같은
  상태를 거쳐야 세계에 영향을 준다 (docs/design/personas.md).

ERD와 설계 근거는 docs/design/implementation-notes.md에 정리했다.

### 패키지

- `packages/domain` — 카탈로그/게임 스키마 + 기록·일정 테이블 타입 + **페르소나**(Zod)
- `packages/sim` — 전력 분석 패킷 + **키포인트**(`key-points.ts` — 감독이 읽어내는
  전술 미스매치 13축: 뒷공간·측면 1대1·공중볼·압박 저항·창조·마무리·골문·수비 조직·
  경합·활동량·세트피스·경험·거친 선수. **감독의 눈만큼만 보인다** — 분석이 개수를
  (30→4개 · 85→9개), 전술이 정밀도를 정한다: 낮으면 "상대 최종 수비가 발이 느리다",
  높으면 "요케레스(pace 86) vs 자케(pace 64)". 크게 벌어진 짝은 눈이 어두워도
  또렷하고, 자르는 순서가 벌어진 폭이라 분석 능력이 "운"이 되지 않는다.
  발동한 상성·체력 구멍은 **눈에 보이는 사실**이라 이 필터를 지나지 않는다)
  + **공략**(`exploits.ts` — 키포인트를 **표적**으로 만든다. 감독이 "요케레스 뒤로
  계속 보내"라고 하면 중계 모델이 후보 목록에서 id를 골라 `exploit_point`를 부르고,
  코어가 실재를 대조한 뒤 존 조정을 얹는다. **이적 협상의 설득과 같은 분업** —
  무엇을 노렸는지는 LLM이 옮기고, 사실인지는 코어가 가리고, 얼마나 먹히는지도
  코어가 정한다. 없는 지점은 반려된다. 이득에만 소화율, 대가는 온전히,
  **동시에 두 곳까지**(셋을 노리는 건 아무것도 안 노리는 것이다).
  ⚠️ 이게 없던 때 키포인트는 **읽히기만 하는 정보**였다 — 분석 능력을 올려도
  화면의 문장 수만 늘 뿐 그 판단이 결과에 닿을 길이 없어, 판을 읽고 대응하는
  핵심 루프가 열려 있었다. AI 팀도 자기 등급만큼 공략한다: 상대 벤치가 아무것도
  안 하면 우리 약점은 영원히 드러나지 않는다)
  + **xg 구간 시뮬레이터**(`match-engine.ts` —
  경기 결과가 정해지는 곳) + 경기 장부 검증. **전력은 개인에서 시작한다** —
  `effective = roleFit × 상태 × 포지션 적응도 × 전술 적응도`이고 존 전력은 그
  평균이다. **포지션 적응도의 감점 폭이 전술 적응도보다 크다**
  (`PROFICIENCY_SPREAD` 30%p vs `FAMILIARITY_SPREAD` 15%p) — 전술은 훈련으로 몇
  주면 익히지만 자리는 커리어가 만든다. 12%p였을 때는 자리를 몰라도 손해가 거의
  없어서, 배치 최적화가 10번을 6번에 세우고도 팀 합이 더 높다고 계산했다.
  이 팩터는 sim이 단일 소스로 갖고 엔진의 배치 채점(`slotStrength`)도 같은 함수를
  부른다 — 복제하면 "배치가 고른 자리"와 "경기가 계산하는 자리"가 조용히 갈린다.
  전술 적응도는 **자리마다 무게가 다르다**(`TACTICAL_SENSITIVITY` —
  중원 1.4 · 수비 1.2 · 최전방 0.6). 전술 6축은 이득과 대가를 함께 내고,
  **두 전술은 상성 매트릭스에서 서로를 만난다**(`tactical-counters.ts` — 뒷공간·
  압박 함정/우회·빌드업 붕괴·밀집 공략·역습·늘어진 전술 등 14종. 조건이 맞을 때만
  발동하고 발동하면 문장으로 드러난다). **체력은 자리와 전술이 함께** 정하고
  (`stamina.ts` — 실제 주행거리 기준), 소진된 선수를 안 빼면 **그 라인에 구멍**이 난다
- `packages/engine` — 카탈로그 빌드·인스턴스화·일정 축·tick·스킬·경기·시즌
  전환·저장(SAVE_VERSION 6)·오피스 뷰·어드민 + **스카우팅(안개)·읽기 전용 조회**
  - **구단 재정**(`finance.ts` — 월간 보고서·상각·PSR)
  - **컵 대회** — 유럽 대항전(`europe.ts`·`euro-knockout.ts`)과 국내 컵
    (`domestic-cup.ts`)이 승부차기(`shootout.ts`)를 공유한다
  - **기자회견**(`press.ts` — 질문 생성·스탠스 한도)
- `packages/llm` — 제공자 중립 GameLLM + Anthropic/Gemini/OpenAI 어댑터.
  **프로바이더는 티어가 고른다 — 세트로 묶지 않는다.** 한 회사가 모든 자리에서
  가장 좋거나 가장 싸지는 않아서다: GM은 Claude Opus, **경기 중계는 Gemini 3.5
  Flash Lite**, 잡무(훈련 결산·경기 평점·심경)는 **GPT-5.6 Luna**.
  중계를 가볍게 두는 이유는 **사건을 코어가 정하기 때문**이다 — 모델은 xg가 굴린
  결과를 문장으로 옮길 뿐인데 90분에 스무 번 도니 지연이 곧 게임 속도다. 잡무를
  Luna로 보내는 이유는 값보다 **빈도**다 (입력 $0.20 / 출력 $1.20 per 1M —
  Flash-Lite $0.30/$2.50 · Haiku 4.5 $1.00/$5.00).
  ⚠️ `LLM_PROVIDER`는 **전 티어를 갈아엎는 스위치가 아니라 선호 순위**다 — 티어가
  고른 곳에 키가 없을 때 어디로 보낼지를 정한다. 예전엔 이 한 줄이 세 티어를 통째로
  옮겨서, 티어별 선택이 있으나 마나였다. 키가 없는 제공자로는 보내지 않는다 —
  그러면 그 티어만 조용히 mock이 되어 "GM은 멀쩡한데 중계만 무뎌진" 이유를 알 수 없다.
  **`MODELS`의 빈 칸은 "그쪽으로 안 보낸다"는 뜻**이다: OpenAI는 잡무 모델만 두어
  키 하나가 없어져도 서사가 그쪽으로 새지 않는다.
  ⚠️ **GPT-5.6은 Chat Completions에서 함수 도구와 추론을 함께 못 쓴다** —
  `reasoning_effort: "none"`이라야 한다(아니면 `/v1/responses`). 잡무 티어는 원래
  사고를 최소로 두는 자리라 어긋나지 않지만, 서사를 이쪽으로 옮기면 Responses API로
  갈아타야 한다. 같은 이유로 **OpenAI 어댑터는 스트리밍을 구현하지 않았다**.
  라우팅은 순수 함수(`tierConfig(name, env)`)라 키 조합을 인자로 갈아 끼워 검증한다.
  **입력 3층 캐시 계층**(고정 / 레퍼런스 / 이력)과 제공자별 휘발 상태 채널.
  **사고는 최소**(Anthropic 끔 · Gemini `MINIMAL`)이고 **출력 상한은 64,000** —
  상한은 사고와 본문을 함께 덮으므로 장면 길이로 좁히면 본문이 잘린다
  (온보딩을 1,200으로 잡았다가 첫 장면이 끊긴 적이 있다).
- **도구 표면은 33개다 — 한 판단은 한 도구로.** 같은 순간에 함께 정해지는 것들이
  갈려 있으면 GM이 하나를 빠뜨린다. 그래서 라인업은 **1·2군 이동까지 한 요청**으로
  받고(`set_lineup`의 `squadLevels` — 승격 → 배치 → 강등 순, 반쪽 상태를 남기지
  않는다), 한 선수의 **자리·역할·개인 지시**는 `set_player_tactic` 하나이며,
  오퍼는 방향을 인자로 갖고(`send_offer` kind=buy/sell/loan), 오퍼 판정은
  **누가 답할 차례인지 협상이 알아서**(`respond_offer`) 처리한다.
- `packages/agents` — GM 오케스트레이터 (실모드 provider tool loop + **mock 모드**)
  - **훈련 결산**(`training-rater.ts`) — `advance_time`이 넘긴 구간의 훈련을 **한
    묶음으로** 판정한다. 입력은 그 기간의 훈련 일지·감독과의 대화·개인 지시고,
    코어가 박아 둔 앵커 위에서 전술 적응도를 ±3 흔들거나 능력치를 팀 전체 3점까지
    올린다. 세션마다 부르지 않는 이유는 훈련엔 경기 같은 사건 목록이 없어 하루치만
    보면 판단할 거리가 없기 때문이다. **잦은 이벤트라 싼 티어(`chore`)를 쓰고**,
    실패하면 앵커가 남아 훈련은 언제나 완결된다..
  - **심경 결산**(`mood-rater.ts`) — 선수별 한 줄을 맥락으로 다시 쓴다. 훈련·평점
    결산이 도는 **같은 자리**(시간 진행 뒤 · 경기 종료 뒤, 평점이 매겨진 다음)에서
    같은 티어로 돌고, 코어가 사실을 검사해 어긋난 문장은 버린다. 자세한 계약은
    "선수 상태" 절 참고.
    **인물 카드**(성격·동기·말투+예시 대사)를 레퍼런스 층에 실어 화자를 일관되게 한다
- `apps/web` — Next.js 채팅 UI + 오피스 뷰(스쿼드·달력·재정·대회·커리어),
  API, `/admin` **선수 카탈로그 편집** (15축·잠재력·포지션·**주급** — 게임과 무관한
  초기치 DB라 편집은 새 게임에만 반영되고 진행 중 세이브는 영향 없음)
  - **경기가 끝나면 그 대화는 메인 채팅에서 접힌다** (`ChatTurn.inMatch`·`matchId`).
    평시의 GM과 경기의 중계는 다른 에이전트이고 감독이 거는 말도 다르다(훈련·이적
    지시 vs 교체·팀토크). 경기 하나가 중계 수십 턴을 남기는데 그게 평시 대화 사이에
    그대로 흐르면 한 시즌을 보낸 뒤 "지난주에 뭘 지시했더라"를 찾을 수 없다.
    경기는 그 자체로 하나의 사건이므로 **결과 한 줄짜리 카드**로 접고(`.match-log`),
    누르면 그때의 중계와 지시가 그대로 나온다. **진행 중인 경기는 접지 않는다** —
    지금 보고 있는 것이다(결과가 아직 없는 것으로 가른다).
    표시는 **창이 아니라 턴**에 남긴다 — 새로고침해도 그 구간은 그대로 접혀 있다.
    화면에서 파생하지 않는 이유는 킥오프 직전 라인업 확인처럼 **경기 중이지만
    중계가 말하지 않은 턴**이 빠지기 때문이다. `matchId`가 없는 옛 이력은 묶지 않고
    그대로 흐른다(무엇을 펼치는지 알 수 없는 카드는 만들지 않는다).
    색은 이미 "무대 밖의 목소리"를 뜻하던 `--gold-soft`로, 중계 화자 이름과 같아
    설명이 필요 없다. 옛 세이브엔 두 필드 다 없다(optional — 버전 유지).
  - **채팅이 곧 화면이다.** 게임 화면은 두 줄뿐이다 — 얇은 상단 띠와 **무대(stage)**.
    무대의 기본은 채팅이고, 경기가 시작되면 무대가 둘로 갈려 **왼쪽 판세
    (`MatchView`) · 오른쪽 중계·지시**가 나란히 선다. 감독은 읽던 자리에서 그대로
    지시한다 — 탭을 오가며 상황을 기억할 필요가 없다(좁은 화면은 판세가 위로 얹힌다).
    장부 뷰(스쿼드·달력·재정·대회·커리어)는 **오른쪽 위 아이콘 줄**에서 연다.
    예전 3단 구조(상단·탭바·본문)에서는 채팅이 여섯 탭 중 하나로 보여 화면의 주인이
    흐렸다. 아이콘은 이모지가 아니라 단색
    SVG(`components/icons.tsx`, 24 그리드·stroke 1.7·`currentColor`) — 이모지는
    플랫폼마다 모양·크기가 달라 줄이 맞지 않고 팔레트를 무시한다.
  - **장부는 채팅을 대신하지 않고 나눠 쓴다** (`.stage-split.panel-split`). 폭이
    **1200px 이상**이면 왼쪽 채팅 · 오른쪽 장부로 반반이다. 통째로 덮던 때는 명단을
    확인하려고 연 순간 대화가 사라져서, "방금 코치가 뭐라고 했더라"를 확인하러 다시
    채팅을 열어야 했다. 그 아래 폭에서는 나란히 두면 **둘 다 못 읽으므로**(채팅 한 줄이
    스무 자로 끊기고 명단 표 560px가 가로로 스크롤된다) 장부가 무대를 대신한다.
    ⚠️ **갈림은 CSS가 정한다** — 폭을 자바스크립트로 재면 첫 렌더가 서버와 어긋나고
    창을 줄일 때마다 다시 그린다. 마크업은 언제나 둘이고 좁을 때 한 열이 **접힌다**
    (사라지는 게 아니라 접히는 것이라 스크롤 위치와 쓰던 입력이 그대로 남는다).
    같은 이유로 채팅 자동 스크롤에서 `panel === null` 조건을 걷어냈다 — 옆에 남아
    있는 채팅에 도착한 턴이 스크롤 밖에 쌓인다.
  - **스쿼드는 명단이 먼저, 전술판은 덮는다** (`SquadView`의 `boardOpen`). 스쿼드가
    통째로 반쪽에 들어오면 전술판이 200px로 눌려 칩을 끌 자리가 없다. 감독이 채팅을
    보며 곁눈질하는 것은 대개 **명단**이고(누가 부상인가, 폼이 어떤가), 판을 만지는
    것은 그 자체로 하나의 일이다. 그래서 명단이 오른쪽에 서고, 손잡이로 판을 펼치면
    그때 채팅을 덮고 화면을 통째로 쓴다. 선택은 기억한다 — 탭을 오갈 때마다 접히면
    판을 쓰는 감독이 매번 다시 편다. ⚠️ **접힘은 채팅이 옆에 설 만큼 넓을 때만
    뜻이 있다** — 그 아래에서는 접어도 얻는 자리가 없고 명단만 늘어나 열 사이가
    벌어지므로, 손잡이도 1200px 이상에서만 선다(눌러도 아무 일이 없는 버튼을 남겨
    두지 않는다).
  - **디자인 토큰은 `globals.css`의 `:root`에 있다** — 간격 4의 배수(`--sp-*`),
    모서리 4단계(`--r-*`), 글자 9단계(`--fs-3xs`~`--fs-2xl`), 전이 속도(`--ease`).
    눈대중으로 고른 값(12px과 12.5px, 8px과 10px 모서리)은 위계로 읽히지 않고 줄이
    안 맞아 보일 뿐이다. **표면의 높이는 그림자가 아니라 밝기**로 나타낸다
    (bg → panel → panel-2) — 어두운 화면에서 그림자는 배경에 묻힌다.
  - **선수 상세의 기준 폭은 화면이 아니라 명단 칸이다** (`.roster-scroll`이
    `container-type: inline-size`). 같은 1024px 화면이라도 2열이면 명단이 576px,
    한 열이면 990px다 — 뷰포트로 재면 그 차이를 못 본다. 칸이 좁으면 포지션 줄을
    **아래로 내리고**(`flex: 1`로 눌러 담으면 안의 칩이 51px 밑으로 안 줄어 그대로
    삐져나온다) 능력치 축을 5 → 4 → 3열로 접는다.
  - **선수 상세는 카드가 아니라 그 행 아래로 파인 면**이다 — 테두리·라운드·강조 띠
    같은 장식 없이 한 단계 가라앉은 배경으로 "펼쳐진 속"처럼 읽히고, 글자 크기와
    구분선 톤도 표와 맞춘다. 표 위에 뜬 패널처럼 보이면 시선이 명단을 떠난다.
  - **칸(선발·벤치·예비·2군)은 행의 왼쪽 선 색**이 말한다 — 초록/파랑/회색/흐린 회색.
    글자로 적는 배지 열을 오른쪽 끝에 두면 142px을 먹고 시선도 이름에서 멀다.
    ⚠️ **띠만으로는 부족하다** — 2px 선은 표의 가장자리처럼 보여서 선발과 벤치의
    경계가 눈에 안 들어오고, 색과 뜻을 잇는 범례도 없다. 그래서 **칸이 바뀌는
    자리에 구역 이름**을 세운다(`tr.tier-head` — 순위표가 진출권 구역을 다루는
    방식과 같다: 경계선 + 이름). 띠는 각 행이 어디 속하는지를 이어서 말하고,
    머리 한 줄이 "이 색이 무슨 칸인지"를 말한다. **칸 순 정렬일 때만** 그린다 —
    OVR·나이로 정렬하면 칸이 흩어져 구역이 성립하지 않는다.
    교체 화살표와 상태 배지는 이름 칸으로, 벤치 지정은 **상세 카드**로 옮겼다.
  - **명단 표는 골 대신 평점**을 보여준다 — 득점은 자리에 따라 뜻이 갈리지만 평점은
    모든 자리를 같은 잣대로 비교할 수 있다. 골·도움은 행을 펼치면 시즌 기록에 있다.
  - **OVR은 자리마다 다르다 — 화면도 그걸 보여준다.** `overall`은 **주 포지션**
    가중치의 파생이라 센터백을 윙에 세워도 안 움직이는데, 경기에 쓰이는 값은
    **배치된 자리**의 `roleFit`이다(`slotStrength`). 그 간극을 감독이 못 보면
    자리를 옮긴 대가가 결과에만 나타난다. 그래서 뷰가 `slotOverall`(맡은 자리 기준 —
    요구 역량이 주 포지션과 같으면 `null`)과 `positions[].overall`(자리별 전력)을
    함께 실어 보내고, **전술판 칩·명단 OVR·선발 평균이 모두 자리 기준**이다(툴팁이
    주 포지션 값을 함께 밝힌다). 상세의 포지션 목록엔 자리마다 전력이 붙는다.
    ⚠️ **안개는 축에만 씌우고 합성값은 거기서 파생시킨다.** 예전엔 서버가
    `overall`에 한 번, 자리별 전력에 또 한 번 **값마다 따로** 오차를 굴려 내려보냈다.
    그러면 화면은 참값이 없어 같은 규칙을 재현할 수 없고, 자리를 옮겨 보려면
    "서버 값에 차이만 얹는" 보정을 해야 했다 — 그 보정이 명단과 전술판에서 갈려
    같은 선수의 OVR이 두 숫자로 보였다(예비 선수를 판에 끌어 넣으면 명단은 종합값에
    눌러앉고 칩만 자리 값으로 뛰었다). 선발 평균은 아예 `overall`을 더하고 있어
    센터백을 윙에 세워도 꿈쩍하지 않았다.
    지금은 규칙이 하나다 — **관측된 축의 `roleFit` + 관측 오프셋 하나**
    (`observedFit` / `apps/web/lib/slot-overall.ts`). 서버가 `SquadViewRow.observation`
    으로 **안개 자체를**(지식 수준·오차 폭·오프셋) 실어 보내므로 화면이 어느 자리로
    옮겨 계산해도 서버와 어긋날 수 없고, **참값은 여전히 안 나간다.** 오프셋이
    자리마다 같아서 **자리끼리의 비교도 흔들리지 않는다** — 오차만으로 "주 포지션보다
    나은 자리"가 생겨나지 않는다.
    표시용 종합만은 예외로 **저장값(`attributes.overall`)에 오프셋만** 얹는다:
    관측 축에서 `bestOverall`을 다시 굴리면 생성 시점 목록으로 계산된 저장값과
    갈려 리그 전체 눈금이 부수효과로 움직인다.
    계산을 화면이 아니라 `lib`에 두는 이유는 **단위 테스트** 때문이다 — 새 게임엔
    안개(적응 중인 새 영입)가 자연히 없어 e2e로는 이 갈림을 만들 수 없다.
    **정확도는 화면에 나온다** — OVR 옆 `±N`(`Margin`, 오차 0이면 그리지 않는다)과
    선수 상세의 `정보 적응 중인 새 영입 ±3` 한 칸. 예전엔 `?` 하나뿐이라 갓 데려온
    선수와 거의 다 적응한 선수가 같은 표식을 달고 있었다.
  - **전술판 칩은 자리마다 색이 다르다** — 최전방 붉게 · 중원 초록 · 수비 파랑 ·
    골키퍼 노랑. 판을 훑을 때 라인이 색 띠로 먼저 읽힌다. 색의 근거는 선수의 주
    포지션군이 아니라 **지금 서 있는 자리**다 — 센터백을 윙에 세우면 그 칩은
    최전방 색이어야 판이 실제 배치대로 읽힌다.
    ⚠️ **색은 배경에, 상태는 테두리에** — 두 축을 섞지 않는다. 테두리는 이미
    선택(초록)·못 뛰는 선수(빨강)가 쓰고 있어서 자리 색까지 얹으면 초록 테두리가
    "고른 것"인지 "미드필더"인지 알 수 없다. 그래서 자리 색 규칙은 상태 규칙보다
    **앞에** 놓아 테두리를 양보한다(순서가 곧 우선순위다).
    ⚠️ **색은 잔디 위에 얹는다** — 칩 배경을 색으로 갈아 끼우면 초록 잔디가 비쳐
    들어 미드필더 칩이 판에 묻히고 붉은 칩은 탁한 갈색이 된다. 어두운 바탕
    (`--chip-base`) **위에** 색 층을 올린다. 중원만 농도가 높은 것도 같은 이유다 —
    잔디와 같은 계열이라 다른 색과 같은 농도로는 갈리지 않는다.
    **역할 약칭(`slot-role`)도 같은 색을 쓰고 톤만 다르다** — 카드보다 진하게 얹어
    "이 카드 안의 것"으로 읽힌다. 예전엔 전원이 초록 하나였다: 붉은 공격수 카드
    위에서는 색이 부딪히고 초록 중원 카드에서는 배경에 묻혀 약칭이 안 보였다.
    글자색은 균일하게 두지 않는다 — 초록·노랑은 그 농도에서 밝아 **어두운 글자**가,
    빨강·파랑은 어두워 밝은 글자가 읽힌다. 일관성보다 대비가 먼저다.
    (선수 상세의 역할 알약은 그대로다 — 거기 색은 **고른 것**을 뜻하는 상태 채널이라
    자리 색을 얹으면 선택 표시와 부딪힌다.)
  - **표식은 한 물건이다** — 이름 뒤에 붙는 자격(HG·U21)과 상태(부상·정지·불만·
    이적 리스트)가 **같은 크기·모양**(`.tag`)을 쓴다. 예전엔 상태만 `.badge`
    (11px·테두리)라 한 칸 안의 표식들이 서로 다른 물건처럼 보였다. 갈리는 것은
    **톤뿐**이다: 자격은 각주처럼 가라앉히고, 감독이 지금 손을 써야 하는 상태는
    색을 살려 앞에 세운다. 크기로 세우지 않는 이유는 줄이 흔들리면 명단을 훑는
    눈이 이름을 잃기 때문이다.
  - **적응도는 숫자가 아니라 채워진 원**(`FitGauge`). 감독이 알고 싶은 건 74와 76의
    차이가 아니라 "이 자리를 아는가"이고, 숫자로 두면 OVR·나이·평점과 뒤섞여 세로
    비교가 안 된다. 정확한 값은 툴팁이 갖는다.
    **무엇을 그리는지는 그 화면이 무엇을 정하는 곳인지가 정한다** — 명단은 자리와
    전술을 합친 `adaptation`(그 선수가 지금 얼마나 준비됐나), **전술판 칩은 포지션
    적응도**다. 전술 적응도는 팀 전체에 걸린 상수라 칩마다 같은 값이 섞이면 "누구를
    어느 자리에 세우나"라는 자리끼리의 비교만 흐려진다.
    ⚠️ 전술판 칩의 게이지는 **이전 두 자리 숫자와 같은 폭**이어야 한다. 칩이 넓어지면
    배치가 통째로 흔들린다(자유 배치 e2e가 잡았다).
  - **선수 상태는 `체력` 하나**(0~100, 높을수록 좋다)와 **심경 한 줄**로 보인다.
    예전엔 사기와 피로를 따로 저장하고 화면에서만 합쳐 보여줬는데, 감독이 읽는 것도
    판단에 쓰는 것도 결국 "이 선수 지금 쓸 만한가" 하나여서 **저장도 한 축으로 합쳤다**
    (`PlayerState.condition` — 옛 세이브는 로드 때 두 값을 합쳐 옮긴다). 경기가
    깎고, 휴식이 채우고, 팀토크·면담이 올리고, 방치된 불만이 매일 갉는다. 왜 낮은지는
    `describeMood`가 말한다. 명단에서 선수를 누르면 그 행 아래에 상세가 펼쳐지고
    15축 능력치가 전부 나온다.
    ⚠️ **한 축으로 합쳤다고 이 축에서 감정까지 읽으면 안 된다.** 경기 한 판이
    30~50을 가져가므로 경기 다음 날은 누구나 바닥이다 — "체력이 낮다 = 사기가
    낮다"로 읽는 순간 **이긴 다음 날 선수단 전원이 침울해지고 팀을 떠나고 싶어
    한다**. 실제로 그랬다: 심경 줄이 "몸이 무겁고 **표정도 어둡다**"였고,
    이적 확률·재계약 확률·들어오는 오퍼가 전부 `condition < 45`를 "마음이 떴다"로
    읽고 있었다(승리가 얹는 +4는 그 낙폭에 묻힌다).
    지금은 **몸은 몸의 말로, 마음은 마음의 말로** 쓴다 — 마음의 근거는 `hasIssue`
    (라커룸 불만)·폼·정착·출전 기회고, 체력은 감정어 없이 사실로만 곁들인다
    ("다리가 무겁다"). 심경 줄은 **직전 경기의 결과와 그 선수의 평점**을 먼저
    읽어(3일까지) 팀 결과와 개인 활약을 갈라 말한다 — 이긴 경기에서 부진한 선수와
    진 경기에서 제 몫을 한 선수는 마음이 다르다.
    ⚠️ **폼 문턱은 `formLabel`과 같은 눈금을 쓴다** — 폼이 −3~3 정수이던 시절의
    ±1·±2.2가 심경 줄에 남아 있어 그 가지가 통째로 죽어 있었다(정의역이 −1~+1이라
    ±2.2는 영영 오지 않는다). 잘나가는 선수의 심경이 비던 이유다.
    같은 결로 **출전 기회 불만은 개막 뒤에만** 말한다 — 프리시즌엔 아무도 뛴 적이
    없어 벤치 전원에게 걸렸다.
    **그 한 줄은 코어가 앵커를 박고 결산이 맥락으로 다시 쓴다** (`mood-rater` —
    경기 평점·훈련 결산과 같은 계약, 싼 티어). 규칙만으로는 늘 같은 문장이 나온다:
    같은 "불만"도 벤치에서 반 시즌을 보낸 고참과 이적 요청을 넣은 주전이 다르고,
    같은 "승리의 여운"도 결승골을 넣은 선수와 5분 뛴 선수가 다르다. **사실은 코어가
    잡고 결만 맡긴다** — 불만이 걸린 선수의 문장에 그 말이 없으면 버리고, 여러
    문장·긴 문장·대상 아닌 선수도 버린다. 버려지면 앵커가 남아 빈 줄이 생기지 않고
    시간 진행도 막히지 않는다. **대상은 그 구간에 사건이 있었던 선수 여덟 명까지**
    (`MOOD_BATCH`) — 못 뛰는 선수는 앵커가 이미 정확해서 빼고, 43명을 매번 태우지도
    않는다. 문장은 `GAME_PLAYER.state.moodNote`에 저장한다(대화·사건이 원본이라
    파생할 표가 없다 — `SETTLING_EVENT`와 같은 이유, optional이라 버전은 그대로).
    **열흘이 지나거나 부상·정지가 생기면 코어가 이긴다** — 지난주의 결이 오늘의
    심경인 척하면 화면이 거짓말을 한다.
    ⚠️ **로테이션은 이 축이 만드는 결정이다** (attribute-model §4.2 · match-sim §2.1).
    한 경기는 자리 × 전술 × 지구력 × 뛴 시간만큼 깎는다 — 90분 기준 중원 −65 ·
    풀백 −62 · CB −44 · GK −18이고 게겐프레스면 중원이 −97까지 간다. 그 위에
    **그날의 몫**(`drainVariance` ±12%)이 곱해진다 — 잠·이동·상대처럼 모델링하지
    않지만 늘 있는 것들의 자리라 같은 미드필더가 라운드마다 −60~−72를 오간다.
    `시드:경기:선수` 해시라 **결정적**이고 **경기 내내 고정**이다(구간마다 굴리면
    평균으로 상쇄되고 막대만 덜컹거린다). 폭은 지구력(±25%)보다 좁게 둔다 — 운이
    능력을 덮으면 스쿼드를 짜는 판단이 흐려진다. ⚠️ 해시는 FNV-1a만으론 부족하다:
    하위 비트 확산이 약해 라운드 숫자만 다른 키가 한쪽으로 몰리고, 그러면 편차가
    "얘는 원래 잘 지친다"는 숨은 능력치로 굳는다 (murmur3 마무리 믹스 필수). **경기 후
    정산도 그 값 그대로**(`pendingMatch.matchFatigue`)다: 상수 −34를 물리던 때는
    90분 뛴 윙백과 85분에 들어간 교체 선수와 골키퍼가 똑같이 지쳤고, 사흘이면
    전원이 100으로 돌아와 **"누구를 쉬게 할까"라는 판단 자체가 게임에 없었다**.
    같은 장부에서 **상대 팀도 함께** 정산한다 — 예전엔 우리를 상대한 클럽만 주중
    연전을 공짜로 소화했다. **회복도 같은 축(`stamina`)이 정한다**(`dailyRecovery` —
    본훈련 8 · 휴식·경기일 13 · 회복 세션 16). 그래서 **주 1경기는 로테이션을
    강요하지 않고**(엿새 +74) **사흘 간격은 강요한다**(+42 → 60대 킥오프). ⚠️ 회복을
    이보다 낮추면 주 1경기만으로도 시즌 내내 내려가 12월에 스쿼드가 눕는다.
    ⚠️ **회복의 지구력 편차는 ±10%로 소모(±25%)의 절반 아래다** — 한 축이 양쪽에
    곱으로 걸리면 격차가 복리로 벌어져 지구력 하나가 나머지 열네 축을 덮는다.
    ±20%였을 땐 지구력 30과 99가 사흘 뒤 54와 94로 갈렸다. 회복은 소모가 만든
    차이를 거들 뿐이다.
  - **폼은 숫자가 아니라 시기로, 그리고 추이로 보인다** — 라벨
    (`절정`·`상승세`·`평소`·`침체`·`바닥`) + **각도로 도는 화살표**, 상세에는
    **최근 5경기 평점**이 왼쪽(오래된)부터 늘어선다. 폼 숫자 하나로는 "오르는 중"과
    "식는 중"을 구분할 수 없다. 화살표는 **도형 하나(SVG)를 `formAngle`만큼
    돌린 것**이라 눈금 없이 연속이고, **절정(+1)에서만 12시**를 본다(평소 3시 ·
    바닥 6시). 유니코드 화살표는 단계로 끊기고 `⇑⇓`만 폴백 폰트로 빠져 굵기가
    어긋났다. 각도·경계는 UI가 아니라 `engine/form.ts`가 정하고 뷰가 실어 보낸다.
  - **시계는 화면에 하나뿐이다.** 정확한 시각(`2026-08-15 PM 2:30`)은 **상단 띠**가
    갖고, 채팅의 장면 표시는 **날짜 + 때**(`2026-08-15 오후`)로 접는다 —
    새벽·아침·오전·오후·저녁·밤. 채팅이 알려야 하는 건 "장면이 언제로 넘어갔나"뿐이라
    눈금이 굵을수록 좋고(오전 → 오후가 12:10 → 12:40보다 훨씬 큰 사건이다), 분까지
    세우면 같은 화면에 시계가 둘 서서 같은 것을 두 번 말한다. **바뀔 때만** 선다.
    ⚠️ 경기 중에도 **시계는 흐른다** — 날짜만 막는다(`applyScenePoint`). 통째로
    묶어 두면 상단이 킥오프 시각에 얼어붙어 채팅의 장면과 어긋난다(09:00에 멈춘
    세이브를 봤다). 경기 중에 하루가 넘어가면 훈련·성장·협상이 통째로 굴러 버리므로
    막아야 할 것은 날짜뿐이다.
  - **시간은 채팅이 아니라 손잡이로 넘긴다** (`TIME_SKIPS` — 하루·일주일·다음 경기).
    매번 "다음 경기로 가자"를 타이핑하게 두면 GM이 대신 "시간을 보내시겠습니까?"
    하고 되묻는 장면으로 턴을 닫는다 — 감독이 알아서 할 일을 세계가 물어보는 꼴이라
    GM 프롬프트에서 그 되묻기를 막고 손잡이를 감독 쪽에 뒀다. 눈금이 셋인 건
    프리시즌 때문이다: 7월엔 다음 경기가 몇 주 뒤라 "다음 경기로"만 있으면
    이적·훈련을 들여다볼 틈 없이 개막까지 날아간다.
    버튼은 **엔진과 채팅을 둘 다 돈다** — 엔진만 돌리면 날짜만 바뀌고 세계는 아무
    말도 하지 않는다. 경기 중에는 숨긴다(그때 시간은 경기가 민다).
    ⚠️ **손잡이의 시간만은 모델보다 먼저 흐른다** (`parseTimeSkip` → `advanceForSkip`).
    다른 시간 이동은 모델이 첫 줄 헤더에 시점을 적고 코어가 따라가지만
    (`applyScenePoint`), 그 구조에서는 일주일을 넘긴 턴에 모델이 **그 일주일에 무슨
    일이 있었는지 모른 채** 장면을 쓴다 — 코어의 digest는 화면 기록으로만 남고
    이력에 실리지 않아, 부상도 오퍼도 감독은 다음 턴에야 전해 듣는다. 감독이 얼마를
    넘길지 이미 정해서 누른 손잡이에는 물어볼 것이 없으므로, 코어가 먼저 굴리고 그
    사이의 일을 상태 스냅샷에 실어 보낸다(`buildGmStateNote`의 "그 사이 벌어진 일").
    모델은 도착한 자리에서 **보고**를 하고, 그 턴의 헤더는 날짜를 또 밀지 못한다
    (안 그러면 하루를 눌렀는데 이틀이 간다). 클라이언트의 조작 문장을 서버가 되읽는
    구조라 문구를 바꾸면 파서도 함께 고쳐야 한다 — agents 테스트가 문장을 고정한다.
    **"다음 경기"에는 날짜를 실어 보낸다** — `@: [시간 진행 — 다음 경기 (2026-08-15)]`.
    클라이언트는 그 날짜를 이미 알고 있으므로(달력 뷰의 `isNext`) 모델이 일정을
    조회해 옮겨 적을 필요가 없다. 남은 경기가 없으면 버튼을 그리지 않는다.
    ⚠️ **화면 조작과 감독의 말은 데이터에서부터 갈린다** (`ChatTurn.role`의
    `operator`). 손잡이를 누른 것이지 감독이 "하루만 넘기자"라고 말한 게 아니므로
    ① 채팅에 그리지 않고(낙관적 유저 턴도 띄우지 않는다) ② **모델 이력에도 감독
    발화가 아니라 조작으로** 들어가며(`buildOperatorMessage` — `@감독이름:` 대신
    **화자 없음** `@: [시간 진행 — 하루]`. 표기를 새로 만들지 않는 이유는
    `@:`가 이미 "화자 없음"이라 감독 발화가 아니라는 사실이 설명 없이 문법에서
    드러나기 때문이다. 버튼이 보내는 문장도 구어체가 아니라 **조작의 이름**이다)
    ③ 훈련 결산
    브리프의 대화 목록에서도 빠진다. 플래그로 화면에서만 숨기면 이력에는 여전히
    `@감독: 하루만 넘기자`가 쌓여 GM이 거기서 감독의 말투와 의도를 읽는다.
    지우지 않고 남기는 이유는 따로 있다 — 없애면 GM이 왜 갑자기 사흘이 지났는지
    모른 채 다음 장면을 쓴다. role 값이 하나 는 것뿐이라 세이브 버전은 그대로다.
  - **시각은 접고 화자는 접지 않는다.** 시각(`scene-stamp`)은 값이 달라질 때만 서서
    장면의 경계가 된다 — 턴마다 반복되면 시간이 흐른다는 신호가 오히려 죽는다.
    ⚠️ 그 신호가 살려면 **시각이 실제로 흘러야 한다**. 예전 프롬프트가 "감독이
    진행을 요청하지 않았으면 같은 날 **같은 시각**에 머물러라"라고 시켜서, 날짜
    점프를 막으려던 문장이 시각까지 얼렸다 — 대화가 몇 턴을 이어져도 시계가
    `AM 9:00`에 못박혀 경계가 한 번도 서지 않았다. 지금은 **페이싱은 모델이,
    브레이크는 코어가** 쥔다: 시계는 그 장면이 걸린 만큼 흐르고(이어지는 대화는
    몇 분 · 자리를 옮기면 한두 시간 · 하루가 저물었으면 다음 날 아침), **날짜를
    넘기는 것도 막지 않는다** — 지나치면 안 되는 것 앞에서는 `advanceTime`이 이미
    멈춰 세우므로(경기일·시즌 종료·기한 당일 협상) 프롬프트가 같은 일을 또
    지시하면 페이싱만 굳는다 (game-loop.md §3).
    반대로 **화자 이름은 턴 단위로 늘 선다**: 감독의 말과 코치의 답이 번갈아 오는
    화면에서 이름이 빠진 턴은 누가 말하는지를 위쪽까지 거슬러 찾아야 한다.
    (한 턴 안에서 같은 사람이 이어 말할 때 묶는 `groupUtterances`와는 다른 이야기다)
  - **하단은 한 덩어리이고, 땅은 대화와 같다.** 시간 이동 줄과 입력창이 각자 띠를
    가지면 화면 아래가 두 겹으로 보인다 — `.composer` 하나가 여백을 갖고 둘을 담는다.
    그리고 그 바닥은 `--panel`이 아니라 **대화와 같은 `--bg`** 다: 상단 바 같은
    크롬이 아니라 대화의 연장이라, 밝은 블록으로 칠하면 화면이 위아래로 갈린다.
    떠 있어야 할 것은 **입력칸 하나**다.
    입력창도 **하나의 물건**이다: 테두리를 바깥이 갖고 전송 버튼을 그 안에 넣으면
    "여기에 쓰고 여기서 보낸다"가 한눈에 읽힌다(예전엔 네모난 입력칸 옆에 큼직한
    초록 버튼이 따로 서서 두 덩어리로 읽혔다).
  - **스크롤바는 트랙을 비우고 손잡이만 남긴다.** 어두운 화면에 브라우저 기본이
    그대로 뜨면 밝은 띠가 되어 시선을 뺏는다. 손잡이에 투명 테두리를 두르고
    `background-clip: content-box`로 잘라 내면 **폭은 유지한 채**(잡기 쉽다)
    보이는 두께만 얇아진다.
  - **되풀이되는 라벨에 강조색을 쓰지 않는다.** 화자 이름을 원색 초록(`--accent`)으로
    뒀더니 턴마다 강조색이 깜빡여 읽기 피로했다. 이름은 **굵기와 자리**로 이미
    머리글이므로 색은 한 걸음 물러난다(`--silver`) — 강조색은 누를 수 있는 것과
    지금 상태에 남겨 둔다.
  - **읽는 폭이 곧 대화 열의 폭이다.** 본문만 좁히고 입력창은 넓게 두면 두 덩어리의
    오른쪽 끝이 어긋나 화면이 어수선해진다 — `.chat-pane` 하나가 폭을 정하고
    말풍선·입력창이 그 폭을 공유한다(한국어 15px에서 780px ≈ 한 줄 46자).
  - **기다리는 중을 문장으로 적지 않는다.** "세계가 반응하는 중…"을 띄우던 때는
    매 턴 같은 문장이 대화 사이에 끼어 **대사인 척**했고, 그 자리에 곧 진짜 대사가
    들어오면서 화면이 덜컹거렸다. 답이 올 자리에서 **점 세 개**가 조용히 뛰면 된다
    (`role="status"` + `prefers-reduced-motion` 존중).
  - **화면 조작은 모아 두었다가 한 번에 읽힌다** (`state.pendingEdits` ·
    `recordEdit`/`takeEdits`). 전술판·명단·역할 편집은 채팅 턴을 만들지 않는데,
    그렇다고 아무 말도 없으면 모델은 감독이 무엇을 바꿨는지 모른 채 이야기를
    잇는다(배치는 컨텍스트에 없고 조회 도구로만 안다). 그래서 조작마다 결과 한
    줄을 쌓아 두고 **다음 발화 때** 상태에 실어 보낸다 — "이미 반영됨, 도구로
    다시 하지 마라"와 함께. **같은 대상은 접힌다**: 역할을 세 번 눌러도 마지막
    한 줄이고, 전술판을 열 번 저장해도 `lineup` 한 줄이다. 과정이 아니라
    결과가 모델이 반응할 거리다. 턴이 실패하면 비우지 않아 다음에 다시 읽힌다.
  - **결과를 보여주는 채널은 셋이고, 무엇을 어디에 둘지가 규칙이다.**
    ① **채팅 카드** — 감독이 결정을 내리려고 원본 숫자를 봐야 하는 것(스카우팅
    보고서·오퍼 조건·딜 확률). ② **접히는 칩** — 자기가 지시한 것의 확인. 이름은
    영문 식별자가 아니라 **사람이 읽는 이름**(`SKILL_LABEL`)이고, 펼치면 줄글을
    항목으로 편다(`A · B` 꼴을 끊어 `이름/값`으로 나눈다). 대화형(면담·팀토크·
    기자회견)은 **왼쪽 점 하나로 결만** 보인다 — 사기 ±N을 늘 세우면 대화가 숫자로
    읽히고, 그렇다고 아무 표시도 없으면 잘 풀렸는지 알려고 매번 펼쳐야 한다.
    ③ **레일 말풍선** — 장부가 바뀌었다는 알림(`panelHintsOf`). 라인업은 스쿼드
    아이콘 아래, 훈련은 달력 아래에 한 줄로 서고 **그 화면을 열면 사라진다**.
    채팅에 카드로 그리면 무대의 주인이 서사에서 대시보드로 넘어가고, 아무 표시도
    없으면 지시가 먹혔는지 확인하러 탭을 열어야 한다.
    ⚠️ 표시 이름 표는 **웹이 갖는다** — 스킬 카탈로그를 클라이언트에서 import하면
    `node:path`가 딸려 와 번들이 깨진다. 어긋남은 테스트가 지킨다.
  - **표시는 벌어진 자리에 선다** (`apps/web/lib/turn-pieces.ts`). 골 카드도 스킬
    칩도 턴 맨 앞에 몰아 두면 감독은 결과를 먼저 보고 장면을 거꾸로 읽는다 — 한 턴에
    두 골이 들어가거나 스킬이 여럿 걸린 구간에서는 어느 문장이 어느 사건이었는지도
    흐려진다. 자리를 아는 방법은 **둘이고 둘 다 기록에서 온다**: 골·경고는 중계 줄의
    **분**(`23′`)으로, 스킬은 **호출 시점의 줄 수**(`ToolCallRecord.line`)로 선다.
    갈리는 이유는 만들어지는 순서다 — 사건은 굴린 **뒤에** 중계가 쓰이고, 스킬은
    코치가 "조정하겠습니다"라고 답한 **뒤에** 불린다.
    ⚠️ 줄 수는 **어댑터가 이어 붙인 본문**에서 센다(`ToolCallContext.text`).
    스트리밍 델타를 세면 안 된다 — 어댑터는 반복마다의 텍스트를 `\n`으로 잇는데 그
    줄바꿈은 스트림에 흐르지 않아 반복 경계에서 두 줄이 한 줄로 합쳐진다. 글자 수가
    아니라 줄인 것도 같은 결이다: 본문은 저장 전에 손질되지만(선수 id → 이름) 줄은
    그대로다. 코어가 스스로 남긴 기록(시계 이동·경기 마감)과 옛 세이브에는 자리가
    없고, 없으면 지금까지처럼 맨 앞에 선다. 같은 자리의 칩들은 한 줄에 나란히 서고
    (칩마다 문단을 끊으면 장면이 토막 난다), 말 묶음은 표시 자리에서 끊는다.
  - **스킬 칩은 감독이 부른 것만 세운다** (`ToolCallRecord.silent`). 시계 이동처럼
    코어가 한 일은 기록으로 남되 칩이 되지 않는다 — 어드민 스킬 목록에 없는
    이름이 칩으로 뜨면 유령 스킬이 호출된 것처럼 읽힌다. **이름으로 거르지
    않는다**: 문자열은 바뀌면 조용히 깨진다(옛 세이브 호환용 이름 비교만 예외).
  - **UI에 조작법 안내 문구를 넣지 않는다.** "…를 누르세요", "…하면 교체됩니다" 같은
    설명 배너·힌트는 화면을 어지럽히고 금방 실제 동작과 어긋난다. 조작은 **눈에 보이는
    것**으로 알린다 — 선택된 칩 하이라이트, 행에 뜨는 화살표 버튼, 비활성 버튼의 툴팁.
    (경기 중 잠김처럼 **왜 안 되는지**를 밝혀야 할 때만 예외)
  - **읽는 것과 고르는 것은 생김새가 달라야 한다.** 안내 문구를 안 쓰는 만큼 모양이
    그 몫을 진다. 선수 상세에서 포지션과 역할이 둘 다 같은 알약이던 때, 감독이
    포지션을 눌러 보고 고장인 줄 알았다 — 하나는 읽는 값이고 하나는 조작인데
    똑같이 생겼기 때문이다. 지금은 **포지션은 테두리 없는 글자**, **역할만 알약 +
    손가락 커서 + 눌리는 반응**이다. ⚠️ 그 줄에서 **테두리를 가진 건 역할뿐**이어야
    한다 — 지금 선 자리를 상자로 표시했더니 그것도 눌리는 것처럼 보였다(지금은 밑줄).
  - **포지션 칩은 색이 등급, 밑줄이 위치다 — 두 축을 섞지 않는다.**
    선호 **금색**(`--gold-soft`) · 소화 가능 **은색**(`--silver`) · 목록에 없는데
    세워 둔 자리 **꺼진 회색**(`--dim`), 그리고 지금 맡은 자리에는 **파란 밑줄만**
    얹는다. 지금 자리라고 글자까지 파랗게 바꾸면 그 자리가 선호인지 무리한
    배치인지가 화면에서 사라진다. ⚠️ 못 보는 자리에 `--muted`를 썼더니 은색과
    밝기가 비슷해 헷갈렸다 — 여기 필요한 건 "조금 흐림"이 아니라 "꺼져 있음"이다.
  - **전술판 칩의 역할 표기는 기본 역할이 아닐 때만** 뜬다(FM 약칭 `BPD`·`IWB`·`RGA`).
    열한 칩이 전부 `센터백`·`풀백`·`윙어`를 달고 있으면 읽히지 않는다 — 감독이
    실제로 **고른** 것만 보여야 판을 훑을 때 그 선택이 눈에 남는다. 아이콘을 쓰지
    않는 이유도 같다: 역할이 50종이라 `레지스타`와 `딥라잉 플레이메이커`를 가를
    그림이 없고, 약칭은 이미 감독이 아는 말이면서 칩에 들어갈 만큼 짧다.
  - **제목이 이미 말한 것을 부연하지 않는다.** "리그컵 1라운드 대진 추첨"에 "이날 다음
    상대가 정해진다"를 덧붙이는 식의 설명문은 새 정보가 없는데 자리만 차지한다.
    부제·상세 줄에는 **제목에서 읽을 수 없는 사실**만 쓴다 — 득점자, 이적창 기간,
    훈련 focus 축. 쓸 게 없으면 `null`로 두고 아무것도 그리지 않는다. 빈 상태도
    마찬가지다: "일정 없음"이면 충분하고, 뒤에 무엇을 하라는 안내를 붙이지 않는다.
  - **라인업 변경 경로는 둘뿐이고 각각 한 가지 뜻만 갖는다.**
    ① **칩을 끌어** 다른 칩 위에 놓으면 두 자리를 맞바꾼다 (클릭으로는 안 바뀐다 —
    누르는 건 고르기·상세 보기일 뿐). ② 선수를 하나 고르면 **칸이 다른 모든 행**에
    화살표가 떠서 서로의 칸을 맞바꾼다 — 칸은 `선발 / 벤치 / 예비 / 2군` 넷이고
    **어떤 조합이든 열린다**(2군↔선발이면 승격·강등이 함께 일어나 1군 인원은 그대로다).
    위로 올라오면 `←`, 내려가면 `→`. 같은 칸끼리만 닫힌다 — 선발 자리 맞바꾸기는
    ①의 드래그가 맡기 때문이다. 명단 행 클릭 자체는 **상세 보기뿐**이다.
    1·2군 이동은 라인업과 **한 요청**으로 저장된다(`squadLevels` — 승격 → 배치 →
    강등 순). 나눠 보내면 "승격은 됐는데 배치는 실패"한 반쪽 상태가 남는다.
  - **전술 적응도의 기억은 선수마다 따로다** (`TacticAssignment.drilled`). 팀 평균
    한 숫자로 기억하던 때는 개인 보정이 평균을 벗어나면 그 값이 기억돼 **왕복만으로
    적응도가 불어났고**(70 → 100을 봤다), 그래서 모든 보정을 재분배로 짜야 했다 —
    적응도가 "나한테 맞나"가 아니라 "남들보다 맞나"가 된 이유다. 이제 각자 자기
    기억에서 도착하고 **왕복은 기억이 닫는다**. 팀 적응도는 그 평균(파생)이다.
    **보정은 결과에 더하지 않고 거리에 넣는다**(`personalDistance` — 습득력 ×
    적합도) — 더하면 도착 수준이 기억과 어긋나 다시 샌다. **해 본 전술이면 그
    기억만 쓴다**(전이 후보와 섞지 않는다). **새 영입은 기억을 갖고 온다**
    (`memoriesOf` — 직전 소속 팀의 전술과 그 팀이 익힌 수준): 첼시에서 온 선수는
    첼시 축구를 이미 알고, 우리가 그 전술로 바꾸면 **오른다**. 그래서 세 전술을
    돌려막으면 셋 다 익고, 바닥은 매번 새 설정으로 옮겨 다닐 때 온다.
    **기억은 안 쓰면 옅어지고 그 속도도 선수마다 다르다** — 기본 14일마다 1에
    `memoryRetention`(습득과 같은 축)이 곱해져 0.7~1.5배로 갈린다. 빨리 그리는
    선수가 오래 갖고 있어야 개인차가 상쇄되지 않는다.
  - **화면 조작은 자동 저장 한 번으로 모인다.** 좌표·벤치 지정·1·2군 이동·**세부
    역할**이 모두 같은 요청(`/lineup`)에 실린다 — 역할만 따로 `/role`을 클릭마다
    부르던 때는 결정 하나가 요청 여럿이 되고, 그때마다 서버가 적응도를 깎을
    빌미가 됐다. 클라이언트는 **서버와 달라진 것만** 보내고(역할·1·2군), 라우트는
    승격 → 배치 → 강등 → **역할** 순으로 적용한다(역할이 마지막인 이유: 방금
    선발이 된 선수에게도 걸려야 한다).
  - **스쿼드 = 왼쪽 전술판 · 오른쪽 명단, 편집 모드 없음.** 화면에 보이는 게 곧 편집
    대상이고, 조작이 멈추면 자동 저장된다(저장·취소 버튼 없음). 칩을 끌어 자리를
    옮기면 **좌표가 포지션을 정하고**(볼란치를 조금 올리면 CM, 9번을 내리면 CF),
    칩끼리 겹쳐 놓으면 서로 교체된다. 겹침은 도메인이 풀어 카드가 서로를 가리지
    않는다. 벤치/예비 지정과 선발 교체는 오른쪽 명단에서 한다. 경기 중(`phase=match`)에는 **직접 저장되지 않고 지시가 된다** — 판에서 조작하면
    그것이 오퍼레이터 지시로 GM에게 가고(`onOrder`, 시간 이동 손잡이와 같은 경로),
    GM이 `substitute`·`set_tactics`·`set_player_tactic`으로 판정한다. 예전엔 판을
    통째로 잠갔는데, 그러면 감독이 손에 쥔 전술판을 두고 채팅에 "손흥민 빼고
    이강인"이라고 타이핑해야 했다. 경기 중 라인업·전술을 **직접 저장하지 않는**
    이유는 그대로다: 교체 횟수·적응도 대가·상대 반응이 걸려 있어 코어와 GM의
    판정을 거쳐야 한다. 1·2군 이동만은 경기 중에도 막힌다(경기와 무관한 등록 문제).
  - **스쿼드는 한 화면에 담기고, 스크롤은 명단 하나만 갖는다.** 페이지가 통째로
    스크롤되면 명단을 훑는 동안 전술판이 위로 사라진다 — 라인업을 고치려고 들어온
    화면에서 판이 보이지 않는 게 가장 나쁘다. 그래서 전술판은 **남는 높이에 맞춰
    줄어들고**(`.pitch-wrap`의 `container-type: size` + `min(100cqh, 133.333cqw)` —
    높이만 맞추면 폭이 열을 넘고 폭만 맞추면 세로로 넘친다), 명단이 남는 자리를
    다 쓴다.
  - **2열은 명단이 표를 담을 수 있을 때만 성립한다.** 명단 표의 최소 폭이 560px라
    전술판 열(400)과 여백을 빼면 화면이 **1024px**는 돼야 한다 — 그 아래에서 2열을
    고집하면 명단이 322px로 눌려 표가 가로로 스크롤된다(아이패드 세로에서 실제로
    그랬다). 1024 미만은 한 열로 쌓고 스크롤을 페이지에 돌려주며, 전술판과 전술
    패널은 **같은 폭으로 가운데** 정렬한다(판만 좁고 패널만 화면 폭이면 한 덩어리로
    안 읽힌다).
  - **세로도 레이아웃의 변수다.** 전술판 높이는 "남는 높이 − 전술 패널"이라
    세로 768~800px(가로형 태블릿·노트북)에선 판이 230px까지 눌린다. 그래서
    ① 머리글 요약을 **한 줄**로 깔고(세로로 쌓으면 84px를 먹는다) ② 세로가 짧으면
    전술 패널을 촘촘히 하며 ③ 세로가 넉넉한 넓은 화면(≥1200×860)에서만 전술판 열을
    460px로 넓힌다 — 판이 높이에 눌리는 화면에서 열만 넓히면 좌우가 빈다.
  - **화면은 서버 왕복을 기다리지 않는다.** 스쿼드의 자동 저장은 3초 디바운스 +
    왕복이라, 칩·명단·상세가 저마다 서버 값(`slotOverall`·`assignedPosition`·
    `roleId`)을 보면 역할을 바꿔도 숫자가 한 박자 뒤에 따라온다 — "이 역할로 바꾸면
    얼마가 되나"를 손으로 더듬어 볼 수가 없다. 지금은 **로컬 배치·역할이 단일
    소스**이고 `roleFit`·`positionProficiency`·`adaptationOf`가 전부 도메인 순수
    함수라 화면이 직접 조합한다(212ms 안에 칩과 명단이 **같은 값**으로 바뀐다).
    ⚠️ 다만 **처음부터 다시 계산하지 않고 차이만 얹는다** — 화면의 OVR은 스카우팅
    오차를 지나온 값이라(적응 중인 새 영입은 흐리다) 재계산하면 안개가 걷힌다.
  - **`set_lineup`은 조정해 둔 좌표를 건드리지 않는다.** 채팅으로 선수 하나를
    바꿔도 나머지 자리는 그대로다 — 우선순위는 `point`(전술판에서 찍은 점) →
    `position`(코드 지정: 이미 그 코드면 미세 조정 좌표 유지) → **기존 선발이면
    좌표 그대로** → 빠진 선수 자리 물려받기 → 프리셋 순이다. **배열 순서로 자리를
    정하지 않는다** — 순서가 자리를 정하면 목록을 다시 보내는 것만으로 전원이
    프리셋으로 튕긴다. 전면 재배치는 **포메이션을 바꿀 때만** 일어난다(`setTactics`).
  - **포메이션 이름은 UI가 고르는 값이 아니라 배치에서 읽는 파생값**이다
    (`shapeOf` — 칩을 옮기면 헤더 숫자가 즉시 따라 바뀐다). 그래서 전술판에는
    포메이션 셀렉트가 없고, **프리셋 교체는 채팅**(`set_tactics`)이 유일한 경로다
    — 전술판 저장은 좌표만 보내고 `spec.formation`을 건드리지 않는다. 프리셋을
    바꾸면 엔진이 선발 좌표를 그 프리셋으로 되깔고 적응도를 −25 한다.
  - **사건이 될 만한 것만 사건이다** — 골·슛·선방·카드는 `MatchEvent`, 패스·전진
    패스·xG 합은 `MatchLedgerState.stats`에 숫자로 쌓인다. 패스 900회를 사건으로
    만들면 장부가 폭발하고 정작 골이 묻힌다. 흐름의 양은 굴리지 않고 **나눈다**
    (구간 길이 × 점유 × 템포 → 자리·`passing`·`vision`으로 배분). **전진 패스
    비율은 전술 × 선수 성향**이다 — 시야 0.5 · 킥 0.3 · 침착성 0.2(`daring`).
    전술은 방향을 정하고 그걸 실행하는 건 선수라, 같은 지시에도 앞을 보는 선수는
    라인을 넘기고 못 보는 선수는 옆으로 돌린다(같은 팀 안에서 23~38%로 갈린다). **슛에는 xG가
    붙고**(`MatchEvent.xg` — 슛 평균 0.1, 골 0.3 안팎) 팀 xg 예측과 견주면 "기회를
    만들었나 vs 넣었나"가 갈린다. ⚠️ 한 값은 한 곳에서만 온다 — 뷰의 슛·선방·패스는
    `stats`가 원본이고 골·도움·카드만 사건에서 센다.
  - **도움은 결과까지 살아남는다** — `MatchResult.assists`는 `scorers`와 같은
    순서·길이·형식(`"home:playerId"`)이고 단독 득점은 빈 칸이다. 예전엔 득점자만
    남겨서, 코어가 골의 68%에 붙인 도움이 경기가 끝나는 순간 사라졌다(시즌 합계
    숫자 말고는 어디에도 없었다). 유저 경기와 타 팀 퀵심이 같은 규약을 쓴다.
  - **중계의 스코어는 화면에 붙어 있다** (`position: sticky`). 판세·전술·명단을
    훑느라 아래로 내려가면 지금 몇 대 몇인지가 시야에서 사라지는데, 중계에서 그건
    늘 보여야 하는 하나다 — 나머지는 그걸 설명하는 것들이다. 바로 아래 **득점
    기록**(분·득점자·도움)이 서서 "누가 넣었나"를 중계로 거슬러 찾지 않게 하고,
    선수 행에는 **골·도움·카드만 칩**으로 붙는다(슛·선방은 툴팁). 열한 행에 다
    세우면 표가 숫자밭이 되어 정작 골이 안 보인다. 기록은 전부 `ledger.events`의
    파생이라 장부와 갈릴 수 없다.
  - **경기 중에는 탭이 셋으로 갈린다** — 중계 채팅은 흘러가지만 판세는 남아 있어야
    한다. 감독이 정지점에서 묻는 것이 셋뿐이라 탭도 셋이다 (match-sim §2.3):
    **판세**(어디가 밀리나 + 왜 — 존 막대와 키포인트가 **한 화면**이다. 갈라 두면
    밀리는 걸 보고 이유를 찾으러 건너가 다시 읽어야 한다) · **팀**(전술판 + 명단,
    우리/상대 하위 탭) · **대회**(순위표 + 다음 경기). 인원이 줄면 **퇴장을
    적는다** — 표에서 사라진 이름을 감독이 스스로 추리하게 두지 않는다.
    킥오프 전·종료 후에는 탭 자체가 없다(`views.match === null`).
    - **전술 6축은 판세가 아니라 전술판이 갖는다** — 거기서는 읽는 김에 **고칠 수도**
      있고(경기 중엔 그 한 줄이 곧 지시다), 두 곳에 같은 값을 세우면 어느 쪽이
      진짜인지 흐려진다.
    - **우리 팀과 상대 팀은 같은 구성, 다른 정확도** — 양쪽 다 판 → 전술 → 명단
      순으로 읽힌다. 배치를 맞춰 둬야 오갈 때 눈이 매번 자리를 다시 찾지 않는다.
      상대 쪽은 조작이 없고, 체력은 구간이며 전력에는 `±오차`가 붙는다
      (**명단 화면과 같은 채널** `observationOf` — 갈리면 같은 선수가 두 화면에서
      다른 숫자로 보인다). 다만 **전술 6축은 흐리지 않다**: 라인이 올라가고 압박이
      들어오는 건 90분 동안 그냥 보이는 사실이라 흐릴 것은 선수의 수치뿐이다.
      상대 판의 좌표는 `anchorOf` + `separateBoardPoints`(기본 자리) — 남의 판을
      훔쳐볼 수는 없다.
    - **달력 탭은 없다** — 90분 안에 볼 것이 아니다. 그때 궁금한 일정은 다음 경기
      하나뿐이라 대회 탭 맨 아래 한 칸(`competitions.nextMatch`)으로 옮겼고,
      **며칠 남았는지를 크게 적는다**(체력 회복이 며칠에 걸리므로 "사흘 뒤"가 곧
      로테이션 판단이다). ⚠️ 그 계산에서 **진행 중인 경기는 뺀다** — 결과는 종료
      시점에 쓰이므로 그냥 고르면 "오늘 · 지금 상대"가 뜬다.
  - **달력 칸에 글자가 오르는 건 경기뿐이다** — 경기(스코어·상대)와 추첨 전
    컵 라운드(대회·단계)만 칩으로 서고, 나머지 일정은 **점 하나**로 알린다:
    훈련은 노랑, **추첨은 보라**(그날 상대가 정해지는 날 — 다른 일정과 종류가
    다르다), 그 밖(이적창 개폐)은 파랑. 무슨 일정인지는 툴팁과 칸을
    눌러 여는 상세가 말한다. **상세는 고른 날이 있는 그 달 카드 안에서 펼쳐진다**
    — 화면 맨 위에 두면 3월 칸을 눌러도 패널이 시야 밖에서 열려 아무 일도 안 난
    것처럼 보인다 (명단의 선수 상세와 같은 결: 선택한 칸과 같은 배경 + 왼쪽 강조 띠).
  - **순위표는 등수가 아니라 경계를 보여준다** — 4위와 5위의 차이는 한 계단이 아니라
    챔피언스리그와 유로파리그의 차이다. 순위 칸 왼쪽 색 띠 + 구역 끝의 경계선 +
    표 아래 한 줄 범례(색·이름·순위 범위). 구역은 UI가 고르는 값이 아니라
    **대회 카탈로그의 티켓 수에서 파생되는 사실**이다(`buildStandingZones` —
    리그는 UCL/UEL/UECL 진출권, 대항전 리그 페이즈는 본선 직행/플레이오프).
    ⚠️ **강등 구역은 없다** — 승강 처리가 미구현이라(game-overview §16) 선을 그으면
    지키지 않는 약속이 된다.
- 테스트: Vitest 958 (유닛·API 통합) + Playwright e2e 7. `pnpm test` / `pnpm e2e`

### LLM 입력 (요약 — 상세는 docs/design/llm-io.md)

입력은 **변경 빈도** 순 3층이다. 앞 두 층은 프롬프트 캐시(0.1×), 마지막 층만
매 턴 정가(300~400 토큰)로 읽힌다.

1. **고정** — 도구 정의 + 시스템 프롬프트 (세이브 무관)
2. **레퍼런스** — 감독 프로필 + **수석코치 인물 카드** + 선수 명부
   (`id|이름|주포지션`) / 경기 땐 전력 패킷.
   ⚠️ **명부는 조회 표가 아니라 사람들이다** — 헤더가 "도구 입력엔 id, 서사엔 이름"
   이고 그 아래 "수치가 필요하면 `search_players`"가 붙어 있던 때는, 둘 다 출력
   문법·철칙 3에 이미 있는 중복인데 그 중복이 블록을 **id를 찾는 표**로 못박았다.
   그러면 카드가 주어진 코치·구단주·기자만 화자로 읽히고 서른 명은 조회 대상이
   되어, **한 번 이름이 불린 선수만 계속 말한다.** 지금은 헤더가 "이 이름들이 모두
   화자다"이고 안내 두 줄은 지웠다. 철칙 11도 "인물은 매번 같은 사람이다"에서
   "카드가 있는 화자는 카드대로 말한다"로 고쳤다 — 앞의 문장은 목소리의 일관성을
   말하려던 것인데 **출연진이 고정이라는 뜻으로 읽힌다.**
3. **이력** — 지난 턴들 (시작점을 6턴 단위로만 이동 — 프리픽스 안정).
   ⚠️ **평시와 경기가 갈린다** (`relevantTurns`) — 한 이력에 섞으면 중계 모델이
   이적 협상 스무 턴을 읽고 평시 GM이 중계 수십 턴을 읽는다. 토큰만 먹는 게 아니라
   **맥락을 오염시킨다**(경기 중에 "이적료" 이야기가 이력에 있으면 중계가 끌어온다).
   대신 **두 다리**로 잇는다: 경기로 들어갈 땐 `buildMatchBrief`(직전 평시 턴의 감독
   발화 3개를 **그대로** — 요약하면 말투와 의도가 가장 먼저 사라진다), 경기가 끝나면
   `matchDigest`(결과·득점·최고 평점)가 상태 스냅샷에 실려 평시로 넘어온다.
   다이제스트는 **장부에서 코어가 뽑는다** — LLM 요약을 한 겹 더 태우면 틀릴 여지만 는다.
4. **이번 턴** — 감독 발화 + 상태 스냅샷(`role:"system"` 오퍼레이터 채널)

능력치·컨디션·배치·타 팀·순위·일정·과거는 컨텍스트에 넣지 않고 **읽기 전용 조회
도구**로 온디맨드 제공한다 — `search_players`/`get_player`/`get_squad`/`get_team`/
`get_league`/`get_schedule`/`get_career`.
조회는 **창이 아니라 검색**이다 — `get_league`의 일정은 상대·대회·라운드·날짜
범위·방향(지난/예정)으로 좁혀 "다음 맨유전", "토트넘과 지난 맞대결"에 정확히
답하고, `search_players`는 대회로 풀을 좁히며 **무엇을 뒤졌는지** 헤더에 밝힌다
(좁히지 않으면 5대 리그 전체가 풀이다). 팀·대회 이름은 약칭까지 해석하고 모호하면
후보를 돌려준다. 답할 도구가 없으면 모델은 지어내므로, 현재 배치(`get_squad`)·
훈련 일정(`get_schedule`)·지난 시즌(`get_career`)처럼 컨텍스트 밖 사실에는 각각
읽을 자리를 준다. `state.phase`는 **라우팅 전용** — 모델 입력에 넣지 않는다.

**선수 근황 — 세계에 지금 무슨 이야기가 있는가** (`engine/cues.ts` → 상태 스냅샷).
스냅샷이 선수 이름을 내보내는 자리는 원래 부상·정지·불만 셋뿐이었다. 셋 다 몇 주씩
바뀌지 않아 GM이 아는 "이야기가 있는 선수"는 늘 같은 두세 명이고, 나머지 서른 명은
명부에 이름만 있는 사람이 된다 — **모델이 다른 선수를 세울 근거가 없다.** 그래서
1군 선수의 사실 한 조각을 최대 3명분 싣는다: 폼이 절정이거나 바닥 · 복귀 임박 ·
아직 적응 중인 새 영입 · 연속 명단 제외. 회전은 두 축이다 — **최근 여섯 턴에 말한
사람은 뒤로 밀고**(지우지 않는다: 어제 말한 선수라도 오늘 부상에서 돌아왔으면 그게
더 큰 사건이다) 나머지는 **날짜로 굴린다**(근황은 며칠씩 그대로라 정렬만으로는 같은
이름이 몇 주 내내 맨 앞이다). 결정적이라 같은 날 같은 세이브면 같은 목록이다.
⚠️ **코어가 내놓는 것은 사실뿐이다** — 누가 말해야 하는지도, 그 사람이 할 말도
정하지 않는다(회견의 `PressFact`와 같은 결). 장면을 여는 사람은 그 일에 가장 가까운
사람이라는 규칙이 프롬프트에 이미 있고, 문장은 GM이 쓴다.

**정보 비대칭(안개)** — 선수 단위 5단계. 우리 선수·스카우팅 완료는 정확,
직접 상대해 본 선수는 ±3, 평판뿐인 선수는 ±6 오차(숫자 대신 라벨). **잠재력은
누구도 단정하지 못한다** — 숫자가 아니라 구간이고, 우리 선수는 출전이 쌓이면
(±6→±2), 타 팀은 스카우트를 거듭 보내면(±12→±6) 좁아진다. 지식 수준은 저장하지
않고 출전 명단·스카우트 리포트에서 파생하며, 오차는 시드 해시로 **결정적**이다
(같은 질문에 같은 답 · 참값은 항상 구간 안에 있다).

⚠️ **경기 중 체력은 이 5단계를 쓰지 않는다** (`readCondition` — attribute-model
§3.2). 리포트는 "이 선수가 어떤 선수인가"를 좁히지 오늘 저 다리에 얼마가 남았는지는
말해 주지 않고, 반대로 처음 보는 선수라도 걸어 다니기 시작하면 보인다. 폭은
**둘의 합**이다 — **출발점을 아는가**(우리 2 · 상대 12. 우리 쪽도 0은 아니다:
측정값이 있어도 "지금 쓸 만한가"는 끝내 판단이다)와 **90분이 얼마를 가져갔나**
(소모량 × 우리 0.15 · 상대 0.28 — 이건 **아무도 실시간으로 못 잰다**). 그 위에
감독의 **분석**이 곱으로 걸려 99면 `ANALYSIS_FLOOR`(0.35)배까지 좁아진다 —
남의 다리도 내 다리도. 90분 기준 우리 ±7~±2 · 상대 ±22~±8이고, **킥오프엔 거의
정확하고 후반 막판이 가장 흐리다** — 정작 교체를 정해야 하는 그 순간에.
**누구도 값 하나로 서지 않는다**(최소 ±1): 0을 허용하면 그 선수만 안개 밖에 있게
되고 화면은 그걸 "잰 값"으로 읽는다. 편향은 **경기마다 한 번** 뽑혀 90분 내내
흔들리지 않는다 — 정지점마다 값이 튀면 상대가 지치는 중인지 내 눈이 흔들리는지
구분할 수 없다. ⚠️ **다리가 멈춘 건 안개가 못 가린다**: 추정 구간은 구멍 문턱
(`GAP_CONDITION` 22)을 넘지 않고, 같은 이유로 구멍 키포인트에 소진 수치를 적지
않는다 — 안 그러면 흐린 값이 문장으로 새어 나오고 "구멍"이라 적힌 선수의 막대가
멀쩡해 보인다. 화면은 확실한 만큼만 채우고 그 위로 모르는 폭을 흐리게 얹는다.
경기 밖(스쿼드·조회 도구)의 우리 선수 체력은 그대로 정확하다.

**막 영입한 선수는 우리 선수여도 흐리다**(`adapting`) — 훈련장에서 본 게 전부라
스카우트 수준의 오차가 남는다. **언제 걷히는지는 달력이 아니라 감독이 정한다**
(`settling.ts`). 예전엔 42일 고정이라 매 경기 선발로 뛴 선수와 벤치에서 여섯 주를
보낸 선수가 같은 날 적응을 마쳤다 — 감독이 무엇을 하든 결과가 같으면 그건 규칙이
아니라 타이머다. 지금은 **크레딧**이 쌓여 목표에 닿으면 끝난다: 경기 출전 8 ·
팀 훈련 1.5 · 그냥 보낸 하루 0.5. 필요량은 그 이적이 얼마나 큰 변화였는지가
정한다(나라를 건넜으면 ×1.3 · 라커룸에 동포가 있으면 ×0.85 · 21세 이하 ×1.2 ·
30세 이상 ×0.9). 주전으로 쓰면 한 달, 아무 데도 안 쓰면 반년이다.
**아직 못 쓰는 선수에게도 감독이 할 일이 있다** — 면담(앵커 +5)·팀토크(+1.5)·
주장 지명(+15)이 `SETTLING_EVENT` 원장에 남아 정착을 앞당기고, 몰아세우면 음수로
뒤집힌다. **무게는 GM이 정한다** — 같은 "격려"라도 통역을 붙여 준 이야기와
지나가며 한 말이 다르므로 스킬이 `settling` 인자를 받고, 코어는 앵커 ±`EVENT_BAND`
안으로만 받는다(경기 평점과 같은 구조). 대화는 어디에도 기록이 안 남아 파생할 원본이 없어서 이것만 저장한다
(옛 세이브는 빈 배열 — 버전을 올리지 않는다). **하루에 한 번**만 쌓여 면담 연타가
최적 전략이 되지 않는다. 진행률은 명단 OVR 옆 `?`의 툴팁이 갖고, **남은 일수는
어디에도 쓰지 않는다** — 얼마나 걸릴지는 감독이 앞으로 무엇을 하느냐에 달렸다.

선수 데이터는 EPL 실선수 777명(팀당 26~48명, 유망주 포함 · **2026-27 개막
시점** 등록 스쿼드 — `packages/engine/src/data/epl-players.ts`에서 카탈로그로
파생, 라이선스 부채는 data-sourcing.md §7). ⚠️ 시드 파일에는 팀 카탈로그에서
빠진 4팀(웨스트햄·울버햄튼·레스터·사우샘프턴)도 남아 있다 — `buildFromSeed`가
카탈로그를 순회하므로 읽히지 않고, 챔피언십을 추가할 때 되살아난다.
시드는 축마다 출처가 다르다 —
**명단·생년월일은 위키백과**(구단 문서의 1군 등록 스쿼드 + 시즌 문서의
이적·임대·방출 표), **능력치 6축은 EA FC 27 공개 등급**, **주급은 공개 급여
집계**. 어긋나면 명단은 위키, 포지션은 기존 시드를 우선한다(EA는 윙어를
LM/RM으로 적어 포지션군 구성이 흔들린다). 유럽 4대 리그(`eu-squads.ts`)는
아직 2025-26 기준이라 EPL과 시점이 다르다.

**주급은 시드가 원본이고, 없으면 모델이 계산한다** — `RealPlayerSeed.weeklyWage`가
새 게임의 초기 `CONTRACT`에 그대로 실린다(`initialWages`). 실측이 모델보다 정확하고,
무엇보다 **구단마다의 특수 사정**(장기 계약을 남긴 베테랑, 팔지 못한 고액자)은 모델이
만들어낼 수 없는 사실이다 — 그 왜곡이 곧 PSR·재계약 서사의 재료다.

값이 없는 선수(유럽 4대 리그·합성 선수)는 **`wages.ts`가 실제 급여 변인으로 계산**한다:
**구단 예산**(리그 임금 수준 × 성적 등급 × 브랜드 규모)을 **개인 지분**(스쿼드 서열 ×
나이 × 포지션)대로 나눈다. 계수는 EPL 20구단 644명의 공개 급여에서 적합했다.

⚠️ **주급은 OVR에 매달리지 않는다.** 예전엔 `wageForOverall(overall)` 하나였는데,
파생 축을 실측에 맞춰 OVR 눈금이 +2 오르자 리그 전체 임금이 부풀어 하위 구단이
파산했다 — 능력치 보정과 재정이 한 축에 묶여 있었다. 지금은 서열이 **서수**라
눈금이 통째로 움직여도 주급이 그대로다(테스트로 고정). 능력은 "얼마짜리 선수인가"가
아니라 "이 팀에서 몇 번째인가"로 들어간다. 나이가 서열 다음으로 큰 변인이다 —
18세는 아무리 잘해도 아카데미 계약(×0.06)이고, 30대는 전성기 장기 계약이 남아 높다.

주급이 리그를 알게 되면서 재정의 **균등 배분 리그 배율도 켰다**
(`EQUAL_SHARE_LEAGUE_SCALED` — 끄고 있던 이유가 사라졌다).

**2부 64클럽은 실선수 시드 없이 20명씩 절차 생성**한다 — 국내 컵에만 나오므로
로테이션·유스 육성이 필요 없다.

⚠️ **세이브 호환성** — v6는 스키마 전면 개편이라 이전 세이브를 로드하지 않는다
(`SAVE_VERSION=6` 미달 파일은 목록에서 스킵). 새 테이블은 로드 시 빈 배열로 채우고
새 필드는 optional로 둬서 버전을 올리지 않는 것이 원칙이다 (재정 v1이 그 방식).
구조 변경 시 `.data` 초기화가 필요하면 사용자에게 먼저 확인한다.

미구현(다음 마일스톤): **이적·계약 협상 스킬**(v6 구조는 준비됨 — TRANSFER에 status를 얹으면 오퍼/합의
흐름이 된다. 스카우팅으로 "누구를 노릴지"까지는 이미 가능하다), **보드**(구단주 요청·경고),
페르소나 시스템, 서사 아크, 토큰 예산 상한
— docs/design/implementation-notes.md 참고.
