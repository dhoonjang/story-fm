import path from "node:path";
import { defineConfig } from "vitest/config";

export default defineConfig({
  resolve: {
    alias: {
      "@": path.resolve(import.meta.dirname, "apps/web"),
    },
  },
  test: {
    // 시뮬 코어(장부·패킷)는 LLM 없이 단위 테스트한다 (AGENTS.md 5장)
    // apps/web/test는 API 통합 테스트 (mock GM 모드)
    include: ["packages/*/test/**/*.test.ts", "apps/*/test/**/*.test.ts"],
    /**
     * 케이스 시간 상한 — **멈춘 것을 끊는 자**이지 속도를 재는 자가 아니다. 속도를
     * 재는 자리는 하네스이고(`vitest.balance.config.ts`), 회귀를 잡는 자는 케이스의
     * 단언이다 (→ docs/simulation/balance-harness.md §6).
     *
     * ⚠️ 케이스에 상한을 따로 적지 마라. 이 값을 옮겨 적은 것은 여유가 아니고, 이
     * 값보다 낮은 것은 숨은 속도 단언이다 — 케이스가 자라면 단언이 아니라 상한이 먼저
     * 빨개지고, 저자는 자기 변경의 회귀인지 상한의 문제인지 로그로 가리지 못한다.
     * vitest에 **파일별 상한은 없다**. 손잡이는 이 전역과 케이스 인자 둘뿐이고, 지금
     * 스위트에 케이스 인자는 하나도 없다.
     *
     * ── 왜 10분인가
     *
     * 벼랑에 선 것은 한 시즌을 하루씩 도는 통합 케이스 둘이고, 나머지 2,913개는
     * 자기 상한의 12% 아래다(가장 가까운 것이 `season.test.ts`의 시즌 전환 —
     * 3.6초 / 30초). 잰 값:
     *
     *   euro-knockout 「한 시즌 완주」  12코어 맥 단독 42.4초 · 로컬 전체 65.3초
     *                                   CI(ubuntu-latest 4코어, 샤드 2/2) 117.4초
     *   training-plan 시즌 훑기          로컬 전체 58.3초 (같은 describe의 나머지 둘은
     *                                   그 결과를 캐시에서 받아 0초다)
     *
     * CI에서 117.4초였던 것(#608 직후)이 22커밋 뒤 상한 120초를 넘어 main을 빨갛게
     * 만들었다(run 32948509878). **케이스가 하는 일은 그 사이 바뀌지 않았다** — 같은
     * 커밋 범위에서 단독 실행이 39.3초 → 42.4초로 8% 무거워졌으니, 사라진 여유는
     * tick이 가져간 것이다. 케이스를 쪼개도 되돌아오지 않는다.
     *
     * 러너 부하만으로도 그만큼이 흔들린다 — 인접한 두 커밋의 같은 샤드가 30분 사이에
     * 테스트 CPU 916초 ↔ 1,267초였다(run 32945662127 ↔ 32948509878). 그래서 상한은
     * 「지금 값 + 조금」이 아니라 **나쁜 러너에서의 값 × 자랄 자리**여야 한다.
     *
     * 천장은 `ci.yml`의 `timeout-minutes: 25`가 쥔다. 샤드 벽시계가 지금 4~7분이므로
     * 케이스 하나가 10분 멈춰도 잡은 살아서 나머지 판정을 보고한다 — 잡이 죽으면 그
     * 샤드의 결과가 통째로 사라지고, 그것이 케이스 하나가 빨간 것보다 나쁘다.
     */
    testTimeout: 600_000,
    /**
     * `pool`·`isolate`·`maxWorkers`는 손대지 않는다 — 재 봤고, 줄지 않는다.
     *
     * 이 스위트는 CPU에 묶여 있다. 잰 값(12코어 맥, `--maxWorkers=8`이 8코어
     * 러너와 같은 자리에 선다 — 로컬 185초 · CI 216초):
     *
     *   pool=forks(기본) 185초 · pool=threads 184초 — 차이가 노이즈 안이다
     *   isolate=false     199초, import CPU는 15% 줄지만 `apps/web/test/api.test.ts`가
     *                     깨진다. 카탈로그와 세이브 저장소가 모듈 전역이라 파일
     *                     사이로 샌다 — 격리를 끄면 그 전제가 무너진다
     *   maxWorkers 6/8/12/16/24 → 222/185/183/191/184초. 여덟에서 이미 평평하다
     *
     * import이 전체 CPU의 30%지만 그건 파일마다 엔진 모듈 그래프를 다시 **평가**하는
     * 비용이고, 격리를 꺼도 4%밖에 줄지 않았다.
     *
     * 가장 느린 두 파일(euro-knockout 68초 · training-plan 54초)을 쪼개는 것도 답이
     * 아니다 — 꼬리가 아니라 총 CPU가 벽이라, 같은 일을 파일 여럿으로 나누면 그
     * 자리에 다른 파일이 설 뿐이다.
     *
     * 테스트를 **지워서** 줄이는 길도 없다. 같은 자리에서 이어 잰 값:
     *
     *   120파일 전부                                  196초 (CPU 842초)
     *   가장 무거운 2파일 제외                        187초 (CPU 758초)
     *   가장 무거운 10파일 제외 — 테스트 시간의 46%   163초 (CPU 566초)
     *
     * 259개를 통째로 버려도 17%다. 남는 110파일이 다시 파일당 2.2초씩 import를
     * 내기 때문이다 — 이 스위트의 값은 몇몇 무거운 케이스가 아니라 파일 수다.
     */
  },
});
